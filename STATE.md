# بيان — حالة المشروع

> **المصادر الصادقة — اقرأ ولا تنقل.** كل رقمٍ هنا منقولٌ، وكلُّ منقولٍ ينحرف.
>
> | المعلومة | مصدرها الوحيد |
> |---|---|
> | نسخة الواجهة | `BUILD` في `js/ui.js` سطر ١٠ — الآن `b41` |
> | حالة القاعدة | **`select … from sql_log`** — انظر القسم ٠ · أ. ولا رقمَ هنا |
> | المخطّط | `sql/00_schema.sql` — **لقطةٌ مؤرَّخة** (٥ سبتمبر)، لا حقيقة |
> | أعداد المحتوى | استعلام القسم ② — لا هذا الملف |
>
> ⚠️ سبق أن قال الرأس `b19` والشيفرة عند `b30`، ثم `b30` والشيفرة عند `b38`.
> والانحراف يتكرّر لأن النقل عادة. **إن شككت، افتح المصدر.**
>
> 🔴 **وفي ٥ سبتمبر كلّف هذا الانحرافُ هدماً لا خطأً في رقم:** قُرئ
> `00_schema.sql` وهو مستخرَجٌ عند الملفّ `50`، فاستُبدلت `get_quiz` و
> `quiz_readiness` و`quiz_for_edit` بنسخٍ أقدم ⇒ **سقط نمط `gap`
> والإعدادات** (`plays` · `reveal` · `shuffle` · `tool` · `station`).
> استُرجعت بإعادة تشغيل `63` و`65`، وحلّ محلّها `75`.
>
> ⇒ **قاعدةٌ لا تُخرق: لا `create or replace` قبل `pg_get_functiondef`
> للدالّة نفسها.** المخطط لقطةٌ مؤرَّخة، والقاعدة هي الحقيقة.

---

## ٠ · البدء والختام — يُقرأ أولاً ويُعمَل به

> **كلُّ محادثةٍ في هذا المشروع تبدأ من هنا وتنتهي إليه.**
> لا لتنظيم، بل لأن تخطّي هذه الخطوة كلّف هدماً في ٥ سبتمبر ٢٠٢٦.

### أ · قبل أن تكتب سطراً — ثلاثة استعلامات

```sql
-- ① أين نحن؟  (يحلّ محلّ أيّ رقمٍ مكتوبٍ في هذه الوثيقة)
select n, title, coalesce(applied_at::date::text,'—') as طُبِّق, note
  from sql_log order by sql_log_sort(n), n;

-- ② بمَ تعمل الواجهة؟
--    BUILD في js/ui.js سطر ١٠ — يُفتح ولا يُنقَل

-- ③ الأعداد — إن لزمت
select (select count(*) from pg_tables   where schemaname='public') as جداول,
       (select count(*) from pg_proc     where pronamespace='public'::regnamespace) as دوالّ,
       (select count(*) from pg_policies where schemaname='public') as سياسات;
```

### ب · القواعد التي كلّفت ثمناً — لا تُخرق

**① لا `create or replace` قبل `pg_get_functiondef` للدالّة نفسها.**
اقرأ الحيّ، وقارنه بما عندك، ثم استبدل. **`00_schema.sql` لقطةٌ مؤرَّخة
لا حقيقة** — وقراءتُه وحده هدمت `gap` والإعدادات في ٥ سبتمبر.

**② `STATE.md` حُكمٌ لا مصدرَ أرقام.** كلُّ رقمٍ هنا منقول، وكلُّ منقولٍ
ينحرف. الأرقام في القاعدة، والاستعلامات أعلاه تجلبها.

**③ لا تُحِل إلى ملفٍّ قبل أن يوجد.** «طبِّق `sql/58`» بقيت شهوراً وهو
ملفٌّ لم يُكتب قطّ — نيّةٌ صيغت أمراً فصُدِّقت.

**④ الفحص يُصاغ لكلّ حالةٍ بما يخصّها.** اختبارٌ واحد على ثلاثة أشياء
يُنتج «فشلاً» لا يعني شيئاً، ويُدرَّب الناظر على تجاهل الإنذار.

**⑤ الفجوة تُرى والصمت لا يُرى.** رقمٌ ملغًى يُسجَّل بسببه، وقرارٌ
يُشرَح في موضعه، وعطلٌ مؤجَّلٌ يُكتب — **والصامت أخطر من الصاخب دائماً.**

### ج · عند الختام — أربعة

```
① كلُّ ملفّ SQL يُختَم بسطره:
   insert into public.sql_log (n, title, applied_at)
   values ('NN', 'العنوان', now())
   on conflict (n) do update set applied_at = now();

② كلُّ قرارٍ لا يُقرأ من الشيفرة يُكتب في موضعه —
   في تعليق الملفّ إن خصّه، وفي ⑤ هنا إن خصّ المنصّة.

③ «حيث انتهينا» أدناه يُحدَّث: ما أُنجز يُشطب، وما ظهر يُضاف.

④ إن تغيّرت الواجهة: BUILD يُرفَع في js/ui.js.
```

### د · حيث انتهينا — ٥ سبتمبر ٢٠٢٦

**جاهزٌ للتشغيل:**
- `76` سجلّ الهجرات — **لم يُشغَّل بعد.** أوّل ما يُنفَّذ.

**يلزم استخراجٌ قبل أن يُنسى:**
- 🔴 `48` سياسات التخزين **مطبَّقةٌ في القاعدة ولا نصَّ لها في `sql/`**.
  ⇒ تُستخرَج من `storage.objects` ملفّاً `77`. والقسم `⑪` في المخطّط
  مكتوبٌ بيدٍ لا مولَّد، فقد لا يطابقها.

**تصحيحاتٌ صغيرة مؤجَّلة:**
- رأس `00_schema.sql` يقول «`٠١ إلى 68`» والصواب `75`.
- رأس `50` يقول «`45 ·`» ورأس `75` يقول «`68 ·`» — رقمٌ نُسخ ولم يُصحَّح.

**فخّان نائمان — لا ضرر اليوم:**
- `list_quizzes` بلا حرّاس تقاعد (⚫ ميتة · ٠ نداء).
- `question_count` يعدّ المتقاعدين في `save_question` و`duplicate_question`.

**بُني وينتظر خطوةً تالية:**
- شارة «نسخة ٢» على الوريث — المسار (ج): التاريخ يُقرأ عند السؤال
  لا في الشريط. تحتاج حقلاً في `quiz_for_edit` ونداءً كسولاً للسلف.
- `tool_readiness` لا تفحص ما يقع عند `done` — **أوّل جلسة تسكينٍ كاملة
  تُجرَّب قبل النشر للطلاب.** (وبها ظهر عطل `67ب`.)

**قراران تربويّان ينتظران — لا يُحسمان بالشيفرة:**
- ما مصير درجة طالبٍ أُبطل سؤالٌ من اختباره؟ (الإبطال ≠ التقاعد:
  التقاعد يمنع المستقبل، والإبطال يصحّح الماضي.)
- هل يُخبَر الطالب أنه أجاب عن نسخةٍ صُحّحت بعده؟

**عملٌ لم يبدأ:**
- 🔴 إحكام `EXECUTE` — انظر `⑦ الأعطال`. خطوةٌ قائمة بذاتها تمسّ كلَّ
  دالّة، وتُجرَّب بحساب طالبٍ ومعلّمٍ ومدير.

---

## ① الروابط

- المنصة: https://bayan-edu.github.io · Supabase: `tjzrfevymfpktfuormhs`
- `bayan-edu.github.io` (عام) · `quiz-content` (خاص)
- `bayan-backup` (خاص · نسخ أسبوعية · استعادة مجرَّبة ١٧ أغسطس)
- ⚠️ مشروع `Test-Prep` (نموذج أوّلي · موقوف) — يُصدَّر أو يُحذف

---

## ② الأرقام

### ٢-١ بنية المنهج — مقروءة من `00_schema.sql` ⑨

هذه ثابتة في ملفّ المخطط، فيصحّ نقلها ما دام الملفّ هو المرجع:

```
٦ سلالم  ·  ١٨ صفاً/مستوى  ·  ٤ مسارات  ·  ٢٠ مادة  ·  ٤٠ مقرَّراً
١٣ نمط مصدر (item_kinds)   ·   ٢٧ كود تشخيص (dx_codes)
٣٤ جدولاً  ·  ٩٠ دالّة  ·  ٤٩ سياسة RLS  ·  عرضان  ·  ٣٤ تفعيل RLS
```

✅ ورؤوس الأقسام تطابق المتن منذ استخراج ٥ سبتمبر — صُحِّحت كلُّها.

⚠️ **لكنّ رأس الملفّ ما زال يقول «يقابل `٠١ إلى 68`» والصواب `75`**:
الملفّات `72`–`75` غيّرت القاعدة بعد `68`. الرؤوس مكتوبةٌ بيدٍ والمتنُ
مولَّد — **صدّق المتن دائماً**.

### ٢-٢ المحتوى الحيّ — يُقاس ولا يُنقل

```sql
select set_config('request.jwt.claims', json_build_object('sub',
       (select id from profiles where role='admin' limit 1))::text, true);

select (select count(*) from questions)                      as أسئلة,
       (select count(*) from quizzes)                        as اختبارات,
       (select count(*) from quizzes where published)        as منشورة,
       (select count(*) from questions
         where variant_key is not null)                      as موسوم,
       (select count(distinct variant_key) from questions)   as خانات,
       (select count(*) from lessons where published)        as دروس_منشورة,
       (select count(*) from objectives)                     as أهداف,
       (select count(*) from profiles where role='student')  as طلاب;
```

**آخر قياس (أغسطس ٢٠٢٦ — يحتاج تجديداً):**

```
اختبارات: ٤ — كلها منشورة
اختبار ٣ (إنجليزية · أول ثانوي · درس ١): ٥٤ سؤالاً · ١٨ خانة × ٣ نسخ
الاختبارات ١ و٢ و٤ بلا وسم — تُعرض كاملة
دروس منشورة: ٣   ·   ١٣ طالباً   ·   معلم/مدير واحد
```

🔴 **٤٠ مقرَّراً في القاعدة وثلاثة دروس منشورة.** العائق **محتوى لا تقنية**.
الأدوات جاهزة من طرفٍ إلى طرف، والباقي كتابة.

---

## ③ الخريطة — ما الذي يعمل الآن

| النظام | القاعدة | الواجهة | الحال |
|---|---|---|---|
| المنهج والهوية | `scales/levels/paths/subjects/courses` | `auth.js` | ✅ يعمل |
| التأليف | `save_*` · `author_tree` | `editor*.js` | ✅ يعمل |
| الاختبار والتصحيح | `get_quiz` · `submit_attempt` | `quiz.js` | ✅ يعمل |
| التشخيص | `dx_codes` · `dx_map` | شاشة المراجعة | ✅ يعمل |
| بنك الخانات | `variant_key` · `set_variant` | `editor_quiz.js` | ✅ يعمل |
| نمط msq | `41` · `42` · `43` · `44` | `quiz.js` | ✅ يعمل |
| الإرشاد | `choose_mentor` · `mentorships` | `student.js` | ✅ يعمل |
| الرسائل | `messages` + ٣ سياسات | `student/teacher.js` | ✅ يعمل |
| التصحيح المقالي | `attempts` (تحديث مباشر) | `teacher.js` | ✅ يعمل |
| الوسائط | جرّة `audio` + ٤ سياسات | `media.js` · `upload.js` | ✅ يعمل |
| تتبّع المصادر | `track_item` · `item_progress` | `student.js` | ✅ يعمل |
| الإدارة | `request_teacher_access` · `admin_*` | `auth.js` | ✅ يعمل |
| **المراجعة المتباعدة** | `review_queue` — **تُكتب** | — | ⚫ بلا شاشة |
| **حلقة العلاج** | `objectives.remedial_item_id` | `quiz.js` جاهز | 🔴 **بلا محتوى** |
| بلاغات الأسئلة | `question_reports` + سياستان | — | ⚫ بلا شاشة |
| الفصول | `classes` · `enrollments` | — | ⚫ بلا شاشة |
| التحليلات | `v_trap_analysis` جاهز | — | ⚫ بلا شاشة |

**الواجهة:** ١٢ وحدة JS. `api.js` وحدها تلمس Supabase · `media.js` وحدها
تبني الروابط · `upload.js` وحدها ترفع. وثلاث قواعد كلُّها قابلة للفحص بـ`grep`.

**الشاشات الثماني:** `subjects` · `feedback` · `chat` · `grade` · `inbox` ·
`my` · `editor` · `requests`.

---

## ④ التالي

1. 🔴 **جرّب البنك بحساب طالب** — حلّ · سلّم · **أعد**. هل جاءت أسئلة أخرى
   تقيس نفس المهارات؟ كل ما سبق تحقّقٌ في القاعدة؛ وهذه التجربة الحقيقية.
2. 🔴 **املأ `objectives` لدرسٍ واحد** — الحلقة مبنيّة كاملةً وتنتظر صفوفاً.
   سؤالٌ يحمل `objective_id` ⇒ الطالب المخطئ يرى «راجع قبل الإعادة» ورابطَ
   المصدر. **أعلى عائدٍ تعليميّ في المشروع، وكلفتُه صفوفٌ لا شيفرة.**
3. 🟡 **طبّق `sql/58`** — الجداول الباقية على صلاحيات Supabase الافتراضية.
4. ❓ **افحص أثر المؤقّت على التشخيص** (استعلام ⑨) — قبل التوسّع.
5. 🔴 **كتابة المحتوى** — الدروس الباقية في مقرَّر الصف الأول.
   والبدائل تُكتب بزرّ ⇄، والفاحص يحرس تكافؤها.
6. شاشة «مراجعة اليوم» — البيانات تتراكم منذ الآن، فالشاشة تقرأ تاريخاً لا تبدؤه.
7. صقل المحرّر بما تكشفه الكتابة.
8. استقطاب معلمين ← فتح `can_author` ← شاشة «مصادر إضافية» للطالب.
9. الفصل البصري: دروس صفّي / مراجعة صفوف سابقة (`own_level` جاهز).
10. الصفحة التعريفية — **بعد** توفّر محتوى.

---

## ⑤ المنفَّذ — بالتفصيل

### ٥-١ المنهج والهوية ✅

- ٦ سلالم: أكاديمية (بكالوريا مصرية · ثانوية سعودية · جامعي · أخرى) وإتقانية
  (CEFR · IELTS). و`levels` تخصّ سلّماً واحداً ⇒ `sec1` المصري و`sa1` السعودي
  صفّان مختلفان وإن تشابه الاسم.
- التسجيل يحمل `scale/level` في **بيانات auth الوصفية** لا في `profiles`:
  بين التسجيل وتأكيد البريد لا جلسة، والبيانات الوصفية تعبر الفجوة.
- `set_my_grade` دالّة لا `UPDATE` — تتحقّق أن الصفّ ينتمي للسلّم.
- ⚠️ `level_from_score` (`sql/57`) **لا تُخمّن الصفّ من درجة** — قرارٌ صريح:
  الدرجة تقيس مهارةً، والصفُّ حقيقةٌ إدارية. خلطُهما يُنزل طالباً صفّاً.

### ٥-٢ التأليف — المحرّر ✅

- بوّابتان: `can_curate` (هيكل المنهج · الرسميّ) · `can_author` (مصدرٌ باسمي).
- عقد المحرّر كاملاً: `save_lesson/unit/quiz/item/question/passage` ·
  `quiz_readiness` · `publish_quiz` + حارس مؤجَّل · `publish_lesson` ·
  `author_tree` · `author_lessons` · `lesson_items` · `quiz_for_edit` ·
  `duplicate_question` · `delete_*` · `attach_passage` · `set_section` ·
  `reorder_items`.
- النصّ المشترك والأقسام **حاويات تعلو مدى أسئلة** لا خصائص لكل سؤال.
- استيراد JSON (لصق · رفع · سحب وإفلات) + تصدير + معاينة تجربة الطالب.
- اتجاه النصّ وحرف الخيار يتبعان لغة المحتوى (`A B C` / `أ ب ج`).
- المحرّر مصمَّم **للحاسوب أولاً** بخلاف بقية المنصة — التأليف عمل مكتب.
  استثناءٌ واعٍ لا سهو.
- 🔑 المشتّت وتشخيصه **صفٌّ واحد** — لا درج ولا نافذة. تكتب الخيار وأنت
  تفكّر لماذا سيقع فيه الطالب. ولو أُخفي الكود لصار واجباً إدارياً يُملأ آخر الوقت.

### ٥-٣ الاختبار والتشخيص ✅

- 🔒 لا يصل المتصفح مفتاح إجابة واحد. التصحيح كلّه في `submit_attempt`.
- `answers` تسجّل لكل إجابة: `dx_code` · `seconds` · `changes` · `confidence` · `meta`.
- شاشة المراجعة تعرض اسم الخطأ وعلاجه من `dx_codes`.
- **الحلقة:** أخفق بـ`KW` ← قرأ العلاج ← أعاد بأسئلة جديدة تقيس المهارة نفسها
  ← يُقاس هل زال النمط. لا «أنهى الدرس» بل **«زال الخلل»**.
- `submit_attempt` تُرجع أيضاً `remedial` — عنوان المصدر العلاجيّ ورابطه،
  و`quiz.js` يعرضه. **مبنيّ وينتظر `objectives`.**
- وتكتب `review_queue`: ٣ أيام إن صحّت · يومٌ إن أخطأ · والفاصل يتضاعف حتى ٦٠.
- `59` can_access: النشر شرطٌ يعلو الاتّصال بدرس. قِيس ١·٤·٠ ⇐ ٠·٠·٠
- `60` item_quiz_ready: البطاقة تتبع المحتوى
- `61` ٣٣ كوداً · ترحيل ٩ · لغة تشخيص IELTS
- `62` student_note: لغتان — المعلّم يُصنّف والطالب يُصلح. و«ما تكرّر معك اليوم»
- `63` نمط gap: {{n}} في النصّ · grade_gap · case_slip · wrong_map
- `65` إعدادات الاختبار: plays · reveal · shuffle · pass_mark
- `66` أداة التسكين — البنية: placement_routes · placement_sessions · station_kind
- `67` أداة التسكين — المحرّك: placement_start · placement_submit · tool_readiness
- `68` إصدارات الأسئلة — البنية: retired_at · supersedes_id · فهرسٌ جزئيّ للأحياء
- ⛔ `69` · `70` · `71` **ملغاة — لا تُشغَّل.** بُنيت من مخطّطٍ مستخرَجٍ عند
  `50` فاستبدلت ثلاث دوالّ بنسخٍ أقدم ⇒ سقط `gap` والإعدادات. بديلها `75`.
- `72` الخانة تُحسب بالأحياء: variant_report · set_variant · clear_variant
- `73` سجلّ النصّ السابق: passage_history · save_passage
- `74` القلب: retire_question · delete_question تصير تراجعاً عن الإصدار
- `75` الحرّاس فوق النسخ الصحيحة: quiz_readiness ١٩ · get_quiz ٥ · quiz_for_edit ٢
- 
### ٥-٤ بنك الخانات ✅ (SQL 34 · 34b · 35 · 36 · 38)

```
34   variant_key · set_variant · clear_variant · variant_report
34b  duplicate_question تنقل section  (كانت تُسقطه — عطلٌ من هجرة سابقة)
35   get_quiz تنتقي نسخة من كل خانة بالبذرة
36   quiz_for_edit تُرجع variant_key
38   فحص التكافؤ: النصّ يجب أن يختلف بين النسخ + passage_map
```

**البذرة** = معرّف المستخدم + عدد محاولاته ⇒ الإعادة تُغيّر المسار، والمسار
نفسه ثابتٌ داخل المحاولة الواحدة. والنصّ يُنتقى أوّلاً ثم تُفضَّل أسئلته.

**الوسم حكمٌ بشريّ والفحص رقابة عليه:** بصمة الفخاخ شرطٌ لازم لا كافٍ —
تجمع ما ليس زوجاً وتفرّق ما هو زوج.

✅ التوافق الخلفي مقيس: اختبار ١ يُرجع ١٧ سؤالاً كما كان بالضبط.

**حدود `set_variant` — مقروءة من نصّها لا مستنتَجة:**
- اختبارٌ واحد · نمطٌ واحد · سؤالان فأكثر · معرّفات غير مكرَّرة
- مفتاحان مختلفان بين المحدَّدين ⇒ رفض «فُكّ إحداهما أولاً»
- مفتاحٌ واحد ⇒ يُتبنّى فتتّسع الخانة، ولا يُعاد توليده
- لا مفتاح ⇒ يُولَّد `'v' + أصغر معرّف`

⇒ الانضمام إلى خانة قائمة **مدعوم**. والدمج مرفوض — قيدٌ مقصود في القاعدة
لا نقصٌ في الواجهة.

🔑 المفتاح **اسمٌ لا رابط**: `v117` مشتقٌّ من أصغر معرّف، ولا رتبة بين
الأعضاء. حذفُ ١١٧ يترك `v117` مفتاحاً صالحاً لمن بقي.

🔓 القفل يخصّ المحتوى لا الخانة: `variant_key` وسمُ تجميعٍ أثرُه في السحب
القادم وحده. أما سؤالٌ أُجيب عنه فلا يُعدَّل — **بل يتقاعد ويولد وريثه**
(انظر ٥-١١). والوريث يرث المفتاح، والمتقاعد يحتفظ به في سجلّه ولا يُسحب.

**زرّان في شريط الخانة، لفعلين مختلفين لا صورتين لفعل واحد:**
```
⇄ اصنع بديلاً مكافئاً  →  duplicate ثم set_variant — للتأليف من نسخة
⇄ اقرن بسؤالٍ قائم    →  set_variant وحدها — لمن يؤلّف البندين معاً
```
🔑 البصمة تُعرض **قبل** الإعلان: كل مرشّح بفخاخه وبحكم «✅ مطابق» أو
«⚠️ يختلف في: الفخاخ · الصعوبة · القسم»، والمطابق يُقدَّم في الترتيب.

⚠️ **والإنذار يقول «صحّح الخيار لا الكود»**: أسهل طريق إلى ✅ هو إعادة وسم
مشتّت كذباً، وذلك يُفسد التشخيص من داخله.

**تصميم البنك — حجم الخانة قرارٌ في القياس:**
```
عدد الخانات = عدد المهارات المقيسة   ·   حجم الخانة = مقاومة الحفظ

١٤ بنداً في ٧ خانات ⇒ ٧ أسئلة لكل اختبار
١٤ بنداً في ٢ خانة  ⇒ سؤالان   ← المعتمد
١٤ بنداً في ١ خانة  ⇒ سؤال واحد · وادّعاء تكافؤٍ كاذب
```

🔴 والتجميع تحكمه البصمة لا الترتيب. أعضاء الخانة صورٌ لسؤالٍ واحد، فإن
اختلفوا فيما يقيسون صار الاختبار يختلف من طالب إلى طالب — وتُقارَن درجتان
قِيستا بمهارتين. **ولا يظهر ذلك في أي شاشة.**

**بنك قسم F (اختبار الدرس ٢):**
```
F-TC   KW · TC    تمييزٌ داخل الحقل      (preserve/restore …)
F-VOC  KW · VOC   استبعادٌ من خارج الحقل  (harmony/silence …)
```
وكلٌّ متوازنة بين المرادفات والأضداد، فلا يميل السحب إلى اتجاه. ومكسبٌ
عرضيّ: كل اختبار يقيس المهارتين معاً لأن لكلٍّ خانتها.

✅ **الاستيراد يقرن آلياً**: البنود ذات الوسم نفسه تُقرن بعد الإدراج.
والمدقّق يفحص الخانات قبل الرفع — عضوٌ واحد أو أنماط مختلطة يُحجبان،
والفخاخ غير المتطابقة تحذيرٌ لا حجب. (مفحوص: ١٢ بنداً · خانتان قُرنتا.)

### ٥-٥ نمط الاختيار المتعدّد (msq) ✅

```
SQL 39 البنية · 40 الحفظ · 41 التصحيح · 42 البوّابة
    43 إغلاق ما يعتمد على correct_id · 44 تبسيط (إسقاط كود الإغفال)
الواجهة b20: quiz.js · editor_quiz.js · screens.css
```

**قواعد ثابتة:**
- الدرجة «كلٌّ أو لا شيء» — المجموعة الناقصة إجابة خاطئة
- المقارنة بالاحتواء المتبادل (`<@`) لا بالتساوي: الإجابة مجموعة عديمة
  الترتيب، والمصفوفة متتالية مرتَّبة. و`=` تقارن الترتيب فترسب صحيحاً.
- ٢ ≤ الصحيحة ≤ الخيارات − ١ · والحدّ الأدنى ٣ خيارات
- الكود للمشتّت وحده — كما في mcq
- **لا يُعلَن العدد للطالب**: إعلانه يعيد الحلّ الترجيحي الذي وُجد النمط لإلغائه
- تشخيص الإغفال **بالنمط لا بالوسم** — يُحسب في `quiz.js` من `judgments`:
```
wrong=[] و missed ناقصة  →  «توقّفتَ قبل أن تُكمل»
missed = الصحيحة كلها    →  «الحدّ لم يتّضح بعد»
كلاهما غير فارغ          →  «الحدّ مضطرب من طرفيه»
wrong≠[] و missed=[]     →  «توسيعٌ للمفهوم»
```

**متى يُستعمل:** حين يكون **حدّ المفهوم** هو المُشكِل (أيّها ينطبق؟). أما
«أيّها الأصحّ؟» فالاختيار الواحد أدقّ وأرخص. وسؤال msq واحد في القسم كافٍ —
الزمن الذي يستهلكه أضعاف.

✅ فُحص ولا يحتاج تعديلاً: `variant_report` تقرأ `dx_map` مباشرة ·
`import_bank` تقرأ `mcq` و`essay` فلا تُنشئ msq أصلاً.

### ٥-٦ الإرشاد والرسائل ✅

- `choose_mentor` **بالمادة**: لكل مادةٍ معلمٌ واحد، و`p_teacher = null` تعني
  المتابعة الذاتية — الطالب لا يُجبَر على معلم.
- `max_mentor_switches` يحدّ التنقّل · `max_teacher_families` و
  `set_subject_capacity` يحدّان حِمل المعلم.
- `was_my_mentor` تُبقي حقّ الاطلاع على تاريخ من درّسته.
- الرسائل جدولٌ واحد بمحورٍ واحد: `student_id` هو الخيط، و`sender_role`
  يميّز الطرف. ثلاث سياسات: قراءة · إرسال · تعليم بالمقروء.
- ⚠️ `messages` و`attempts` لكلٍّ **مفتاحان إلى `profiles`** ⇒ كل تضمين يحتاج
  تلميحاً صريحاً (`messages_sender_id_fkey`…). جمعُها في `api.js` أهمّ ما
  تشتريه تلك الطبقة: تغييرُ اسم مفتاحٍ في SQL يكسر الرسائل **بلا خطأ ظاهر**.

### ٥-٧ الوسائط والتخزين ✅

- جرّة `audio` · ١٠ م.ب · `audio/*` · أربع سياسات تنادي `can_upload_media()`.
- 📐 **القاعدة تخزّن مفتاحاً لا عنواناً**: `audio/l12-x.mp3` ·
  `fb:audio/…` لمستودعٍ موسوم · `https://…` يُترك كما هو. فنقل المستودع
  لا يمسّ صفاً واحداً، والعنوان يُشتقّ في `media.js` ولا يُخزَّن.
- زرّ الرفع **يلتصق بحقلٍ قائم ولا يستبدله** — فاللصق اليدوي يبقى يعمل.
- ⚠️ الفحص في المتصفح **راحةٌ لا حراسة**؛ الحراسة في سياسة `storage`.

### ٥-٨ التتبّع والتقدّم ✅ / ⚫

- `track_item` تسجّل `opened` و`completed` في `item_progress` مع الثواني.
- `lesson_done` · `has_opened` · `best_pct` جاهزة في القاعدة.
- `lessons.requires_id` يفتح درساً بدرس — والسلسلة تُبنى وتُعرض.
- ⚫ `review_queue` تمتلئ ولا تُقرأ · `question_reports` بلا شاشة ·
  `classes/enrollments` بلا شاشة (و`teaches()` تقرؤهما).

### ٥-٩ الإدارة والصلاحيات ✅

- `request_teacher_access` ← `admin_requests` ← `admin_decide`.
- **المبدأ:** الصلاحية بالعمود · والسياسة بالصفّ · والمعنى بالدالّة.
  الكتابة كلُّها عبر `security definer`، فلا تتأثّر بما يُسحب من `authenticated`.
- المواضع **الخمسة** الوحيدة التي تكتب فيها الواجهة مباشرة: إدراج رسالة ·
  تعليم رسالة بالمقروء (عمودان) · تحديث أعمدة التصحيح على `attempts`.
- `rls_auto_enable` يقفل RLS على كل جدولٍ جديد في `public` تلقائياً.
- `question_keys` محجوبة عن الطالب · العروض `security_invoker`.

- 🆕 **`59` · الوصول إلى اختبار: النشر شرطٌ يعلو الاتّصال بدرس.**
  كان `can_access` لا تسأل عن `quizzes.published` إطلاقاً ⇒ متّصلٌ بدرسٍ منشور
  تُقرأ مسوّدتُه وتُسلَّم (٥ · ٧) · ومنفصلٌ عن الدروس `true` بلا شرط.
  صار الفحص **قبل التفرّع** ⇒ يُغلق البابين، والسياسات الثلاث تتبع
  (`p_questions_read` · `p_options_read` · `p_passages_read`).
  و`p_quizzes_read` كانت تسأل عن النشر سلفاً ⇒ **إعادةُ اتّساقٍ لا تشديد**.

③ — 64: أداةُ قياسٍ بلا مقرَّر · delete_quiz

### ٥-١١ إصدارات الأسئلة ✅ (SQL 68 · 72 · 73 · 74 · 75)

**المسألة:** «السؤال الذي أُجيب عنه لا يُعدَّل ولا يُحذف» قاعدةٌ تحمي
السجلّات وتمنع المعايرة معاً — وأداةُ القياس تُعايَر بالبيانات التي
تُنتجها. مشتّتٌ لا يختاره أحد وزنٌ ميّت، ومشتّتٌ يجذب المتفوّقين بندٌ
معطوب — ولا يُعرف ذلك إلا **بعد** التطبيق.

🔴 **والأخطر أن القاعدة كانت تحمي الكذب كما تحمي الصدق:** سؤالٌ مفتاحه
خاطئ، أو مشتّتٌ وُسم `KW` وهو `VOC`، كان يُحرَس بالقوّة نفسها — فيبقى
الكود الكاذب في `answers.dx_code` ويُرسَل الطالب إلى علاجٍ لا يخصّه.

**المبدأ البديل:**

> **ما رآه الطالب يُحفظ. أما السؤال فيتطوّر.**

الثبات ينتقل من *السؤال* إلى *سجلّ اللقاء*: القديم يتقاعد ولا يُمحى،
فتبقى إجاباته وخياراته وشرحه وخريطة تشخيصه كما رآها الطالب.

```
68  retired_at · supersedes_id · questions_live_position (فهرسٌ فريد على الأحياء)
74  retire_question: التقاعد ثم ميلاد الوريث في موضعه وخانته ونصّه
72·75  ٢٦ حارس `and retired_at is null` في ٦ دوالّ
73  passage_history: النصّ يُعدَّل في مكانه، ويُحفظ ما كان
```

**الترتيب حاسم في `retire_question`:** التقاعد **قبل** ميلاد الوريث —
الفهرس الجزئيّ فريدٌ على الأحياء، فلو وُلد الوريث أولاً لتزاحما.

**وتنتقل مع الوريث:** الخيارات بمعرّفاتٍ جديدة والمفتاح مُعاد البناء ·
`review_queue` (وإلا بقي الطالب مجدوَلاً لسؤالٍ لن يُعرض) · والبلاغات
تُوسَم `reviewed` لا `fixed` — الوريث لم يُحرَّر بعد.

**والحذف صار تراجعاً:** حذفُ وريثٍ قبل تحريره يُعيد سلفه حيّاً، وإلا
خسر الاختبار سؤالاً وبقي المتقاعد بلا خلف.

**موضعان بلا حارس عمداً:**
- `submit_attempt` — مراجعتها يجب أن ترى المتقاعد، وإلا كذبت
- `quiz_for_edit · used` — `delete_passage` تحرس بالعدّ نفسه غير مفلتر،
  فلو فُلتر لأعطى المعلّم رقماً يناقض ما يمنعه به النظام

🔑 **والنصّ المشترك عومل بغير ما عومل به السؤال — بقرار:** أكثرُ تعديلاته
تصحيحٌ يسير لا يمسّ سؤالاً، والإصدارُ عليها يُنشئ نسخةً لكلّ فاصلة ويشقّ
إحصاء الفخّ. ⇒ **يُعدَّل في مكانه بلا عائق، ويُحفَظ ما كان** في
`passage_history` — ثقةٌ موثَّقة، لا صرامةٌ تُعيق ولا ثقةٌ تُخفي.

⚠️ **وثغرةٌ كانت قائمةً وسُدَّت:** `save_passage` كانت بلا حارسٍ إطلاقاً —
فقاعدةُ «المُجاب عنه لا يُعدَّل» كانت مخترقةً من باب النصّ. القفل يحرس
نصّ السؤال («ما دلالة الكلمة في السطر الثالث؟») ولا يحرس السطر الثالث.

---

### ٥-١٠ نظام التصميم (b20 → b41)

**المسألة الحاكمة:** الشاشة جزءٌ من أداة القياس لا زينةٌ فوقها. نصٌّ يُقرأ
بصعوبة يُسقط كلمةً، وإسقاط الكلمة يُنتج كود `KW` كاذباً — فيُرسَل الطالب
إلى علاجٍ لا يخصّه. **العرضُ يُفسد التشخيص كما يُفسده المؤقّت.**

- ✅ **السلّم الطباعيّ** — ست درجات بأسماء وظيفية (`--fs-tag/meta/ui/read/q/h`)
  بدل أربعين مقاساً متناثراً، وأرضية **١٢px** لا شيء تحتها. ووزن الجسد ٣٠٠ ← ٤٠٠.
  🔴 وصُحّح **انقلابٌ هرميّ**: كان `.trap` — شرحُ الفخّ، وهو ميزة المنصة —
  ١٣٫١px والسؤالُ ١٦٫٢. صار الشرح ١٦٫٨ و`.rev-q` ١٥٫٢، أي **الشرح أكبر مما
  يشرحه**: في شاشة المراجعة السؤالُ سياقٌ والتشخيصُ متن.
  و`.q-hint` («اختر كل ما ينطبق») قفز إلى ١٦٫٨ — من يفوته يُخفق في msq
  لسببٍ لا يقيسه أي كود.
- ✅ **طبقة الأدوار** — ١٧٧ موضع لون صارت ٤١ رمزاً، وستّون شفافيةً اختُزلت
  إلى أربع رتب لكل عائلة (`soft · fill · line · edge`). والرمز يُسمّى بوظيفته:
  `--accent` لا `--cyan`. 🔑 **الثابت المحروس: صفر لونٍ حرفيّ خارج كتلتَي السِمة.**
- ✅ **سِمتان** — فاتحة افتراضية (ورقٌ دافئ `#EDE9DF`) وداكنة، بمبدّل `◐`
  يُحفظ في `localStorage`، **والقراءة في `<head>` قبل أول رسم** وإلا ومض
  البياضُ في وجه من اختار الداكنة هرباً منه.
  ⚠️ ولوح الصفحة **مُعمَّق عمداً**: صفحةٌ بيضاء وبطاقةٌ بيضاء = لا ارتفاع
  (كان ١٫٠٣:١). صار ١٫٢١:١ — يُرى ولا يُتعب.
- ✅ **الترويسة الواحدة** — الهوية في `#bar` بصفر بكسل رأسيّ، فنزل ما فوق
  المحتوى من **٣٠٪ إلى ١٩٪** من أول شاشةٍ في الجوّال.
- ✅ **فصل العرض** — قراءة ٦٨٠ · افتراضي ٧٦٠ · اختيار ١٠٦٠ · تأليف ١١٨٠،
  والرتبة تُشتقّ بـ`:has()` لا بنداءٍ يُنسى.
  🔴 **والشبكة للمواد والمعلمين وحدهما**: ترتيبهما اعتباطيّ. أما الدروس
  ومصادرها فترتيبها **محتوى** — عرضُها شبكةً يقول للطالب صامتاً إنها متكافئة
  يُبدأ بأيّها، وتلك كذبةٌ تربوية يقولها التخطيط لا النصّ.
- ✅ **البوابة** عمودان **بلا ألسنة** — اللسان يفرض اختياراً قبل الفهم ويسوّي
  بين حالتين غير متساويتين. فأخذ الأكثرُ احتمالاً الزرَّ الأول.
- ✅ **زرّ الخروج** كان ٣٫٣:١ وحدّ AA أربعةٌ ونصف. صار ٦٫٦:١.
- ✅ **الشعار** — كان `logo.svg` ٦٤٩KB و**٩٦٪ منه PNG مضمَّن** (تصدير Canva)،
  ووردمارُكه أبيضُ ١٠٠٪. أُعيد تركيبه في `logo-light/dark.png` — ١٤٫٨KB لكلٍّ.
  وفي الشريط يظهر الوردمارك النصّيّ: ٢٥px لا تكفي لشعارٍ مركَّب.
- ✅ `quiz.js` نجا بلا تعديل — يشتقّ العدّاد من `qs.length` لا من رقم مخزَّن،
  والمؤقّت من المعروض لا من عمود.
- 🔓 **b22** — الاختيار تبديلُ فئة لا إعادةَ رسم. كانت `renderQ()` تُستدعى
  عند كل اختيار فتهدم `<audio>` وتقفز بالطالب أعلى الصفحة. وأثرُه أبعد من
  الراحة: **وقتُ التمرير كان يُحسب في `a.sec`، فيُقاس التردّدُ حركةً لا فهماً.**

---

## ⑥ الثوابت — ما لا يُكسر

1. **طبقةٌ واحدة تلمس Supabase.** `grep -rn "\bdb\." js/ --exclude=api.js` ⇒ لا مخرجات.
2. **موضعٌ واحد يبني روابط الوسائط.** الفحص في رأس `media.js`.
3. **نقطة اختناقٍ واحدة للرفع.** `grep -rn "\.upload(" js/ --exclude=api.js`.
4. **صفر لونٍ حرفيّ خارج كتلتَي السِمة.**
5. **لا يصل المتصفح مفتاح إجابة واحد.**
6. **لا كتابة إلا بدالّة** — إلا المواضع الخمسة المعلنة في ⑤-٩.
7. **`dx_code` للمشتّت وحده** — ولا سؤال بمشتّت بلا كود (`save_question` تفرضه).
8. **ما يُشتقّ لا يسكن في `S`** — نسختان لشيءٍ واحد تختلفان يوماً.
9. **الدوالّ المصدَّرة `function` لا `const`** — الاستيراد الدائري يعتمد على الرفع.
10. **`00_schema.sql` مولَّد** — لا يُحرَّر بيد، ولا يُعاد ترتيب أقسامه.

---

## ⑦ أعطال معروفة

- 🔴 **`sql/58` لم يُطبَّق** — *مقيسٌ ٥ سبتمبر*: `has_function_privilege`
  تُرجع `true` لـ`public` على دوالّ `public` كلِّها، فكلُّ دالّة قابلةٌ
  للنداء من أيّ زائر عبر PostgREST — بما فيها `admin_decide`. والحراسة
  اليوم **داخل الدوالّ وحدها**: طبقةٌ واحدة حيث ينبغي طبقتان.
  ⚠️ خطوةٌ قائمة بذاتها تمسّ كلَّ دالّة — تُجرَّب بحساب طالبٍ ومعلّمٍ ومدير.
  📌 ولهذا يكتب كلُّ ملفٍّ جديد `grant execute` صراحةً (كما `67`): ما يحمل
  منحاً ينجو يوم يُطبَّق `58`، وما لا يحمله يسقط.
  ⇒ الجداول عدا `profiles` و`student_levels` و
  `messages` ما تزال على صلاحيات Supabase الافتراضية (`authenticated` يملك
  الجدول كلَّه، وRLS هي الحارس الوحيد). سياسةٌ متساهلة واحدة تصير حينها
  ثغرةً كاملة — كما وقع في `p_profiles_edit`.
- 🕳️ **`list_quizzes` بلا حارسَي تقاعد** — ⚫ ميتة: `0` نداءً في كلّ
  ملفّات الواجهة و`0` في دوالّ القاعدة. البنية تجاوزتها — الاختبار عنصرٌ
  في درس يعود مع `list_lessons`، ولا قائمة اختباراتٍ مسطّحة في المنصّة.
  ⇒ **لم تُرقَّع عمداً**: رقعةُ دالّةٍ ميتة تزيد التاريخ ولا تزيد الصدق.
  من يُحييها يضيف `and x.retired_at is null` في عدّادَي `mcq` و`essay`.
- 🕳️ **`question_count` يعدّ المتقاعدين** في `save_question` و
  `duplicate_question` (فُلتر في `delete_question` وحدها). لا يُقرأ في
  أيّ موضع ⇒ لا ضرر اليوم. يُصلَح حين تُمسّ الدالّتان لسببٍ آخر.
- 🔴 **شارة «ملاحظة جديدة» لا تُمسح** — *مؤكَّد من المخطط لا مرجَّح*:
  سياسة `UPDATE` الوحيدة على `attempts` هي `p_attempts_grade using teaches(user_id)`،
  والطالب ليس معلّم نفسه ⇒ `markFeedbackRead` تُرجع صفر صفوف **بلا خطأ**.
  العلاج: سياسة `p_attempts_mark` بـ`user_id = auth.uid()` — أو الأنظف،
  دالّة `mark_feedback_read` ثم سحب `UPDATE` كلّياً.
- 🟡 **`teaches()` غير مقيّدة بالمادة** — تقرأ الإرشاد والفصول بلا شرط مادة،
  فمعلّم العربية يرى محاولات الطالب في الإنجليزية. يظهر أثره عند المعلم الثاني.
- 🟢 `favicon.svg` مفقود (مشار إليه في `index.html`).
- 🟢 خمسة أنماط أسئلة يسمح بها المخطط ولا تعرضها الواجهة — يكشفها
  `quiz_readiness` وتمنع النشر.
- ✅ ~~`quizzes.question_count` فارغ و`get_quiz` تقرؤه~~ **زال بـ`35`**.
- publish_lesson تحرس عند العتبة ولا تبقى (١١ · ١٣ عولجا يدوياً)
- has_quiz و lesson_done لا تفحصان نشر الاختبار
- درسٌ منشورٌ بلا محتوًى عامل يُبلَّغ لمؤلّفه — مؤجَّل

**أُصلحت ✅**
- `quiz_for_edit` كانت تحسب `correct` بـ`o.id = k.correct_id` وهو فارغ في msq
  ⇒ `null` لا `false` ⇒ سؤالٌ محفوظ يعود بمربّعات فارغة، وأي حفظٍ بعده يُرفض. (`43`)
- `duplicate_question` كانت تنسخ مفتاحاً واحداً وتُسقط أكواد الصحيح بـ`elsif`. (`43`)
- قالب المثال كان يستعمل `SEM` وهو غير مسجَّل ⇒ «قالب مثال» يُنتج ملفاً
  يرفضه المدقّق منذ بُني. بُدّل بـ`VOC`.
- صندوق الاستيراد لم يعرض الأكواد المتاحة، فيخترع كاتبُ JSON كوداً ثم يُفاجأ.
- شريط الخانة كان يُقفَل مع قفل المحتوى. فُصل القفلان.

---

## ⑧ دَينٌ موثَّق — يُعرَف ولا يُصلَح الآن

- ⚠️ **`is_teacher()` داخل `can_access` تسأل `auth.uid()` لا `p_user`.**
  نداءٌ بمعاملين يسأل المنادي لا المسؤول عنه. لا أثر اليوم (كل المستدعيات
  بمعاملٍ واحد) ⇒ يُصلَح حين يُستدعى بمعاملين.
  
> الفرق بين «تُرك عمداً» و«سُهي عنه» لا يُقرأ من الشيفرة، بل من سطرٍ يقوله.

- ⚠️ **`answers.dx_code` عمودٌ واحد لسؤالٍ قد يحمل فخّين** (msq) — المسجَّل فيه
  أولُ مشتّت بترتيب الموضع، والمتّجه كاملاً في `meta->'dx'`. ⇒ `v_trap_analysis`
  تعدّ كوداً واحداً لكل msq. يُصلَح حين تُبنى لوحة التحليلات: تُقرأ `meta`.
- ⚠️ `list_quizzes.mcq_count/essay_count` تعدّان المؤلَّف لا المعروض (٥١ بدل ١٧).
- - is_teacher() في can_access تسأل auth.uid() لا p_user
- شرطُ جمع الإجابة (plays · التوقيت) لا يُحفظ في answers.meta
  ⇒ يُضاف قبل أن تُقرأ الأكواد
  **غير معروضة في الواجهة** ⇒ فخٌّ نائم لا عطلٌ قائم. العدّ الصحيح حين تُحيا:
  `count(distinct coalesce(variant_key,'q'||id))`.
- ⚠️ `submit_attempt` ترتّب شاشة المراجعة بـ`q.position`، فتتأخّر أسئلة القراءة
  إن انتُقي مسارٌ متأخّر. **تجميليّ** — لا يمسّ الدرجة ولا التشخيص.
- ⚫ `attempts.served_questions` ما يزال بلا كاتب. يلزم حين تُبنى تحليلات
  «ماذا رأى هذا الطالب».
- ⚫ **`import_bank` بابٌ ميّت**: الاستيراد يجري كلّه في المتصفح (`parseImport`
  ثم `save_question`)، والدالّة لا تُنادى من أي ملف. تُحذف أو تُوثَّق كواجهة SQL.
- ⚫ **دوالّ بلا نداء من الواجهة**: `my_rank` · `best_pct` · `level_from_score` ·
  `my_students` · `my_courses` · `list_quizzes` · `variant_report` · `has_opened` ·
  `lesson_done` · `admin_pending_count`. بعضها مقصود (تُنادى داخل SQL)، وبعضها
  ينتظر شاشة. **تُراجَع مرّة واحدة ويُكتب لكلٍّ سببُ بقائه.**
- ⚠️ **الشعار في ملفّين** — يخالف «معلومةٌ في موضعين تتباعد يوماً». والسبب أن
  المصدر ليس متجهاً فـ`currentColor` غير ممكنة. يُصلَح حين يُرسَم الشعار
  متجهاً حقيقياً: عندها ملفٌّ واحد ~٨KB، وتُحذف قواعد `.lt/.dk`.
- ⚠️ **ثمانية مواضع تستعمل `opacity` على نصّ** (`.eq-k` ٠٫٦ · `.drop-s` ٠٫٦٥ …).
  الشفافية تُنقص التباين، وأثرها على الورق أوضح. كلّها بياناتٌ ثانوية —
  تُصلَح بلونٍ صريح لا بشفافية.
- ⚠️ **`.psg` محدودٌ بـ`44vh`** وقد كبر خطُّه. خُفّض `line-height` إلى ١٫٩٥
  لاسترداد سطر — والحلّ الناضج عمودان، وينتظر طالباً يقول إن التمرير أزعجه.
- ⚠️ **جرّة `audio` عامة** ⇒ `/object/public/audio/…` يُقرأ بلا مصادقة.
  الحاجز الحاليّ أنّ الرابط في `passages.media_url` وهي محميّة بـRLS.
  يُعاد النظر يوم يُنتَج أوّل درس استماع: التعرّض المسبق يقيس الحفظ لا الفهم.
- ⏸️ `serve_count` مؤجَّل — وإن أُضيف **فبالقسم لا بالاختبار**.
- ⬜ **صيانة دورية — أكواد معلَّقة في `dx_map`:** `answers.dx_code` مرتبطٌ بمفتاح
  أجنبي إلى `dx_codes`، و`dx_map` لا. فسؤالٌ بكودٍ غير مسجَّل يُحفظ ويُنشر،
  ثم يقع طالبٌ في ذلك الخيار فيرفض المفتاح الأجنبي كتابة الإجابة ⇒
  **تسقط المحاولة كلها**. الفحص:
```sql
select x.quiz_id, x.id, e.value as كود_مجهول
  from question_keys k join questions x on x.id = k.question_id,
       jsonb_each_text(k.dx_map) e
 where not exists (select 1 from dx_codes d where d.code = e.value);
```
- ⬜ لا منفذ لتعديل `difficulty` في بطاقة التحرير — `saveQ` ترسله ولا حقل له.
  وهو مدخل `equal_difficulty` في `variant_report`، فوسمٌ خاطئ يُنتج تحذيراً
  لا سبيل إلى رفعه.
- ⬜ `questions.position` صفةُ استعمالٍ تسكن في صفةِ ملكية — موضع السؤال يخصّ
  الاختبار لا السؤال. يظهر أثره عند بنك المقرَّر.
- ⬜ التحليلات تُبنى على `variant_key` الحالي، فنقل سؤالٍ بين الخانات يُعيد
  تصنيف إجاباته السابقة أثراً رجعياً. إن لزم الثبات، يُخزَّن المفتاح في
  `answers` وقت الإجابة.

---

## ⑨ قرارات مفتوحة

- ❓ **هل يفسد المؤقّت التشخيص؟** ١٨ سؤالاً في ٢٥ دقيقة، وفيها نصّ يُقرأ
  وفقرة تُكتب — يبقى للاختيار أقلّ من دقيقة. وكودٌ مثل `SU` («اعتماد نمط
  سطحي») يفترض أن الاختيار السطحي أثرُ فهمٍ سطحي؛ لكن تحت الضغط **يقرأ الجميع
  سطحياً**، فيقيس الكود السرعة لا المفهوم. والفحص متاح:

```sql
select a.dx_code, count(*) as مرات, round(avg(a.seconds)) as متوسط_الثواني
  from answers a where a.dx_code is not null
 group by 1 order by 2 desc limit 12;
```
✅ المؤقّت والتشخيص: حُسم بـ plays — خاصّةٌ للاختبار لا مبدأً للمنصّة
⏳ Form B · C: قد يُحلّ بـ variant_key القائم — لا تُبنى بنيتُه قبل أن يُكتب
⏳ المحادثة: تسجيلٌ + مقياسٌ رباعيّ + خصوصية أصوات القُصَّر
  إن وقع كودٌ في ثوانٍ قليلة دون سائره ⇒ أثر العجلة لا الفهم.
  **والمؤقّت أداة امتحان تقيس الأداء تحت ضغط؛ والتشخيص يريد العكس.**
  هذا يمسّ ميزة المنصة مباشرةً — أثره أكبر من البنك كله.

- ❓ **ما طولُ السطر الصحيح للعربية؟** قاعدة «٤٥–٧٥ حرفاً» **مُعايَرة على
  اللاتينية**. عرض ٦٨٠ يعطي ٧٧ حرفاً إنجليزياً و٨٢ عربياً. والعربية أكثف:
  كلماتها أقصر لسقوط الحركات ولنظام الجذر. **الحكم لطالبٍ يقرأ لا لجدولٍ
  منقول** — ولا يُضيَّق أكثر قبل ذلك: التضييق يزيد التمرير داخل `.psg`،
  فتكسب سطراً وتخسر سياقاً.

- ❓ **تجميع التحليلات: بالخانة أم بالسؤال؟** «٧ طلاب أخفقوا في `KW`» لا يصحّ
  إن توزّعوا على ثلاث نسخ.

- **السؤال الصوتي** — التخزين جاهز، والباقي سياسة خصوصية لأصوات قُصَّر. بعد ٢٠ درساً.
- **فتح `can_author` للمعلمين** — سطر واحد في الدالة، ينتظر معلمين.
- **الشعب داخل المسار** — البنية تحتملها بـ`courses.path_id`، ولم تُطلب بعد.
- **دمج خانتين** — ترفضه `set_variant` صراحةً. فتحه يقتضي قراراً: أيّ
  المفتاحين يبقى، وهل يُعلَن للمؤلّف.

### قرارٌ مؤجَّل — الدرجة الجزئية في msq

مرفوضة الآن، ويُكتب السبب لئلا يُعاد فتحها بلا حجّة:

① «كلٌّ أو لا شيء» تُبقي عتبة ٦٥٪ ذات معنى ولا تُنتج كسوراً لا تُقسم.
② والقاعدة الشائعة «نصف درجة لكل صحيح بلا عقوبة على الخاطئ» معطوبة في ذاتها:
   من يختار الخيارات كلها يأخذ الدرجة كاملة. ولا تصمد إلا بإجبار الطالب على
   عددٍ بعينه — وذلك يقتضي إعلان العدد فينهدم النمط.
③ وأثرها التعليمي: حين لا يربح الطالب بالاحتياط ولا بالتوسّع، يصير أفضل ما
   يفعله أن يُعلن اعتقاده كما هو. **فالإجابة تقريرٌ صادق عنه.**

**إعادة الفتح تقتضي حلّ ② أولاً.**

### لم يُفحص بعد 🔍

- 🔍 ضمّ ١١٧ و١١٨ إلى `F-TC`. الطريق: ✕ فكّ `v117` ← ⇄ اقرن بسؤالٍ قائم ←
  عضوٌ من `F-TC` ← تصير ٦. ⚠️ وبعد الفكّ يظهر ١١٨ حرّاً في قائمة مرشّحي ١١٧ —
  اختياره يعيد بناء الزوج القديم بدل الانضمام إلى الستّة.
- 🔍 زرّا الاقتران في الواجهة (الاستيراد الآلي وحده هو المفحوص).
- 🔍 عين الطالب: سؤالا msq لا سبعة — والمجموع ٢٢ من ٣٤ بنداً.

---

## ⑩ اقتراحٌ بنيويّ للنقاش — بنك على مستوى المقرَّر ⬜

**الحاجة:** «اختبارٌ شاملٌ على منهج الصف» لا يُبنى اليوم إلا بنسخ البنود،
فتتباعد النسخ ويصير التشخيص كذباً — الكود يشير إلى مشتّتٍ صُحّح في نسخةٍ
ولم يُصحَّح في أخرى.

**العقبة:** عمودٌ مفرد `questions.quiz_id` يخلط مفهومين:
```
من يملك السؤال؟  ⇒  المقرَّر            (course_id)
من يستعمله؟      ⇒  جدول وصل quiz_questions (quiz_id, question_id, position)
```

🔴 **المستوى الصحيح: المقرَّر لا المادة.** لأن `difficulty` نسبيّ إلى الصفّ،
والمفردة نفسها تُسأل بمستويين، و«الشمول» لا يُقاس إلا بمنهج مقرَّر.
⇒ **لغةُ التشخيص (`dx_codes`) للمادة · وأدواتُ القياس للمقرَّر.**

⚠️ **و`quizzes` تحمل `subject_id` لا `course_id`** — مؤكَّد من المخطط:
`save_quiz` تأخذ `p_course` ثم **تطويه إلى `subject_id`** عند الإدراج.
فالبناء على `subject_id` يجمع الإعدادي بالثانوي في خانة واحدة.
**البنية الحالية تدفع إلى الخطأ.**

**يعمل بلا تعديل:** `variant_key` يصير على مستوى المقرَّر · البصمة و
`variant_report` · `dx_map` والتشخيص كله · و`objective_id` يصير محور
«اختبار شامل على أهداف الصف».

**أثمانٌ تُعرف قبل القرار:**
✅ ① **سقط** (SQL 68–75): كان أخطرها — «حارس أُجيب عنه يتجمّد عند عشرات
   المحاولات ⇒ يحتاج إصدارات». والإصدارات بُنيت، فالتعديل لم يعد عابراً.
✅ ② **نُفِّذ أكثرُه**: القائمة نفسها فُلترت للتقاعد — `get_quiz` ·
   `quiz_readiness` · `quiz_for_edit` · `variant_report` · آلة الانتقاء.
   ⚠️ ويبقى `save_question` و`duplicate_question`: `question_count`
   فيهما يعدّ المتقاعدين. **ولا ضرر — لا أحد يقرأ العمود.** فخٌّ نائم.
③ `can_edit_quiz` تسأل عن الاختبار، والأسئلة تصير ملك المقرَّر.

🔴 **التوقيت: لا الآن — لكنّ الثمن صار أخفّ.** ثمنان من ثلاثة سقطا،
والباقي `can_edit_quiz` وحده. وهذه بنيةٌ تُبنى لحلّ ازدحام، ولا ازدحام بعد.
ابنِ خمسة دروس كاملة؛ فإن وجدتَ نفسك تنسخ البند إلى اختبارٍ ثالث فقد نضج
الوقت — وستعرف حينها أيّ التفاصيل تحتاج بدل أن تخمّنها. ولا شيء مما يُبنى
اليوم يُهدَر: العمود يتغيّر والمحتوى لا يُمسّ.
⑥ نمط الفحص — درسان تكرّرا:

- فحصُ RLS يلزمه سطران: set_config يُبدّل الهويّة،
  و set local role authenticated يُخضعك للسياسات (المحرّر postgres).
- set_config في from لا في جملةٍ سابقة — المحرّر يعرض ناتج جملةٍ واحدة.
- واختر طالباً يبلغ مستواه الهدف، وإلا حجبه حارسٌ آخر فبدت الفتحة أصغر.
