# بيان — حالة المشروع

> **المصادر الصادقة — اقرأ ولا تنقل.** كل رقمٍ هنا منقولٌ، وكلُّ منقولٍ ينحرف.
>
> | المعلومة | مصدرها الوحيد |
> |---|---|
> | نسخة الواجهة | `BUILD` في `js/ui.js` سطر ١٠ — **يُفتح ولا يُنقَل** |
> | حالة القاعدة | **`select … from sql_log`** — الاستعلام في `يطبق_كل_محادثة §①` |
> | المخطّط | `sql/00_schema.sql` — **لقطةٌ مؤرَّخة (٥ سبتمبر)**، لا حقيقة |
> | أعداد المحتوى | استعلام القسم ②-٢ — لا هذا الملف |
>
> ⚠️ سبق أن قال الرأس `b19` والشيفرة عند `b30`، ثم `b30` والشيفرة عند `b38`.
> والانحراف يتكرّر لأن النقل عادة. **إن شككت، افتح المصدر.**
>
> 🔴 **وفي ٥ سبتمبر كلّف هذا الانحرافُ هدماً لا خطأً في رقم:** قُرئ
> `00_schema.sql` وهو مستخرَجٌ عند الملفّ `50`، فاستُبدلت `get_quiz` و
> `quiz_readiness` و`quiz_for_edit` بنسخٍ أقدم ⇒ **سقط نمط `gap`
> والإعدادات** (`plays` · `reveal` · `shuffle` · `tool` · `station`).
> استُرجعت بإعادة تشغيل `63` و`65`، وحلّ محلّها `75`.
>
> ⇒ **قاعدةٌ لا تُخرق: لا `create or replace` قبل `pg_get_functiondef`
> للدالّة نفسها.** المخطط لقطةٌ مؤرَّخة، والقاعدة هي الحقيقة.
>
> 🔴 **واللقطة اليوم متأخّرةٌ بواحدٍ وثلاثين ملفّاً** (`76` → `106`). > فخطرُها الآن أعلى مما كان يوم أوقعت الهدم. **تُعاد استخراجاً قبل أي
> `create or replace` على دالّةٍ قديمة.**

---

## ٠ · البدء والختام — يُقرأ أولاً ويُعمَل به

> **كلُّ محادثةٍ في هذا المشروع تبدأ من هنا وتنتهي إليه.**
> لا لتنظيم، بل لأن تخطّي هذه الخطوة كلّف هدماً في ٥ سبتمبر ٢٠٢٦.

### أ · قبل أن تكتب سطراً

🔑 **الاستعلام ليس هنا — وهذا مقصود.** موضعُه `يطبق_كل_محادثة §①`،
وهو **جملةٌ واحدة** تُلصق كما هي. ونسخُه هنا يُنتج نسختين تتباعدان —
وهو الداء الذي طاردته هذه الوثيقة كلُّها. وما يُقرأ من ناتجه:

| البند | ما يعنيه عملياً |
|---|---|
| الرقم التالي | رقم ملفّك. **لا يُؤخذ من أي وثيقة — ولا من هذه** |
| آخر ثلاثة | إن سبقت تاريخَ «حيث انتهينا» ⇒ هذه الوثيقة متأخّرة |
| 🔴 ثقوب | كلُّ رقمٍ هنا قنبلةٌ موقوتة: يُحجز مرّتين أو يُبحث عنه أبداً |
| فجوات موصوفة | ما لا ملفَّ له وسببُه — تُقرأ قبل السؤال عنه |
| الأعداد | انحرافُها عن متن `00_schema` ⇒ المخطّط يحتاج إعادة استخراج |

**وسطرٌ خارج القاعدة:** `grep -n 'BUILD' js/ui.js`

### ب · القواعد التي كلّفت ثمناً — لا تُخرق

**① لا `create or replace` قبل `pg_get_functiondef` للدالّة نفسها.**
اقرأ الحيّ، وقارنه بما عندك، ثم استبدل. **`00_schema.sql` لقطةٌ مؤرَّخة
لا حقيقة** — وقراءتُه وحده هدمت `gap` والإعدادات في ٥ سبتمبر.

**② `STATE.md` حُكمٌ لا مصدرَ أرقام.** كلُّ رقمٍ هنا منقول، وكلُّ منقولٍ
ينحرف. الأرقام في القاعدة، والاستعلام يجلبها.

**③ لا تُحِل إلى ملفٍّ قبل أن يوجد.** «طبِّق `sql/58`» بقيت شهوراً وهو
ملفٌّ لم يُكتب قطّ — نيّةٌ صيغت أمراً فصُدِّقت.

**④ الفحص يُصاغ لكلّ حالةٍ بما يخصّها.** اختبارٌ واحد على ثلاثة أشياء
يُنتج «فشلاً» لا يعني شيئاً، ويُدرَّب الناظر على تجاهل الإنذار.
✅ *وطُبِّق بنجاح في ١٤ سبتمبر*: فُصل محرّك FSRS (`99`) عن بنيته (`98`)
وذاكرته (`100`)، فصار لكلٍّ فحصُه — ١٠ و٧ و٧ بنود.

**⑤ الفجوة تُرى والصمت لا يُرى.** رقمٌ ملغًى يُسجَّل بسببه، وقرارٌ
يُشرَح في موضعه، وعطلٌ مؤجَّلٌ يُكتب — **والصامت أخطر من الصاخب دائماً.**

**⑥ اقرأ المصدر الحيّ قبل أن تُصمّم عليه.** في ١٤ سبتمبر صُمّم جدولٌ
على `course_id` ثم فُتحت `lessons` فإذا هي **لا تحمله إطلاقاً**. تصميمٌ
كاملٌ كان سيُبنى على عمودٍ غير موجود. ⇒ `grep` قبل `create table`.

**⑦ غيابٌ في وثيقةٍ مؤرَّخة ليس دليل عدم.** اللقطة تشهد بما كان، ولا
تشهد بما ليس.

### ج · عند الختام — خمسة

```
① كلُّ ملفّ SQL يُختَم بسطره:
   insert into public.sql_log (n, title, applied_at)
   values ('NN', 'العنوان', now())
   on conflict (n) do update set applied_at = now();
   ⚠️ والعنوان عنوانُ الرأس بحرفه — لا يُستنتج من اسم الملفّ.

② كلُّ قرارٍ لا يُقرأ من الشيفرة يُكتب في موضعه —
   في تعليق الملفّ إن خصّه، وفي ⑥ هنا إن خصّ المنصّة.

③ «حيث انتهينا» أدناه يُحدَّث: ما أُنجز يُشطب، وما ظهر يُضاف.
   🔴 وهي التي تُترك، وهي التي تُوقع.
④ إن تغيّرت الواجهة: BUILD يُرفَع في js/ui.js — **ومعه MOD في كل وحدةٍ
   تحمل ختماً**. واستويا عند b64 في ١٨ سبتمبر، فيُرفعان معاً أو لا يُرفعان.

⑤ إن تغيّرت البنية: 00_schema.sql يُعاد استخراجه، ورأسه يُصحَّح من المتن.

⑥ 🔴 **وهذه الوثيقة تُرفع إلى المستودع.** فنسخةٌ حيّة تعيش خارج Git
   تُنتج قارئاً يقرأ تاريخاً كاذباً فيصدّقه — وقع فعلاً: تأخّرت تسعة
   أيام حتى ١٨ سبتمبر. **والوثيقة التي لا تُرفع لم تُحدَّث.**
```
### د · حيث انتهينا — ٢٥ سبتمبر ٢٠٢٦

**أُنجز منذ ٥ سبتمبر — واحدٌ وثلاثون ملفّاً وخمسة أنظمة:**

`````
76             سجلّ الهجرات — وصار المصدر الصادق لـ«أين نحن؟»
78             لغة تشخيص الرياضيات: ثلاثة أكواد
79 · 80 · 97   الفروع (strands): البنية · العقد · نافذة القراءة
81             تنقيح المعجم لا توسيعه
82 – 96        التحليلات: من تثبيت المحور إلى لوحة الجماعة (١٥ ملفّاً)
98 · 99 · 100  البطاقات: المخزون · محرّك FSRS-6 · الذاكرة
101 – 105      البطاقات تكتمل: الوسائط والملاحظة · الحفظ · الاشتراك ·
               اللعبة · بطاقات التدريب
106            صدقُ الجرس: mark_feedback_read · my_counts
`````

**وبعدها — هويةٌ بصرية لا مخطّط (لا رقم SQL، الشيفرةُ وحدها):** الشعار
صار متجهاً حقيقياً في كل الموقع (الشريط والبوابة)، والبوابة صارت نصفين:
لوحةٌ كحليّةٌ ممتدّة يُكتب فيها الشعارُ مشهدَ افتتاح، ونموذجٌ يقابلها.
التفصيل في ٥-١٠. `BUILD/MOD → b80`.

🔴 **باقٍ يدويّاً:** حذف `img/logo-light.png` و`img/logo-dark.png` —
بلا مرجعٍ في الشيفرة الآن، لكن هذه الجلسة لا تملك صلاحية حذفٍ ولا
طرفيّةً على جهازك، فتعذّر حذفهما من هنا.

**والبطاقات صارت شاشةً لا قاعدةً:** `flashcards.js` و`editor_cards.js` و
`card_game_match.js` حيّة، و`cards` وجهةٌ في `DEST.student`.

**١٨ سبتمبر — الجرس (`106`):**

- 🔴 **أُصلح عطلٌ صامت عاش من غير أن يُرى:** الطالب لا يستطيع تعليم ملاحظته
  مقروءةً، لأن سياسة `UPDATE` الوحيدة على `attempts` للمعلّم ⇒ صفر صفوف
  **بلا خطأ**. والعلاج دالّة `mark_feedback_read` تُرجع عددَ ما تغيّر،
  **فيُرى الفشل**. وانتقلت الواجهة من `update` مباشر إلى نداء دالّة.
- **`my_counts()` جوابٌ واحد لـ«ما الجديد عندي؟»** بحسب الدور، و`bell`
  يُقرَّر في القاعدة لا في الواجهة. **والمستحقُّ من البطاقات خارجه عمداً:**
  رقمٌ يتجدّد كلَّ يوم بتصميم FSRS يصير ضجيجاً دائماً، فمكانه شريط «اليوم».
- 🔑 **ورقمان للمعلّم لا رقم:** `grading` تعدّ المحاولة التي فيها مقاليٌّ بلا
  تصحيح — أي ما يحبس الطالب عن درجته. أمّا «بانتظار التصحيح» في شاشته فتعدّ
  كلَّ محاولةٍ بلا تعليق، ومنها اختيارٌ صحّحته الآلة ولا ينتظر أحداً.
  **سؤالان باسمين، لا نسختان لرقمٍ واحد.**
- ✅ **وفُحص الإرشاد فكان نظيفاً:** لا ازدواج في `mentorships` — معلّمٌ نشطٌ
  واحد لكلّ (طالب · مادة). *(ولو وُجد اثنان لسقطت شاشة المواد: استعلام
  `mentor_name` في `list_subjects` بلا `limit`.)*
① ✅ صدقُ الجرس (106)
② ✅ الهيكل: درج ☰ · جرسٌ بعدده · قائمة حساب — لثلاثة أدوار
③ ✅ رئيسة الطالب: «اليوم» (تابِع · استرجِع · عالِج) ثم المواد   b66
④ ✅ البحث العامّ: تطبيعٌ عربيّ + search_all بنطاق الدور        107 · 108
⑤ ✅ واجهة البحث — لوحةٌ وسط الشاشة، والمفتاح `/`               b67
⑥ 🟡 الرسوم والحركة — **الجزء الأول منفَّذ: الهياكل الشبحية**    b68
⑦ ✅ محاكي الرقم الهيدروجيني ثلاثيّ الأبعاد `sims/ph-water-3d.html`

📐 **وفي المحاكي قراران يلزمان كلَّ محاكٍ ثلاثيّ الأبعاد بعده:**
① المحرّك محلّيٌّ كلاسيكيّ (`vendor/three/three.min.js` يُسنِد `window.THREE`)
  لا وحدة ESM من CDN — لأنّ إطار المحاكاة `allow-scripts` بلا
  `allow-same-origin` ⇒ أصلٌ معتم، وجلب الوحدات منه **يسقط في فحص CORS**.
② الماء جسمٌ مُعتِم والزجاج شفّافٌ بانكسار (`transmission`) — لأنّ three.js
  لا يُدخل الأجسام الشفّافة في مخزن الانكسار، فلو انعكس الاختيار **لاختفى
  الماء خلف زجاج الكأس**. ولهذا يولد الرذاذ والزبد فوق السطح لا تحته.


**٢٤ سبتمبر — محرّر الأسئلة: شريطُ أوامرَ وعمودان (`b72`) 🎨**

شكوى المؤلّف كانت أربعاً، وأصلُها واحد: **الشاشة تُنفق عرضَها على ما
لا يُكتب.** والعلاج أن يُسأل عن كلّ قطعةٍ: أين تعمل؟ فتُوضع هناك.

- 🔴 **العمود الثالث (الجاهزية) أُلغي** — كان ٢٦٠px ملازماً للشاشة يعرض
  **حكماً لا يتغيّر إلا عند الحفظ**، فيدفع الكاتب عرضَه في كلّ حرفٍ
  يكتبه. ولم يُحذف ما فيه بل فُرّق بحسب ما يفعله:
  · الأفعال (إعدادات · معاينة · نشر) ⇐ **شريط أوامرٍ** بعرض الصفحة،
    فالفعل الواقع على الاختبار كلِّه لا يسكن عموداً بجانب سؤالٍ واحد.
  · العدُّ بحسب النمط ⇐ **رقائق** في الشريط نفسه، وهي في حقيقتها
    فلاترُ `setFilter` — فبقيت زنادَها كما كانت.
  · العوائق ⇐ **لافتةٌ بعرض الصفحة**: ما يمنع النشر أحقُّ بالعرض من
    عمودٍ يقطّع سطرَه أربع كلمات.
  ⇒ ٥٦٠px صارت ~١٠٤٠: **الخيار وكودُ تشخيصه في سطرٍ واحد** — والمبدأ
  الحاكم أنهما فكرةٌ واحدة، فإن انكسر الصفّ انكسرت.
- **رتبةُ عرضٍ رابعة** `‎.wrap.wide:has(.eq){1400px}‎` — تُشتقّ من
  المحتوى بـ`:has` كأخواتها في `base.css`، فلا تُنسى في شاشةٍ جديدة.
  ⚠️ والنثرُ لا يتّسع باتّساعها: `‎.eq-main textarea{78ch}‎` — إلا
  **نصَّ السؤال**، فالوسط معاينةٌ بفئات شاشة الطالب، ولو ضاق عمّا
  تحته لرأى المؤلّف تخطيطاً لا يراه الطالب.
- **«＋» صار «＋ سؤال جديد»** زرّاً رئيساً يتصدّر الشريط. كان رمزاً في
  صندوقٍ يقاسمه «⇪» نصفين — فبدا أكثرُ أفعال المحرّر تكراراً زرَّ أداةٍ
  ثانوية، وبدا الاستيرادُ (فعلُ مرّةٍ في عمر الاختبار) نظيرَه.
  والاستيراد اليوم أيقونةٌ مربّعة بجانبه.
- **«⚙» صار «⚙ الإعدادات»** — كان رمزاً أعمى فوق الفتات.
- 🔴 **وعطلٌ صامت أُصلح في الطريق:** مستمعات `scroll`/`resize`/`click`
  كانت تُركَّب داخل `wireList`، و`wireList` تُنادى مع **كلّ حرفٍ** يُكتب
  في صندوق البحث ⇒ مئاتُ المستمعات على النافذة في جلسةٍ واحدة، ولا شيء
  يظهر إلا بطءٌ يُنسب إلى المتصفّح. ⇒ `shutMenuOnce()` تُركِّب مرّة.
- **وقائمة الأنماط كانت تُحاذى باليسار ثابتاً** فتخرج من تحت زرّها في
  صفحةٍ من اليمين — صارت تتبع اتّجاه الصفحة، وتُقلب فوق الزرّ إذا ضاق
  ما تحته بدل أن تُقصَّ فتجلس **على الزرّ نفسه** فتحجب ما نُقر عليه.
  ونقرةٌ ثانية على الزناد تطويها — وكانت لا تُغلق أبداً، لأنّ مستمع
  `capture` على المستند يسبق الزرَّ فيطويها ثمّ يفتحها الزرُّ.
- **و`F_LABEL` كانت بلا `gap`** — فشارةُ الفلتر تكتب مفتاحه بحروفٍ
  لاتينية في شاشةٍ عربية.

**٢٤ سبتمبر — والمستوى الرأسيّ بعده (`b73`) 📐**

صدرُ الصفحة كان يُنفق ٨٦٤px قبل أن تظهر بطاقة السؤال. صار ٧٣٩ — ومئةٌ
وخمسةٌ وعشرون منها كانت تقول ما هو مكتوبٌ تحته أو ما يُعرَف بعد مرّة.

- **لافتةُ «مقفل» صارت سطراً و«i»:** كانت خمسة أسطر تتصدّر العمود
  (١٥٠px ⇐ ٥٣). والبيانُ فيها سطرٌ واحد — **أنّه مقفل وكم مرّةً أُجيب
  عنه**؛ أمّا «لماذا قُفل» و«ماذا يفعل الإصدار الجديد» فشرحٌ يُطلب
  مرّةً ثمّ يُعرف، لا إنذارٌ يُقرأ في كلّ سؤالٍ مقفل.
  ⚠️ **ولم يُنقل الشرح بل أُخّر نقرةً:** يفتح في موضعه تحت السطر نفسه،
  والحالة في `aria-expanded` لا في صنفٍ موازٍ. **الحجّة تبقى حيث يقع
  السؤال** (⑥ الثوابت) — وإنّما صارت تُطلب.
- **واسمُ الدرس مرّةً واحدة:** كان في وصف الترويسة وفي شريط الأوامر
  معاً. حُذف الوصف، واتّسع حقلُ الشريط من ٢٦ حرفاً إلى ٤٦ فصار يسع
  الاسم كاملاً. والترويسة ٩٧px ⇐ ٦٣.
  ⚠️ **والمحطّة لا درسَ لها**، فاسمُها لا يظهر في زرّ الرجوع (وجهتُه
  فهرسُ الأدوات) — فبقي في الترويسة: `محطّة قياس — ‹الاسم›`.
  🔓 و`head()` صارت **تُخفي** الوصفَ الفارغ ولا تُفرّغه: `‎.sub‎` فيها
  `margin-top`، فالفارغُ يحجز حشوَه في كلّ شاشةٍ بلا وصف.
- 🔴 **وتبديلُ السؤال لا يُحرّك الصفحة (`render('keep')`):** كانت كلُّ
  نقرةٍ في الشريط تقذف المؤلّفَ إلى أعلى الصفحة فيعود بالتمرير إلى
  حيث كان. **والشريط ملازمٌ للشاشة** — فالمؤلّف يبدّل السؤال وهو ينظر
  إليه، ولا ينتقل بين شاشتين. ⇒ ثلاثة أحوالٍ للرسم لا حالان:
  `top` للدخول · `focus` للعودة من شاشةٍ أخرى أو تغيّرٍ في البنية ·
  `keep` للتبديل. **ونقرةٌ على السؤال الحاضر تبقى تقريباً** — وهي
  المنفذ الوحيد إلى البطاقة بعد أن سكنت الصفحة.

**٢٤ سبتمبر — والصفوفُ الثلاثة صارت شريطاً واحداً (`b74`) 🎛️**

لافتةُ القفل وصفّا «اصنع بديلاً مكافئاً» و«اقرن بسؤالٍ قائم» — ثلاثةُ
صفوفٍ بعرض العمود، كلٌّ يشرح نفسه بجملةٍ كاملة — صارت **`strip()`:
سطرٌ واحد** (١٦٣px ⇐ ٤٥). وصدرُ الصفحة ٧٣٩px ⇐ ٦١٦.

🔑 **والقاعدة التي فُرز بها: الحالُ يُعرض · والفعلُ يُسمّى · والشرحُ
يُطلب.** فالحالُ رقيقة (`🔒 مقفل · ٢ إجابة`)، والفعلُ كلمتان
(`⇄ بديلٌ مكافئ` · `⇄ اقرِن`)، والجملةُ الكاملة في `title` عند الوقوف.

⚠️ **و«i» تبقى مع `title` ولا تُغني عنها:** اللمسُ لا وقوفَ فيه،
وقارئُ الشاشة لا يقف. فالشرحُ كلُّه يُفتح في موضعه لمن لا مؤشّر له —
والحالة في `aria-expanded`.
⚠️ **والشريط محايدُ اللون والرقيقةُ وحدها كهرمانية:** صفٌّ كاملٌ
كهرمانيّ يصيح في كلّ سؤالٍ مقفل، **والصياحُ الدائم لا يُسمع** (④).

📌 **ومُقاسٌ لا يُنسب إلى الشيفرة:** تبديلُ السؤال قد يُنقص `scrollY`
حين يكون السؤالُ التالي أقصر — وهو **قصُّ المتصفّح** عند حدّ ارتفاع
الوثيقة (٦٥١ ⇐ ٥٧٠)، لا قفزةً تُستدعى. وقيس عكسُه: إلى سؤالٍ أطول
يبقى الموضع كما هو بالضبط.

🟡 **وما بقي في الصدر — يُعرَض ولا يُنفَّذ بلا قرار:** ٦١٦px أكبرُ ما
فيها ~٢٠٠ لمعاينة النصّ المشترك. **وهي تُطوى بارتفاعٍ أقلّ ولا تُخفى**
— مادّةُ السؤال لا شرحٌ عنه، والمؤلّف يقرأ منها وهو يكتب.

**٢٤ سبتمبر — المزاوجة: الصفُّ هو الاقتران (`b76`) ⇄**

🔴 **وفُتح بابٌ كان مغلقاً بالكامل:** `saveQ` تُعيد بناء الخيارات
وتُسقط `o.k`، و`save_question` ترفض بندَ مزاوجةٍ بلا مفتاح («كل بندٍ
يحتاج مفتاحاً ثابتاً»). ⇒ **لم تكن تُحفَظ مزاوجةٌ من المحرّر منذ 112
قطّ** — والرفضُ يظهر إشعاراً عابراً عند الحفظ، فيُنسب إلى القاعدة لا
إلى سطرٍ في الواجهة. والمسار الثاني (`runImport`) يسقط بالعلّة نفسها،
و`parseImport` تشترط المفتاح في الملفّ ثمّ يُسقطه السطرُ بعدها.
⇒ `k: o.k` في الموضعين. **وهذا صنفُ العطل الذي يعيش لأنّ رسالتَه
تُتَّهم بها جهةٌ أخرى.**

🔴 **ودرجٌ في التنسيق سبق الشيفرة:** `screens.css` فيها منذ `112`
`‎.mt-line‎` بشبكةٍ خماسية (رقم · بند · سهم · مقابل · حذف) و`‎.mt-mb‎`
و`‎.mt-arrow‎` و`‎.mt-spare‎` — **والشيفرة ترسم `‎.mt-bank‎` و`‎.mt-chips‎`
و`‎.mt-bt‎`، وهي أصنافٌ لا تنسيقَ لها إطلاقاً.** فكان المؤلّف يرى حقولاً
عارية. ⇒ رُدَّت الشيفرة إلى عقد التنسيق لا العكس.

**والعرضُ صار كما يفكّر المعلّم:**
- عمودان من أوّل نقرة، وثلاثةُ صفوفٍ جاهزة — **البندُ ومقابلُه الصحيح
  في صفٍّ واحد**. وكان عمودَ بنودٍ وقائمةَ مقابلاتٍ منفصلة، فيُنشئ
  المقابلات أوّلاً ثمّ يعود يختار لكلّ بندٍ صوابَه من منسدلة: **خطوةُ
  ربطٍ لا وجودَ لها في ذهنه** — هو يفكّر «التشبيه ← مشابهةٌ بأداة» معاً.
- `mtEnsure` تضمن لكلّ بندٍ مقابلَه، **وهي متماثلة** (قيس: ثماني
  إعادات رسمٍ لا تُكثّر البنك). وبندٌ بلا صوابٍ يُولَّد له مقابلٌ فارغ —
  يُرى فراغاً يُملأ لا صمتاً يُحفظ.
- `accept.pairs` **خرجت من الـDOM إلى الكائن**: صارت بنيةَ الصفّ لا
  اختياراً فيه. وكانت تُبنى من منسدلاتٍ — فلو غابت من الشاشة لخرجت
  المفاتيحُ فارغة.
- **والزائد خرج إلى صندوقه** ولم يسقط: لو كان كلُّ صفٍّ بنداً ومقابلَه
  لما بقي زائدٌ أبداً، فيُحلّ البندُ الأخير بالاستبعاد ويُصيبه من يجهله
  **ولا يُسجَّل له كود**. وصمتُ التشخيص أخطر من خطئه.

✅ **وقُرئ المصدر الحيّ قبل التصميم عليه** (⑥): `get_quiz` تخلط عمود
المزاوجة بـ`order by md5(v_seed‖السؤال‖b->>'k')` — **بالمفتاح لا
بالموضع**. فإدخالُ المقابلات بترتيب البنود ليس إفشاءً، ولا يقفز مقابلٌ
بين محاولتين. وفُحصت شروط `save_question` الستّة على ما تُرسله الشاشة
فعلاً: المفاتيح · تطابقُ عددها بالبنود · وجودُ كلّ مقابلٍ في القائمة ·
لا نصَّ فارغاً · لا مفتاحَ مكرَّراً.

**٢٤ سبتمبر — إسنادُ المزاوجة عند الطالب: نقرتان وسحب (`b77`) 🖐️**
`js/match_dnd.js` — وحدةٌ جديدة لا تعرف الإجابة الصحيحة ولا تمسّ القاعدة.

🔓 **ونُقض قرارٌ مسجَّل — بعد استيفاء شرطه لا بتجاوزه.** كان في
`render_q.js`: «الإسنادُ بقائمةٍ أصيلة لا بسحبٍ وإفلات: تعمل باللمس
وبلوحة المفاتيح وقارئ الشاشة، **والسحبُ يسقط في الثلاثة**». والحجّة
صادقةٌ في **سحب HTML5 الأصليّ** وحده — ذاك لا يعرف اللمس. فاستُوفيت
الثلاثة بدل أن تُهدَر:
① **اللمس** — النواةُ نقرتان، والسحبُ ببصمة المؤشّر لا بـ`dragstart`.
② **المفاتيح** — الطرفان **زرّان أصيلان**، فـ`Enter`/`Space` تعملان بلا
   شيفرة، و`Escape` تُنزل المحمول.
③ **قارئ الشاشة** — `aria-pressed` على المقابل، ونصٌّ مخفيّ داخل الخانة
   يقول «مقابل البند ن»، و`aria-live` يُعلن الحملَ والوضع.
   ⚠️ **ولا `aria-label` على الخانة**: يمحو نصَّها فيُقرأ العنوانُ ولا
   تُقرأ الإجابة.

⏱️ **والحاكمُ الحقيقيّ `a.sec` لا الذوق** (§b22: «وقتُ الحركة يُحسب فيه
فيُقاس التردّدُ حركةً لا فهماً — وذاك أساس التشخيص كله»). ⇒ **لم تُزَد
حركة**: نقرتان أقلُّ من فتح قائمةٍ وتمريرٍ واختيار. والسحبُ مسارٌ ثانٍ
لمن أراده لا طريقٌ مفروض. و`a.pairs` و`a.chg` بعقدهما لم يتغيّرا —
تغيّرت اليدُ ولم يتغيّر ما تكتبه.

- **والاتّجاهان معاً:** تنقر المقابلَ ثمّ البند، أو البندَ ثمّ المقابل.
- **والمقابلُ يُعتم بعد وضعه ولا يُرفع** — سؤالٌ قد يصحّ فيه مقابلٌ
  لبندين، ورفعُه يمنع إجابةً سليمة.
- 🔴 **وعطلٌ ظهر في القياس لا في النظر:** الالتقاط (`setPointerCapture`)
  يُعيد توجيه أحداث التوافق إلى المقابل، **فتقع `click` عليه بعد
  `pointerup` فيُحمَل من جديد ويبقى مضيئاً بعد أن وُضع**. و
  `preventDefault` على `pointerup` لا تمنعها — النقرةُ تُولَّد من تسلسل
  الفأرة لا منها. ⇒ تُبتلَع نقرةٌ واحدة، ويُصفَّر العلمُ عند
  `pointerdown` التالية فلا يبتلع نقرةً بريئة.
- ⚠️ **وثمنُ السحب بالإصبع سطرٌ في CSS:** `‎.bank-w{touch-action:none}‎`
  — الإصبعُ الذي يبدأ على مقابلٍ يسحبه ولا يُمرّر الصفحة. والبطاقةُ
  كلُّها تبقى `auto`، فالتمرير قائمٌ من كلّ ما حوله.

✅ **وقيس كلُّه في المتصفّح لا في الظنّ:** نقرتان في الاتّجاهين · سحبٌ
بالفأرة وبمؤشّرٍ نوعُه `touch` · `Enter`/`Space`/`Escape` وحدها بلا فأرة ·
اللمس عند ٣٩٠px · والإزالة · والتكرار على بندين. و**عقدُ `onChange`
مقيسٌ على كائن الإجابة**: إسنادٌ ثمّ تبديلٌ (يرفع `chg`) ثمّ إزالةٌ
بـ`null` (تحذف المفتاح).

**٢٤ سبتمبر — والزرُّ يصف الحاضر لا ما مضى (`b81`) 🔘**

🔴 **عطلٌ صغيرُ الشيفرة ظاهرُ الكذب:** تنبيهُ الفراغ يبدّل نصَّ الزرّ إلى
«تابع دون إجابة» — **ثمّ لا يعود وإن أجاب الطالب عن كلّ ما تُرك**، ولا
تضيق قائمةُ «بلا إجابة: س١٣» بما مُلئ منها. فالطالب يقرأ في الزرّ حكماً
على حالٍ مضت. ⇒ `syncWarn()` تُعيد حساب `blanks()` بعد كلّ تبديل: يضيق
التنبيه بما بقي، ويزول هو ونصُّ الزرّ معاً إذا لم يبقَ فارغ.

- **ومَجرًى واحد لا ثلاثة:** `onAns` تغلّف المستمعات الثلاثة (النقر ·
  الكتابة · المزاوجة) بدل أن يُنادى التحديثُ في كلّ فرعٍ منها — فالفروعُ
  تنمو، والنسيان يقع في أحدثها.
- ونصّا الزرّ صارا ثابتين (`LBL_NEXT` · `LBL_BLANK`) يُشتقّان مرّة: النصُّ
  الأصل يُميّز جلسةَ التسكين (`last && !PL`)، فلو كُتب في موضعين لعاد
  «إنهاء وتسليم» بعد التنبيه «التالي».
- ✅ **وفجوةُ التسكين أُغلقت بعدها (`b84`):** كانت `LBL_BLANK` وحدها لا
  تُميّز `PL` — سلوكٌ قائمٌ قبل الإصلاح لا ناتجٌ عنه — فيصير الزرُّ في آخر
  صفحةٍ من محطّةٍ «سلّم دون إكمال» وأصلُه «التالي». صار الحارسُ واحداً
  للنصّين (`last && !PL`). 🔒 **والتسكينُ يُخفي حدودَه عمداً** — `qcount`
  بلا مقام، و«التالي» بدل «إنهاء وتسليم» — لأنّ المحطّة التالية تُركَّب
  مكانها، فزرٌّ يقول «سلّم» يُخبر الطالب أنّ ما مضى قُضي فيه ولم يُقضَ.

🔑 **و«عالِج» تجعل ميزة المنصّة أوّلَ ما يراه الطالب** لا آخرَ ما يصل إليه:
تقرأ `student_dx`، ونصُّها من `dx_notes()` فتبقى لغة المعلّم محجوبة (`62`).
**ولا يُعرض نمطٌ إلا إذا تكرّر** — والمرّةُ الواحدة صدفةٌ لا نمط.

🔒 **وللبحث خطٌّ أحمر:** الطالب لا يجد سؤالاً لم يُجب عنه. وإلا صار البحث
نافذةً على البنك، فتسقط مقاومة الحفظ التي بُنيت لها `variant_key` ويقيس
التشخيص الذاكرة لا الفهم. ⇒ نطاقُه: دروسه ومصادره وبطاقاته، وأسئلةٌ **أجاب
عنها** بتشخيصها.

🟠 **وعائقُ البحث محتوى لا تقنية:** ثلاثة دروسٍ منشورة تُنتج «لا نتائج» أكثر
ممّا تُنتج نتيجة، **فيتعلّم الطالب ألّا يبحث**. ⇒ يُبنى بعد ② و③.

**٢٤ سبتمبر — «إكمال من قائمة» (`cloze`) · الملفّ `113` · `b84`** ⇢

النمطُ الخامس في المحرّر — والزرُّ الذي كان «قريباً» صار يعمل. **التأليفُ
وحده، والنشرُ محجوب** حتى يُبنى مسارُه إلى الطالب (وهو مسلك `109` يوم بلغت
المزاوجةُ المحرّرَ قبل أن تبلغ الطالب).

✅ **و`113` مطبَّقٌ على الحيّة في ٢٤ سبتمبر** — وقيس بعده أنّ المثبَّت يطابق
الملفَّ **حرفاً بحرف** (`md5` لجسمَي الدالّتين وطولُهما)، وأنّ `save_question`
تحفظ mcq و msq و gap والمزاوجةَ كما كانت (والمزاوجةُ بدرجاتها = عددُ بنودها)،
وأنّ جاهزيةَ الاختبارات القائمة لم تتغيّر. **وتوقيعُ `save_question` واحدٌ لا
اثنان** — فلا حِملٌ زائد يُنشئ نداءً ملتبساً.

🔑 **ولماذا نمطٌ جديد ولم يكفِ `bank` في `gap`؟** بنكُ `gap` **تلميح**:
نصوصٌ تُعرض والطالبُ يكتب، فالمجال مفتوح — ولا يُشخَّص خطؤه إلا إن خمّن
المؤلّفُ **حرفَ** ما سيُكتب، ولذلك `wrong_map` في `gap` ناقصةٌ بطبيعتها.
وهنا الاختيارُ **مغلق**: كلُّ كلمةٍ ليست صواباً مشتّتٌ معلومٌ باسمه ⇒ يسعها
كودُها. **وذاك سببُ وجود النمط، لا زينةً في عرضه.**

🔑 **والبنيةُ مزاوجةٌ موضعُ بنودها الجملة** — ولهذا لم تُكتب دالّةُ تصحيحٍ
ثانية: `accept.pairs` مفاتيحُها **أرقامُ الفراغات** نصّاً، فتعمل عليها
`grade_match` و`match_pairs` و`match_key_ok` **بلا تعديل**. والقائمة
`[{k,t}]` بمفاتيح — درسُ `112` بحرفه: تحريرُ حرفٍ في نصّ كلمةٍ كان يقطع كلَّ
إشارةٍ إليها، في المفتاح وفي التشخيص، **وبصمت**.

- **حارسان يمنعان تسرّبه إلى طالب:** `save_question` ترفض إضافته إلى اختبارٍ
  **منشور** (فلا يتغيّر اختبارٌ حيٌّ تحت أقدام طلابه)، و`quiz_readiness`
  تحجب النشر ما دام في الاختبار سؤالٌ منه. ولو تُرك لسقط إلى الطالب في فرع
  `else` من `submit_attempt` **سؤالاً مقالياً بلا تصحيح** — والصمتُ يُصدَّق.
- **وحارسٌ منشأ `cloze_keys_ok`:** `match_pairs` تعدّ ولا تقرأ، فمفتاحٌ اسمه
  «٩» في سؤالٍ بفراغين يُعَدّ صحيحاً ثمّ لا يُقرأ أبداً — مفتاحٌ يتيم.
- **ولا فراغَ واحداً:** فراغٌ واحدٌ من قائمةٍ **اختيارٌ من متعدد متنكّر**،
  ونمطُه أوضح. (نظيرُ حكم `msq`: «صحيحه واحد هو اختيارٌ واحد متنكّر».)
- **والصفُّ لا زرَّ حذفٍ له:** الفراغ يُحذف من **النصّ** لا من الجدول —
  وزرٌّ يحذف صفّاً بينما مصدرُه سطرٌ فوقه يُنتج جدولاً يكذب على نصّه.

🔴 **وعطلان صامتان أُصلحا في الطريق — كلاهما في `parseImport`:**

- **`opts` تُقرأ قبل تعريفها:** كانت `const` في ذيل الدالّة وفرعُ المزاوجة
  يقرؤها في صدرها ⇒ `ReferenceError` في منطقة الموت الزمنيّ. **فكلُّ ملفٍّ
  فيه سؤالُ مزاوجةٍ يسقط تحليلُه** — والخطأ في الطرفيّة لا في اللافتة، فلا
  يراه المؤلّف أصلاً. *(قيس على نسخة `HEAD` قبل الإصلاح وبعده.)*
- **وفرعُ المزاوجة بلا `return`:** يسقط بعد فحصه إلى «نمط غير معروف
  matching» تحته. نائمٌ خلف الأول، ويستيقظ بزواله.

🔑 **و`KEYED` اسمٌ واحدٌ لثلاثة مواضع** (الحفظ · التصدير · الاستيراد): سقط
`matching` من أحدها مرّةً (`112`) فعاد الاستيرادُ بلا مفاتيحَ ولا تشخيصٍ بلا
شكوى، وكاد يسقط `cloze` الآن. **شرطٌ يُكتب ثلاثاً يتفارق — واسمٌ يُضاف إليه
النمطُ مرّة.** ومثلُه `F_KIND` تُشتقّ من `F_LABEL`: كانت قائمةَ الأنماط
مكتوبةً مرّتين، فنمطٌ يُضاف إلى إحداهما يظهر في القائمة ولا يُفلتِر.

📐 **وتسميتان لبنيةٍ واحدة (`L_MATCH` · `L_CLOZE`):** المزاوجةُ وهذا النمط
شيءٌ واحد في القاعدة ويختلفان في الاسم وحده — بندٌ ومقابلٌ هناك، وفراغٌ
وكلمةٌ هنا. ⇒ تُمرَّر التسمياتُ ولا تُنسخ الشيفرة. **والنسختان تتفارقان
دائماً** — وهو الداء الذي وُلدت `render_q.js` لدفعه.

✅ **وقُرئ الحيُّ قبل الاستبدال (⓪·ب ①) — ومُقيساً لا ظنّاً:** جُزّئت
`save_question` و`quiz_readiness` إلى مقاطع، وقُورن `md5` كلِّ مقطعٍ لم يُمسّ
بنظيره من `pg_get_functiondef` الحيّة — **خمسةُ مقاطع تطابقت حرفاً بحرف**.
وهو الفحص الذي لو وُجد في ٥ سبتمبر لَما وقع الهدم.

✅ **والفحص لكلّ حالةٍ بما يخصّها (④):** ١٢ حالةً لفرع `save_question` · ٨
لفحوص `quiz_readiness` (بأسئلةٍ مصطنعةٍ في اختبارٍ مسودّة) · ١٣ لـ`czEnsure`
و`czCtx` · ١٩ لرسم `render_q` · ٢٠ لـ`parseImport`. **وكلُّ ما مسّ القاعدة
جرى في `begin … rollback`** وقيس بعده أنّ القيد والدوالّ والصفوف كما كانت.

🟡 **وما بقي — الخطوة التالية:** `get_quiz` ترسله (بخلط القائمة بالبذرة كما
في المزاوجة) · و`submit_attempt` تصحّحه (`grade_match` جاهزة) · و`quiz.js`
تحمل إجابته (`a.pairs` بعقدها) · و`match_dnd.js` **تعمل عليه بلا تعديل**:
الخانةُ `.pair-slot` والكلمةُ `.bank-w` بمفاتيحهما. ويبقى قرارٌ واحد: هل
تُمحى كلمةٌ من خانتها بزرٍّ ظاهر أم بالاستبدال وحده؟

**٢٤ سبتمبر — تنسيقُ «إكمال من قائمة» بعد شكوى المؤلّف (`b85`) 📐**

ثلاثٌ، وأصلُها واحد: **الشاشة تقول الشيءَ مرّتين، وترتّبه بعكس ما يُعمل به.**

- 🔴 **«سياقُ الفراغ» سقط** — كان يتصدّر كلَّ صفّ (`…This is a ▭`) والجملةُ
  **مكتوبةٌ بحرفها في الصندوق فوقه ومرسومةٌ بخاناتها في المعاينة تحته**،
  فكان ثالثةً لا تزيد علماً وتأكل ألفَ بكسلٍ من الصفّ. ورقمُ الفراغ وحده
  يكفي للإشارة إليه — **وهو ما يُرسَل في المفتاح أصلاً.**
- **والصفُّ صار خليّةً في شبكة:** حقلٌ كلمتُه واحدة لا يسكن صفّاً بعرض
  الصفحة. و`auto-fill` لا عددٌ ثابت: عمودان في الواسعة وواحدٌ في الضيّقة،
  بلا استعلامِ وسطٍ يُكتب مرّةً ثم يُنسى عند رتبةٍ جديدة.
- **والمعاينةُ نزلت إلى الذيل، وصارت السؤالَ كاملاً** بقائمة كلماته —
  وكانت في الصدر مجرَّدةً من القائمة لئلّا تُكرِّرها. 🔑 **وأعلى الشاشة
  موضعُ ما يُكتب، وأسفلُها موضعُ ما يُراجَع:** المؤلّف يكتب أوّلاً ثم ينظر
  ماذا صنع، فلا تُدفَع الحقولُ إلى الأسفل بما لم يُصنَع بعد.
- **و«＋ خلطٌ متوقَّع» انتقل إلى ذيل رأس الخليّة** — سطرٌ يُوفَّر في كلّ
  فراغ، وعرضٌ كان يفضُل عن الكلمة صار يحمل الفعل. ⚠️ **والإدراجُ تبعه:**
  `beforebegin` كان سيدسّ صفَّ الخلط **داخل الرأس** بجوار الحقل ⇒ صار
  يُذيَّل بالخليّة.

📏 **وقِيس في المتصفّح لا في الظنّ** (ثلاثُ رتبٍ: ١٤٤٠ · ٩٠٠ · ٣٩٠):
عمودان يُنتجان شبكةً **٢٤٠px** وبطاقةً ١٠٢٨، وعمودٌ واحد ٣٢١ و١١٠٩.
والفراغُ بجانب الخليّة القصيرة ٨٨px — **أرخصُ من ٨١px تُضاف إلى طول
البطاقة كلِّها**، وهو خلفيّةٌ بين حدودٍ لا خليّةٌ مكسورة.

🔴 **وعطلٌ في التنسيق ظهر بالقياس على الهاتف — ويُصيب المزاوجة كذلك:**
صفُّ الخلط عند ≤٥٦٠px كان يُسنَد تلقائياً في ثلاثة أعمدة، **فتقع
«فالتشخيص» في عمود السكّة (٣٢px) فتُقصّ** — تُقرأ «فالتشخي». ⇒ الطرفان
يُثبَّتان في الصفّ الأول، والتسميةُ والقائمة في العمود الأوسط، والفواصلُ
الفارغة تُخفى.

🔤 **والعدد والمعدود:** «٢ كلمات زائدة» خطأٌ يقرؤه المعلّم في كلّ سؤال —
صارت «كلمتان زائدتان»، و`cnt()` تُفرد وتُثنّي وتجمع للنمطين معاً
(**والمثنّى يُغني عن رقمه فلا يُسبَق به**).

**٢٤ سبتمبر — «إكمال من قائمة» يبلغ الطالب · الملفّ `114` · `b86`** 🖐️

اكتمل النمط: يُؤلَّف ويُنشر ويُجاب عنه ويُصحَّح ويُشخَّص. ورُفع قفلُ المرحلة
وحارسُه — **بعد أن بُني ما كانا يحرسانه لا بتجاوزهما.**

🔑 **ولا دالّةَ تصحيحٍ جديدة، ولا آلةَ إسنادٍ ثانية.** `accept.pairs` مفاتيحُها
أرقامُ الفراغات نصّاً ⇒ `grade_match` تصحّحه بحرفها. و`match_dnd.js` تعمل
عليه **بلا سطرٍ واحد** من التبديل: الخانةُ `.pair-slot` والكلمةُ `.bank-w`
بمفتاحيهما. **ومكسبُ 112 يُقبض اليوم:** الهُويّةُ بالمفاتيح جعلت النمطين
شيئاً واحداً في القاعدة، فكلُّ ما بُني للمزاوجة انطبق عليه.

- **ودرجةٌ لكلّ فراغ** لا حكمٌ واحد على الجملة (كالمزاوجة، وبخلاف `gap`):
  القائمةُ مشتركةٌ بين الفراغات، فالسؤالُ إسنادُ مجموعةٍ إلى مجموعة —
  وحكمٌ واحد يُخفي أيَّ فراغٍ سقط، **وذاك ما يقيسه التشخيص.**
- **والقائمةُ تُخلَط بالبذرة بمفتاحها لا بموضعها** (112) — فلا تقفز كلمةٌ
  أمام الطالب إن حُرِّر نصُّها بين محاولتين.
- **والنصُّ المقروء يُحفَظ في `essay_text` بجوار المفاتيح** — فالسجلُّ يُقرأ
  بعد سنةٍ بلا مفتاح فكّ، والإشارةُ لا تنقطع.

🔓 **وسؤالٌ كان مفتوحاً أجابت عنه الشيفرةُ القائمة لا قرارٌ جديد:** «كيف
تُمحى كلمةٌ من خانتها؟» — `match_dnd` تُفرّغ الخانةَ الممتلئة **بنقرةٍ
عليها** منذ `b77`. فلا زرَّ ✕ في متن الجملة، ولا قرارَ يُتَّخذ.
⚠️ **وثمنُه سطران:** الوسمُ `filled` كان يُعلَّق على `.pair` وحدها،
و«إكمال من قائمة» خانتُه في متن الجملة بلا غلاف ⇒ صار يُعلَّق **على
الخانة نفسها وعلى صفّها**. ولولاه لبقيت خانةٌ فيها كلمةٌ تبدو فارغة.

🔴 **وعطلٌ عمرُه ملفّان أُصلح في الطريق:** مراجعةُ «إكمال الناقص» تعرض
`{{1}}` **خاماً** منذ `63` — يقرأ الطالبُ قوسين معقوفين في جملةٍ عربية.
⇒ `bodyRead()` ترسم الفراغ **برقمه**، فيقابل ترقيمَ إجاباته تحته. وتخدم
النمطين، ولا تمسّ ما لا فراغَ فيه.

✅ **وقُرئ الحيُّ قبل الاستبدال ومُقيساً (⓪·ب ①):** سبعةُ مقاطعَ من
`get_quiz` و`submit_attempt` قُورنت بـ`md5` بنظائرها الحيّة — **تطابقت
حرفاً بحرف**، والفرقان الوحيدان تعليقان كتبتُهما. و`save_question` و
`quiz_readiness` لم تُنسخا بيد أصلاً: **وُلّدتا من `113` بحذفٍ مقيس**،
وطُبِّقتا بجراحةٍ على نصِّهما الحيّ — فاستحال أن ينحرف ما لم يُقصد مسُّه.
وبعد التطبيق: `md5` الدوالّ الأربع = ما في الملفّ بالضبط.

✅ **والفحص لكلّ حالةٍ بما يخصّها (④):** ٧ لوصول السؤال (get_quiz لا
تُرسل مفتاحَ إجابة) · ٦ للتصحيح (أصاب · أخطأ بكودٍ مسجَّل · بكلمةٍ بلا كود
⇒ HLF · أخطأ الكلَّ · لم يُجب · فراغٌ واحد) · ٤ لرفع القفل · **١٣ في
متصفّحٍ حقيقيّ** للإسناد (نقرتان في الاتّجاهين · استبدال · تفريغ ·
`Enter`/`Space` بلا فأرة · النصُّ المخفيّ و`aria-live`) · **٥ لانحدار
المزاوجة** (‎.pair.filled‎ وزرُّ الإزالة كما كانا) · و٧٥ وحدةً.
**وكلُّ ما مسّ القاعدة جرى في `begin … rollback`.**

**🔴 صيانةٌ واجبة — تسبق أي بناء:**

- 🔴 **`STATE.md` في المستودع متأخّرة** — *مقيسٌ ١٨ سبتمبر*. تُرفع النسخة
  الحيّة، وحارسُها سطرٌ في ٠·ج ⑥.
- `00_schema.sql` يُعاد استخراجه — متأخّرٌ بواحدٍ وثلاثين ملفّاً، ورأسه
  يُصحَّح من المتن.
- `77` ثقبٌ في `sql_log` بلا صفّ — يُحسم بسطرٍ (انظر ⑦).
- جدولٌ بلا سياسة؟ السياسات نقصت بينما زادت الجداول — يُفحص (⑦).
- 🆕 **قوسٌ شاردٌ في ذيل `css/screens.css`** — `}` زائدةٌ في آخر سطر، سابقةٌ
  لهذه الجلسة. والمتصفّحات تتجاوزها بلا أثر (خطأُ تحليلٍ يُهمَل)، فلم تُمسّ —
  لكنّها تُرى ولا تُصدَّق: من يُلحق قواعدَ بعدها يظنّها داخل كتلةٍ ليست هناك.
- 🆕 **ركامُ التجارب يُصفَّر:** محاولاتٌ مقاليّة بلا تصحيح تعود إلى أغسطس،
  وأكثرُها بحساب تجربة. **وهي تُبقي عدّاد المعلّم مرتفعاً أبداً — وجرسٌ لا
  يسكت يُعلّم الصَّمَم.** ويُصفَّر بـ`begin; … select … rollback;` أولاً.

**٢٥ سبتمبر — التأليف يبلغ المعلّم (`115`) 🔓**

🔴 **عطلٌ صامتٌ عاش منذ أوّل يوم:** `curators` جدولٌ فارغ ⇒ `can_curate` =
`is_admin()` ⇒ **`can_author` = `is_admin()` حرفياً.** فالمعلّمون الثلاثة لا
يملكون تأليف حرفٍ واحد — والشاشة تقول «لا تملك التأليف في هذه المادة»،
**فيُقرأ منعاً مقصوداً لا قفلاً مُطفأً**. والتصديق في الأرقام: ١٦ مصدراً
كلُّها `created_by` فارغ · وصفرُ دروسٍ لغير المدير.

- **والتعليق كان في جسم الدالّة ينتظر** (`-- غداً: or is_teacher()`).
  **ولم يُؤخذ بحرفه:** مطلقةً تمنح كلَّ معلّمٍ تأليفاً في إحدى وعشرين
  مادّة، فيها ما لا يُدرّسه. ⇒ القيدُ `teacher_subjects` — المادّةُ التي
  عليها سعتُه وقبولُه، فالإعلانُ قائمٌ ولا يُنشأ له إعلانٌ ثانٍ.
- 🔑 **وليس تغييراً في البنية بل تشغيلُ ما بُنيت له.** القسمة مرسومةٌ في
  المخطّط، وقُرئت من الحيّ: `can_curate` ⇐ **الهيكل** (`save_lesson` ·
  `publish_lesson` · `save_strand` · `save_card`)، و`can_author` ⇐ **الحشو**
  (`save_item` · `save_question` · `save_quiz` · `author_tree`). فالمعلّم
  **يُثري داخل درسٍ قائم ولا يُنشئ درساً ولا ينشره**. و`DEST.teacher` فيها
  `editor` منذ أن كُتبت، ورأسُ `editor_items.js` مكتوبٌ على «إضافات المعلمين
  — إثراء لا يحجب · باسم مؤلّفه». **الواجهة كانت جاهزةً والمفتاح مُطفأ**،
  ولذلك لم يُرفع `BUILD`: لا سطرَ واجهةٍ تغيّر.
- ✅ **وفُحص بهويّاتٍ حقيقية (§٧)، ولكلّ حالةٍ فحصُها (④):** سبعُ حالاتٍ
  لـ`can_author` (معلّمٌ في مادّته · وفي غيرها · معلّمٌ بلا موادّ · طالب ·
  مدير · زائر)، وثلاثٌ لـ`can_curate` **تشهد أنّ الهيكل بقي مغلقاً**،
  ثمّ `author_tree` بعينها: المعلّم يرى **١٤ مقرَّراً** في مواده الثلاث
  **ولا واحدَ منها بإشراف**؛ ومعلّمٌ بلا موادّ ٠ · وطالبٌ ٠ · والمدير ٣٨.
- ⚠️ **ومقياسٌ لا يُنسب إلى الشيفرة:** `set_config` في `from` أمام دالّةٍ
  `stable` تصدُق لهويّةٍ واحدة في الجملة، **ولا تصدُق لعدّة هويّاتٍ فيها** —
  الأعمدة الفرعية تُحسب على شجرةٍ واحدة **فتكذب بصمت** وتبقى الأعدادُ
  صحيحة، فيبدو الفحصُ ناجحاً. ⇒ **هويّةٌ واحدة في الجملة الواحدة.**
- 🔴 **وما لم يُنفَّذ عمداً:** إلغاء مراجعة المدير في بوابة التسجيل.
  `handle_new_user` تكتب `student` دائماً، و`admin_decide` وحدها تمنح الدور.
  **وتوسيعُ ما يفعله المعلّم المعتمَد ليس توسيعاً لمن يصير معلّماً** —
  وذاك قرارٌ لم يُتَّخذ بعد، لا خطوةٌ تتبع هذه.

**٢٥ سبتمبر — شارةُ المصدر: مَن كتبه، وهل اعتمدته بيان (`b87`) 🏷️**

🔴 **وعطلٌ صامتٌ أكبرُ من الشارة ظهر في الطريق:** `list_lessons` تُرجع
مصفوفتين — `items` (الرسميّ) و**`extras`** (إضافات المعلمين، وفيها `author`
و`reviewed` و`draft` و`from_my_mentor`) — **و`student.js` لا ترسم `extras`
إطلاقاً.** فما يؤلّفه المعلّم لا يبلغ طالبه أبداً. ولم يُرَ العطلُ لأنّ
التأليف كان مقفلاً على المدير (`115`)، **فلم توجد إضافةٌ واحدة تسقط**.
والبياناتُ واصلةٌ منذ زمن، والشاشةُ تتجاهلها بلا شكوى.

- **وما طلبته الوثيقة — «حذف غير مُراجَع» — لا وجود له:** بُحث في `js`
  و`html` و`css` كلِّها فلا أثرَ للعبارة. **فالمطلوبُ إضافةٌ لا حذف.**
- **والشارة تُضاف ولا تُبدِّل:** «اعتمدته بيان» **بجانب** اسم المؤلّف لا
  مكانه — الاعتمادُ حكمٌ على المادّة ولا يمحو مَن ألّفها، ولو حلَّ محلَّه
  لَما عرف الطالبُ لمن يعود بالسؤال.
- ⚠️ **ولا لونَ لها، وهو قرارٌ لا كسل:** الاعتماد ليس «أصبتَ» ولا «اضغط
  هنا»، فليس له عائلةُ معنًى (طبقة الأدوار). **وكلُّ أخضرَ خارج الحكم
  يُضعف الأخضرَ حين يُحكَم به فعلاً.** ⇒ رقيقةٌ محايدةٌ بنصّها.
- **وصفٌّ واحد للرسميّ وللإضافة** (`itmRow`) — **ونسختان تتفارقان دائماً.**
- **والعدّاد للرسميّ وحده:** الإضافة «إثراء لا يحجب»، فلو دخلت المقام
  لَنقصت نسبةُ طالبٍ **لأنّ معلّمه أضاف مصدراً**.
- 🔴 **وأُصلح معه ما كان سيقع أوّلَ نقرة:** مستمعُ النقر يبحث في `items`
  وحدها، فنقرةٌ على إضافةٍ تُنادي `openItem(undefined)` **بلا رسالة**.
- ✅ **وفُحص على الحيّ في `begin … rollback`:** أُدرج مصدران لمعلّمٍ في درسٍ
  يراه طالبٌ حقيقيّ، فوصل المعتمدُ بمفاتيحه كاملةً، **وحجب `can_see_item`
  غيرَ المعتمد** كما يجب. وقيس بعده أنّ العناصر ١٦ كما كانت.
- ✅ **و`credit()` فُحصت من الملفّ الحيّ لا من نسخةٍ تُكتب في الفحص** —
  سبعُ حالات (بلا مؤلّف · معلّمٌ غريب · معتمد · معلّمك · مسودّة · تهريب
  `<`): ٧/٧.
- 🟠 **وما لا يُرى بعدُ عائقُه محتوى لا تقنية:** لا إضافةَ معلّمٍ واحدة في
  القاعدة (٠ من ١٦) ⇒ **قسم «➕ إضافات المعلمين» لا يظهر لأحدٍ اليوم**،
  ويُرى أوّلَ ما يؤلّف معلّمٌ شيئاً.

**٢٥ سبتمبر — سعةُ المعلّم بيده (`116` · `b88`) 🎚️**

🔴 **وقبل أن يُبنى الحقل أُصلحت كذبةٌ في دالّته:** `set_subject_capacity`
جسمُها `update … ; return true;` — **فتحديثٌ لا يمسّ صفّاً يعود بـ«نجح»**.
ومتى يقع فعلاً: `set_my_subjects` هي التي تُنشئ صفَّ `teacher_subjects`
وتحذفه، فمعلّمٌ نقر مادّةً ولم يحفظ بعدُ **لا صفَّ له فيها** — يضبط سعته،
وتقول الشاشة «حُفظت»، ولا شيء حُفظ. **وهو عطلُ `106` بحرفه.**
⇒ `return found` — كلمةٌ واحدة بلا `drop` ولا تبديلِ توقيع، وفُحص قبلها
أن لا مستدعيَ لها في شيفرةٍ ولا دالّةٍ ولا سياسة.

- **والحقل لا يظهر إلا لمادّةٍ مختارةٍ في القاعدة** لا في الشاشة — فلا
  يُعرض حقلٌ لا يحفظ ما يُكتب فيه. ومادّةٌ رُفع اختيارُها **يُعطَّل حقلُها
  ولا يُخفى**: الاختفاء يُقرأ عطلاً، والتعطيلُ يُقرأ نتيجةً لما فعله
  المعلّم للتوّ.
- **و«السعة ٤٠» خرجت من سطر الوصف:** لمادّةٍ غير مختارة لا صفَّ لها،
  فالأربعون افتراضٌ في `list_teachable_subjects` لا رقمٌ محفوظ. **ورقمان
  لشيءٍ واحد يتفارقان** ⇒ السعةُ في حقلها، وهو موضعها الوحيد.
- **والقصُّ في موضعين بقصدٍ لا بسهو:** الحقل يقصّ راحةً — **وحدّاه يُقرآن
  من `min`/`max` نفسِهما لا من رقمين مكتوبين في الشيفرة** — والقاعدة تقصّ
  حراسةً، فمن ينادي الدالّة مباشرةً لا يفلت.
- ✅ **فُحص على الحيّ في `begin … rollback`:** الحفظ في مادّةٍ يملكها
  `true` · وفي مادّةٍ بلا صفّ `false` (**الكذبة زالت**) · والقصّ ٩٠٠⇐٥٠٠
  و٠⇐١ · و`null` لا تغيّر شيئاً · وحارسُ الدور يرفع «صلاحية المعلم
  مطلوبة» لطالب. وقيس بعده: ٦ صفوفٍ بسعة ٤٠ كما كانت.
- ✅ **و`capBox` والقصُّ فُحصا من الملفّ الحيّ** — أحدَ عشرَ بنداً.
- ⚠️ **ومقياسٌ ثانٍ يُضاف إلى أخيه:** فحصٌ **يكتب ويقرأ في جملةٍ واحدة
  يقرأ ما قبل الكتابة** — لقطةُ الجملة واحدة، فأثرُ دالّةٍ `volatile`
  فيها لا تراه بقيّتُها. أوقعني ذلك في «فشلٍ» كاذبٍ ثلاثِ مرّات.
  ⇒ **الكتابةُ والقراءة جملتان، أو `do $$ … $$`.**
- 🔒 **و«أستقبل طلاباً» (`accepting`) تُرك عمداً** — القرار في ⑨.

**٢٥ سبتمبر — جلسات التدرّب: البنية وحدها وبابُ الزائر مغلق (`117`) 🔗**

معلّمٌ يولّد رابطاً لاختبارٍ أو رزمةِ بطاقات، فيدخل حاملُه بلا حساب ويتدرّب
بتشخيصٍ كامل الجودة، بلا تقييمٍ ولا قياس. **وهذا الملفّ لا يمنح `anon`
شيئاً** — الدالّتان اللتان يمسّهما الزائر في `118`، ومسلكُه مسلك `109`:
بنيةٌ تُختبَر قبل أن تُفتح.

🔑 **والعزل بنيةٌ لا سياسة:** لا مفتاحَ خارجيّاً إلى `questions` ولا
`options` ولا `dx_codes` ولا `quizzes`. و`source_id` رقمٌ **بلا مفتاح
عمداً**. والجدولان: RLS مفعَّلٌ **بلا سياسةٍ واحدة** والمنحُ مسحوب ⇒
**المنفذُ الوحيد دالّةٌ `security definer`، لا جدولٌ مفتوحٌ بسياسة.**

🔑 **والتشخيص يُنسَخ كاملاً ولغةُ المعلّم لا تُنسَخ:** لكلّ مشتّتٍ كودُه
و`student_note` وحده — حارسُ `62` و`95` يمتدّ إلى الزائر. وقيس: صفرُ
أكوادٍ بلا نصّ، و`name`/`remedy` **غائبان من اللقطة كلِّها**.

⚠️ **والمقاليُّ يُستبعَد ويُقال عددُه** (`skipped_essay`): لا مصحِّحَ آلياً
له ولا قارئَ لإجابة زائر ⇒ سؤالٌ يُجاب ولا يُردّ عليه. وفي القاعدة ٢٥.

🔴 **وعطلان أمسكهما الفحصُ قبل أن يُفتح الباب — وكلاهما يسقط مفتوحاً بصمت:**

- **① إغلاقُ دالّةٍ يحتاج ثلاثةً لا واحداً.** بوستجريس يمنح `EXECUTE`
  لـ`PUBLIC`، **وسوبابيس فوقه** يضبط `alter default privileges … to anon,
  authenticated` (يُقرأ في `pg_default_acl`) ⇒ **كلُّ دالّةٍ جديدة تُولد
  وفي عنقها منحٌ صريحٌ لـ`anon`**. فـ`revoke … from anon` وحدها لا تكفي،
  و`revoke … from public` وحدها لا تكفي. ⇒ `from public, anon,
  authenticated` ثمّ منحٌ صريح. **والسطرُ يمرّ بلا خطأ فيُقرأ إغلاقاً.**
  وبناةُ اللقطة — وهما يقرآن المفاتيح والتشخيص — **كانا مفتوحَين**.
  📌 **وهذا نفسُه يشرح `sql/58`:** كلُّ دالّةٍ في المشروع مفتوحةٌ لـ`anon`
  ما لم تُسحب صراحةً — والعملُ لم يبدأ.
- **② منطقُ القيم الثلاث يسقط مفتوحاً.** الحارس كان
  `if not (can_author(s) or v_owner = auth.uid())`، و`quizzes.created_by`
  **فارغٌ في محتوى المنصّة** ⇒ `false or NULL` ⇐ `NULL` ⇒ `if not NULL`
  **فرعٌ لا يُنفَّذ**، فمرّ الحارسُ كأنّه لم يكن **بلا خطأٍ يُرفع**،
  وولّد معلّمٌ رابطاً لاختبارٍ في مادّةٍ ليست له. ⇒ `coalesce` على
  الطرفين و`is not null` قبل المقارنة. **والفراغُ يُغلق لا يُفتح.**

✅ **وفُحص كلُّه على الحيّة في `begin … rollback`، ولكلّ شيءٍ فحصُه (④):**
ثمانيةُ بنودٍ للإغلاق (منحٌ · سياسات · بناةُ اللقطة · مفاتيحُ خارجية)،
وسبعةٌ للقطة (المقاليّ · النصّ المشترك · الخيارات · المفاتيح · التشخيص ·
لغةُ المعلّم)، وتسعةٌ للحارس (مادّةٌ له وليست له · رزمةٌ · فارغة · معدومة ·
نمطٌ مجهول · قصُّ المدّة · طالب). وبعده: الجدولان صفران.

**٢٥ سبتمبر — جلسةُ التدرّب تبلغ الزائر (`118`) 🚪**

**أوّلُ بابٍ يُفتح لـ`anon` في هذا المشروع** — دالّتان ولا ثالثة، ولا
صلاحيةَ على جدولٍ إطلاقاً. وكلُّ حارسٍ **داخل الدالّة**: النافذةُ الزمنية
وحدُّ التكرار وحجمُ الإجابة. فالواجهةُ راحةٌ لا حراسة.

🔑 **وبمصحِّح المنصّة نفسِه لا بمصحِّحٍ ثانٍ:** `grade_gap` و`grade_match`
دالّتان `immutable` على jsonb محض ⇒ تعملان على اللقطة بحرفهما. وما لم
يُغطّياه (mcq · msq) مقارنةُ معرّفاتٍ داخل اللقطة بترتيب خياراتها —
وهو ترتيب `position` مجمَّداً. **ونسختان تتفارقان عند أوّل إصلاح.**

🔴 **وعطلٌ ثالثٌ أمسكه التصميم قبل أن يُكتب:** كودان يُسنِدهما **المصحِّح**
لا خريطةُ المؤلّف — `PRC` (أصاب المعنى وأخطأ الصيغة) و`HLF` (أصاب بعضاً)
— وليسا في `dx_map` ولا `wrong_map`. فلقطةٌ بلا قاموسٍ لهما تُعيد **كوداً
بلا نصّ**، والزائر يقرأ «أخطأتَ» بلا «لماذا». ⇒ `dx_extra` في اللقطة،
و`practice_snapshot_quiz` **معدَّلةٌ لا منشأة** في هذا الملفّ.

- **بابٌ واحدٌ للنمطين** (`submit_practice_answer`): الحارسُ واحد وحدُّ
  التكرار واحد، **وبابان إلى غرفةٍ واحدة يُنسى أحدهما عند أوّل إحكام**.
- **ولا حدَّ للمحاولات** — الجلسةُ تدريبٌ بلا قياس، ومنعُ «التجربة حتى
  الفهم» يمنع الغاية. وحدُّ التكرار ١٢٠/دقيقة **للجلسة**: حمايةُ خادمٍ من
  آلة لا نزاهةُ تقييم، ويسع صفّاً من ثلاثين يجيب أربعاً في الدقيقة.
- ⚠️ **والبطاقةُ لا تُصحَّح:** التقديرُ يُسجَّل و`is_correct` **يبقى
  فارغاً** — لا `true` ولا `false`. وصفٌّ يقول «صحيح» عن حكمٍ ذاتيّ
  **يكذب على كلّ من يقرأ الجدول بعد سنة**.
- **والحذفُ في القاعدة لا في الواجهة:** `key` و`explanation` و`dx` و
  `wrong` تُسقَط في `open_practice_session` — فما يصل المتصفّحَ يُقرأ في
  أدوات المطوّر مهما أُخفي في الرسم.

✅ **الفحص — أربعُ جولاتٍ لأربعة أشياء (④):** ١١ بنداً للتسريب والنافذة
(ولا `key` ولا `explanation` ولا `dx` ولا `correct_id` في المرسَل · ٥١
سؤالاً بلا مقاليّ · توكنٌ مجهول · رابطٌ منتهٍ) · ١١ للتصحيح (mcq صواباً
وخطأً **بكودٍ ونصّه** · الشرح يُكشف بعد الإجابة · gap صواباً وهراءً ·
حراسةُ المرجع والتوكن والحجم) · ١١ للمزاوجة والإكمال والبطاقات (والصوابُ
يُردّ **نصّاً لا رمزاً** — درس `112`) · وبندان لأنّ **المرفوض لا يكتب
صفّاً**. وبعده: الجدولان صفران.

⚠️ **ومرّتين كان «الفشل» حسابي لا في الدالّة** — عددُ الصفوف المتوقَّع
أُحصي فيه التوكيدُ مع التسليم. ⇒ **العددُ يُقاس فرقاً (قبل/بعد) لا
يُكتب رقماً في الفحص.**

📌 **وعَرَضاً:** فحصُ المنح أنذر على `practice_cards` (من `105`) لأنّها
مفتوحةٌ لـ`anon`. وقُرئت: تحرس نفسها بـ`auth.uid() is null ⇒ raise`.
**فلا تسرّب — لكنّها محروسةٌ بالمنطق وحده**، وهي حال المشروع كلِّه حتى
يُنفَّذ `58`.

**٢٥ سبتمبر — حمولةُ الزائر تُردّ إلى عقد `render_q` (`119`) 📐**

`render_q.js` هو الراسمُ الواحد («ما يراه المؤلّف هو ما يراه الطالب —
لأنها الدالّة نفسها»)، **وعقدُه يحدّده `get_quiz` لا غيرُه**. وقُورن به ما
يرسله `118`، فإذا ثلاثةُ أسماءٍ تتفارق وسلوكٌ ناقص — **وكلُّها تنكسر
صامتةً، وهي داء `112` بعينه: شيفرةٌ ترسم حقولاً ليست في العقد.**

- 🔴 **`item_key` واسمُه في العقد `k`.** و`render_q` يقرأ `p.k` في
  المزاوجة ⇒ `data-k=""` في كلّ خانة، فتعود `pairs` بمفاتيحَ فارغة،
  **فتُصحَّح كلُّها خطأً بلا خطأٍ يظهر**.
- 🔴 **`passage` واسمُه `passage_id`** ⇒ لا يُربط سؤالٌ بنصّه المشترك.
- 🔴 **`gaps` غائبة** — و`get_quiz` تحسبها بـ`gap_count(body)`.
- 🔒 **والبنكُ كان يُرسَل خاماً بترتيب التأليف** — وهو ليس تجميلاً:
  يضع مقابلَ البند الأوّل أوّلاً ⇒ **الموضعُ يكشف الإجابة**، فيُصيب من
  يجهل. ولهذا يخلطه `get_quiz`. ⇒ يُخلَط في اللقطة، **بالمفتاح لا
  بالموضع ولا بالنصّ** (درس `112`)، وثابتاً لا عشوائياً: اللقطةُ مجمَّدة
  ودالّةُ بنائها `stable`، فالبذرة `id السؤال + مفتاح العنصر`.

✅ **والفحص قارن العقدَين مفتاحاً مفتاحاً:** لا مفتاحَ يرسله `get_quiz`
يغيب عنّي · و`k` حاضرٌ غيرُ فارغ · والبنك `[{k,t}]` يصل · ومخلوطٌ ·
و`gaps` محسوبة · و`passage_id` بالاسم · **ولا تسرّبَ بعد التعديل**.

⚠️ **وفرعٌ لا تختبره البيانات الحيّة:** **لا سؤالَ `gap` واحدٌ في القاعدة
له بنك** (٠ من ١٤) — فالبنكُ فيه تلميحٌ اختياريّ. فحُصّ الفرعُ **ببيانٍ
مصطنعٍ في `begin … rollback`**: مصفوفةُ نصوصٍ · لا عنصرَ يسقط · مخلوطة ·
وهي العناصرُ نفسُها. وبعده ٤٠٥ أسئلةً كما كانت.
📌 **والقاعدة: فرعٌ بلا بياناتٍ حيّة يُصطنع له بيان، ولا يُقال «فُحص».**

**٢٥ سبتمبر — شاشةُ الزائر: `js/practice.js` (`b89`) 🎫**

`‎#/t/‹توكن›‎` يفتح جلسةَ تدرّبٍ بلا حساب. **وتُعترَض قبل `boot` عمداً:**
`boot` تُظهر البوابة لمن لا حساب له — أي للزائر دائماً. ولا مستمعَ لتغيّر
الجلسة معها: `SIGNED_OUT` يصل كلَّ تبويبةٍ مفتوحة، **فيقذف الزائرَ إلى
بوابةٍ لا حسابَ له يخرج منه**. و`mathBoot` وحدها تبقى.

🔑 **ولا راسمَ ثانٍ:** السؤالُ يُرسم بـ`render_q.js` عينِها والمزاوجةُ
تُسنَد بـ`match_dnd.js` عينِها — **ولذلك رُدَّت حمولةُ القاعدة إلى العقد
في `119`، ولم يُعدَّل الراسمُ ليقبل شكلاً ثانياً.** وما يراه الزائر هو ما
يراه الطالب لأنها الشيفرة نفسها.

- **وسؤالٌ واحدٌ في الشاشة بخلاف شاشة الاختبار:** ثَمَّ يُجاب الكلُّ ثمّ
  يُصحَّح، **وهنا التشخيصُ يقع حيث يقع الخطأ** — وتأخيرُه إلى الآخر
  يجعله مراجعةً، والمراجعةُ ليست تدرّباً.
- ⚠️ **ولا درجةَ في الختام ولا نسبة** — حكمٌ لا زينة: رقمٌ في الآخر
  يحوّل التدرّبَ امتحاناً في ذهن من جلس له. فالخاتمةُ **ما تكرّر من
  التشخيص**، مجموعاً على النصّ لا على الكود (`62` · `95`).
- **والتنويهُ قبل البدء لا بعده** (`PLEDGE`): من ظنّ أنّه يُمتحَن يقيس
  نفسه بما لا يُقاس.
- **والبطاقةُ ظهرٌ ثمّ حكمٌ على النفس** — أربعُ درجاتٍ لا «صحيح/خطأ».

🔴 **وعطلان قِيسا في المتصفّح لا في الظنّ:**
- **سطرُ الصواب كان يعرض مفاتيحَ التأليف:** «i١: طويل» — رمزٌ لا يعنيه
  الزائر، ورقمٌ عربيٌّ داخل حرفٍ لاتينيّ. ⇒ المفتاحُ يُترجَم إلى **نصّ
  البند** من `options`. وصار: «قصير ← طويل · غني ← فقير».
- **وتبديلُ الـhash وحده لا يُعيد تحميل الوحدات** ⇒ لصقُ رابط تدرّبٍ في
  شريط العنوان **لا يفعل شيئاً وبصمت**. ⇒ مستمعُ `hashchange` يُعيد
  التحميل عند دخول التوكن أو الخروج منه. (والمنصّة لا تستعمل الـhash في
  موضعٍ آخر إطلاقاً — فُحص، فالتحميلُ لا يقطع مساراً.)

✅ **وقيس كلُّه في متصفّحٍ حقيقيّ على جلستين حيّتين:** التنويه · المزاوجة
بنقرتين مع إعلان `aria-live` · **التشخيص بنصّ الطالب** («نصفُ الخيار
صحيحٌ…» = `HLF`) · «أصبتَ ٣ من ٤» · «إكمال من قائمة» صواباً كاملاً ·
الخاتمةُ بلا درجة · والبطاقةُ قلباً وتقديراً وانتقالاً.

**ومعها جانبُ المعلّم — `js/practice_links.js` (`b89`) 🔗**

صندوقٌ **واحد** يخدم الاختبارَ والرزمة، والنمطُ وسيطٌ لا نسختان — وداءُ
النسختين مسجَّلٌ في هذا المشروع ثلاثَ مرّات (`KEYED` · `F_KIND` ·
`L_MATCH/L_CLOZE`).

- **وموضعُ الزرّ شريطُ الأوامر** في محرّر الاختبار: الفعلُ واقعٌ على
  الاختبار كلِّه، فلا يسكن بطاقةَ سؤال (حكم `b72`). وفي البطاقات صفُّ
  أفعال الرزمة.
- 🔑 **ولا يشترط نشراً:** النشرُ يُدخل الاختبارَ البوّابةَ والدرجة، وهذه
  جلسةٌ لا تُقيَّم. **لكنّ حالَ الأصل يُقال قبل التوليد** — «هذا الاختبار
  مسودّة، واللقطة تُجمَّد على حاله الآن» — فلا يُجمَّد مسودّةٌ على غفلة.
- **ولا زرَّ لرزمةٍ فارغة:** الدالّة تردّها («لا بطاقة في هذه الرزمة»)،
  **وزرٌّ لا يُنتج إلا رسالةَ رفضٍ يُعلّم أنّ الأزرار تكذب**.
- **والعنوانُ يُبنى من الصفحة الحاضرة** لا من ثابتٍ مكتوب: المنصّة تعمل
  على Pages وعلى خادمٍ محلّيّ، وثابتٌ هنا يُنتج رابطاً يشير إلى موضعٍ
  آخر في إحداهما — **وبصمت**.
- **والنسخُ قد يُرفض** (سياقٌ غير آمن · إذنٌ محجوب) ⇒ الحقلُ يبقى ظاهراً
  محدَّداً ليُنسخ باليد، والرفضُ يُمسَك ويُقال.
- **والمقاليُّ المستبعَد يُقال عددُه في الصندوق** لا في سجلٍّ يُقرأ بعد
  شهر: المعلّم ولّد رابطاً ظنَّه كاملاً.
- **وقائمةُ الروابط مصفّاةٌ على (النمط · المصدر)** — المعلّم واقفٌ على
  اختبارٍ بعينه، وقائمةٌ بكلّ روابطه تُقرأ فهرساً لا سياقاً. وعددُ
  الإجابات **مجموعٌ بلا اسمٍ ولا تفصيل**.

🟡 **وحدُّ ما قيس في هذا الجانب — يُقال ولا يُدَّعى غيرُه:** فُحصت
الوحدةُ من ملفّها (بناءُ الرابط في ثلاثة مواضع · المُدَد داخل حدّي
القاعدة · الزرّان موصولان فعلاً · لا نسخةَ ثانية · نداءات `api`
معرَّفة) — **ولم يُنقر الصندوق في متصفّح**، إذ يقتضي دخولَ معلّم.
⇒ **يُجرَّب بحسابك قبل أن يُعدّ منتهياً.**

**٢٥ سبتمبر — سياسة الخصوصية: صفحةٌ ثابتة وتذييلٌ يبلغها (`privacy.html`) 📄**

شرطُ Google لنشر تطبيق OAuth أربعةُ حقولٍ في Branding، والناقصُ منها
**رابطُ سياسة خصوصية**. وبدونه يبقى «الدخول بحساب Google» في وضع
Testing ⇒ لا يدخل إلا مَن يُضاف بيده واحداً واحداً.

- **صفحةٌ مستقلّةٌ عمداً:** لا تستورد `base.css` ولا `screens.css` ولا
  جافاسكربت. هي عقدٌ مع المستخدم وشرطٌ لدى طرفٍ ثالث، فلا يجوز أن
  يكسرها تغييرٌ في نظام تصميم التطبيق ولا تعطُّلُ وحدةٍ في `js/`.
  ⚠️ وألوان الهوية **منسوخةٌ** فيها ⇒ غيّرتَها في `base.css`؟ غيّرها هناك.
- **والشعار مضمَّنٌ لا مرتبط** — الـSVG الخارجيّ لا يقرأ متغيّرات CSS
  للصفحة المضيفة. وهو على لوحةٍ كحليّة ⇒ `--logo-ink` أبيض
  و`--logo-hole` كحليّ. والنسخة المرجعية في `claude/logo-reference.md`.
- **ومضمونُها مقروءٌ من البنية الحيّة لا من نموذجٍ عامّ** — الجداول
  والأعمدة قُرئت قبل الكتابة. ومنه **وعدٌ صار معلَناً**: `card_reviews`
  خاصّةٌ بلا استثناء لا يقرؤها معلّم. كان تعليقاً في القاعدة فصار التزاماً
  منشوراً. وكذلك أنّ سِجلّ الإجابات يُحفظ ولا يُعدَّل.
- **والتسجيل الصوتي قسمٌ قائم** وإن كان معطَّلاً منذ `20`: كُتب بصيغة
  «قد يطلب» بقيودٍ ثلاثة (لا يبدأ إلا بضغط الطالب · إذنُ المتصفّح له
  سحبُه · يسمعه معلّم المادة وحده، ولا يُدرَّب عليه نظام).
  ⇒ **يُفعَّل يوماً بلا تعديل سياسة.**

**وتذييلٌ يبلغها:** `.site-foot` خارج `.wrap` في `index.html`، محايدٌ
لا عائلةَ له (`--text-muted` · `--line`)، ولا يمسّ `--accent` إلا عند
التحويم حيث يصير تفاعلاً فعلاً.

🔴 **وفي البوابة يقع في فخّ الـ`100vh` نفسه** الذي صُفِّر من أجله
`padding` السفليّ: عنصرٌ في المجرى تحتها يُضيف **شريطَ تمريرٍ لا محتوى
وراءه**. ⇒ فوق ٩٠٠px يُرفع من المجرى ويُلصق بأسفل **عمود النموذج وحده**
لا عرضَ الشاشة — لونُه معايَرٌ على الورق، ولو امتدّ تحت الكحليّ لاختفى.
وفي الضيّق تبقى البوابة بطولها الطبيعيّ فيبقى في المجرى.
✅ **وفُحص بالقياس لا بالنظر:** تجاوزُ الطول **صفر** في ١٢٨٠×٨٠٠
و٣٩٠×٨٤٤، والتذييل عند `x=640 w=640` أي نصفُ النموذج لا نصفُ اللوحة.

⚠️ **و`BUILD` لم يُرفع عمداً** (باقٍ `b89`): لم تتغيّر جافاسكربت، و
`BUILD/MOD` **إنذارُ وصولِ وحداتٍ لا رقمُ إصدارِ موقع** — ورفعُه بلا
تغييرٍ فيهما يجعل الإنذار كاذباً، والإنذارُ الكاذب يُدرِّب على تجاهله.

🔴 **باقٍ عليك (خارج المستودع):** لصقُ `https://bayan-edu.github.io/privacy.html`
في Google Cloud → Branding مع `Application home page`، ثمّ
`Audience → Publish app`، ثمّ Client ID و Secret في لوحة Supabase.

✅ **وأُغلقت الفجوة في ٢٦ سبتمبر:** `favicon.svg` كان مُشاراً إليه في
`index.html` و`privacy.html` معاً و**لا وجودَ له**. صار في الجذر.

🔴 **وثمانيةٌ وثمانون بالمئة منه لم تكن رسماً:** الملفُّ الأصل ٦٩KB،
فيه **بيانُ أصالةٍ مضمَّن (C2PA)** يقول «Created by Recraft AI» —
توقيعاتٌ وشهاداتُ مصدرٍ بـ٦٠KB. **وأيقونةٌ تُحمَّل في كلّ صفحةٍ لا
تحمل ستّين كيلوبايت من التوقيعات.** ⇒ أُسقطت كتلةُ `<metadata>`
وحدها ⇒ **8.4KB**، والرسمُ كما هو (١٠ مسارات).
⚠️ **والأصلُ بتوقيعه باقٍ في `Downloads`** — فمن أراد إثباتَ المصدر
وجده. ولم يُحذف شيءٌ من خارج المستودع.

- ✅ **وفُحص قبل النقل وبعده:** XML سليم (جذر `svg` · ١٠ مسارات · بلا
  BOM) · ولا أثرَ لـ`c2pa` في الناتج · وتُحمَّل في المتصفّح
  (`2016×2016`) · والوسمان يشيران إليها في الصفحتين.
- ✅ **ونُظر إليها بمقاساتها الحقيقية** (٢٥٦ · ٦٤ · ٣٢ · ١٦): قلنسوةُ
  تخرّجٍ فوق كتابٍ مفتوح، بكحليّ الهوية وفيروزيّها وذهبيّها.
- ⚠️ **وملحوظةٌ لا عطل:** على خلفيّةٍ كحليّة تكاد القلنسوةُ تختفي
  (`#0B3359` في `#0D345E`). ولا أثرَ له في الاستعمال — لسانُ التبويب
  فاتحٌ أو داكن، لا كحليُّ العلامة. **يُذكر لمن يضعها على اللوحة يوماً.**
- ⚠️ **و`BUILD` لم يُرفع:** لم تتغيّر جافاسكربت. و`BUILD/MOD` **إنذارُ
  وصولِ وحداتٍ لا رقمُ إصدارِ موقع** — ورفعُه بلا تغييرٍ فيهما يجعل
  الإنذار كاذباً. (بحكم الجلسة السابقة نفسِه.)


🔴 **باقٍ يدويّاً:** جلستان حيّتان أُنشئتا للفحص باسم المدير وتنتهيان
**٢٦ سبتمبر** (اختبار `6` وبطاقات الرزمة `3`). تُترك حتى تنتهي، أو
تُحذف بإذنٍ صريح في جلسة.

**٢٥ سبتمبر — التأليف يُشترط له شهادةٌ يكتبها المصادِق (`120`) 🪪**

تحديثٌ في `claude/proposals-ready-to-build.md` **نسخ نصفَ `115`**: كان
الحارس «معلّمٌ في مادّته»، وصار «معلّمٌ في مادّته **وقد أثبت أنّه يملك
بريده**». و`115` كانت صادقةً حين كُتبت — **المواصفةُ تحرّكت لا الشيفرة
أخطأت.**

🔑 **ولماذا عمودٌ خاصّ لا `auth.users.email_confirmed_at`:** ذاك تابعٌ
لإعداد `Confirm email` العامّ — لا يميّز الأدوار، **وإن عُطّل صار كلُّ
بريدٍ «مؤكَّداً» لحظةَ التسجيل فيسقط الحارسُ بلا أن يتغيّر سطر**.
⇒ `profiles.author_verified_at` مستقلٌّ، ومعه `profiles.phone`
(يُجمَع ولا يُتحقَّق منه — قرارُ مرحلةِ إعداد، وTwilio مؤجَّلٌ عمداً).

🔴 **وثغرةٌ لم تقلها الوثيقة، وهي موضعُ العطل كلِّه:** «تُسجَّل النتيجة في
العمود» — **ومن يكتبها؟** لو كتبتها الواجهةُ لفتح المعلّمُ الطرفيّةَ
ونادى الدالّةَ بلا أن يفتح بريده. **فالواجهة لا تُؤتمن على حراسة نفسها.**
⇒ `mark_author_verified` تقرأ الشهادة من **`auth.mfa_amr_claims`** —
جدولٌ يكتبه المصادِق لا العميل — ومدخلُ العميل الوحيد `session_id`
**داخل رمزٍ موقَّع لا يُزوَّر**.

- **و«ليست كلمة مرور» لا «هي otp»:** الطرائق المرصودة هنا `password`
  وحدها، فاسمُ طريقة رمز البريد **غيرُ معروفٍ في هذه القاعدة**، وكتابتُه
  ظنّاً تُنتج حارساً يرفض الجميع. والنفيُ يصدُق على كلّ طريقةٍ تقتضي
  صندوقَ بريدٍ أو مزوّداً. و`auth.mfa_factors` **صفر** ⇒ لا `totp`
  ممكن، وهو الاستثناء النظريّ الوحيد. **يُراجَع يومَ تُفعَّل الثنائية.**
- 🟡 **وما لم يُقَس فلا يُدَّعى:** حضورُ `session_id` في رمز هذا المشروع
  **غيرُ مرصود** — ولا رمزَ حيٌّ في يد جلسةٍ تعمل بـSQL. ⇒ الدالّة
  **تسقط صائحةً** (`no_session_claim`) لا صامتة، ومعها
  `auth_claims_present()` تقيسه في نداءٍ واحد من حسابٍ حقيقيّ
  (أسماءُ المفاتيح وحدها لا قيمُها). **يُقاس قبل الإطلاق.**
- 🔴 **والتوريثُ قرارٌ لا سهو:** المعلّمون الثلاثة **اعتمدهم المدير واحداً
  واحداً** — بوّابةٌ أقوى من رمز بريد. ولولاه لَسُلب أحدُهم تأليفاً ناله
  في `115` **بلا رسالةٍ ولا سبب**، ولَبحث عن عطلٍ في حسابه.
- **و`register_teacher` تحلّ محلّ `request_teacher_access` للجديد:**
  `role='teacher'` مباشرةً + صفُّ `teacher_subjects`، **بلا إدراجٍ في
  `teacher_requests`**. والقديمتان تبقيان — تقاعدٌ في الواجهة لا حذف.
  🔑 **والمادّة معرّفٌ لا نصٌّ حرّ:** `teacher_subjects` تحتاج
  `subject_id`، ونصٌّ حرّ لا يُنتجه — **شرطُ تنفيذٍ لا زينة**.
  وتُحفظ `bio`/`school`/`years` لأنّ `list_mentors` تُرسلها إلى شاشة
  «اختر معلمك» — **فحذفُها يُولد كلَّ معلّمٍ ببطاقةٍ فارغة أمام طلابه.**

✅ **ثلاثُ جولاتٍ لثلاثة أشياء (④):** ٦ بنودٍ لـ`can_author` (ومنها
**سحبُ الشهادة يُغلق التأليف**) · ٤ لـ`mark_author_verified` (رمزٌ بلا
`session_id` · جلسةُ كلمةِ مرور تُردّ · **جلسةٌ بطريقةٍ أخرى تُقبل** ·
والتكرار لا يُحرّك الوقت) · ١٣ لـ`register_teacher` (الاسم · المادة ·
**الهاتف يُردّ ولا يُفرَّغ صامتاً** · التطبيع إلى E.164 · النبذة والمدرسة
والخبرة · صفُّ المادة · **ولا تأليفَ قبل الشهادة** · ولا صفَّ في
`teacher_requests`). وبعدها: ٣ معلّمين · ١٥ طالباً · ٦ صفوف كما كانت.

**٢٥ سبتمبر — البوابة الموحّدة وشهادةُ التأليف (`b90`) 🚪**

ثلاثُ شاشاتٍ صارت اثنتين: **دخولٌ واحد** (كما كان — ولا يُسأل فيه عن
دور) **وتسجيلٌ بدورين**. وسقط رابط «انضم كمعلم ←» ومعه
`renderTeacherSignup` — **باقياً في الملفّ ولا يُعاد وصلُه**: مسارُه
يُنتج `pending_teacher` ولوحةُ البتّ فيه تقاعدت، فمن وصله سقط في حالةٍ
لا مخرجَ منها.

🔑 **ولماذا لا يُسأل عن الدور عند الدخول:** الدورُ صفةٌ في `profiles`
تُقرأ **بعد** المصادقة. ولو سُئل عنه لصار سؤالاً **يُجاب عنه ولا أثرَ
لجوابه** — يختار «معلم» فيدخل طالباً، فيظنّ العطلَ في حسابه.

🔴 **وتصادمٌ حقيقيّ غيّر شكلَ التنفيذ:** الوثيقة تطلب **منتقي مادّةٍ**
في نموذج التسجيل، وقُرئت السياسات فإذا:
`p_scales_read → true` (فالمناهج تُقرأ بلا حساب)، و
**`p_subjects_read → auth.uid() is not null`** (فالمواد لا تُقرأ قبل
الجلسة). ⇒ **لم تُرخَ سياسةُ أمانٍ قائمة لأجل نموذج.** فُصلت الخطوتان:
البوابة تأخذ البريد والكلمة والاسم، ثمّ `renderTeacherDetails` بعد
الجلسة. **وهي الشاشةُ نفسُها التي سيصلها داخلُ Google** — فالمكوّن
(`role_form.js`) واحدٌ للمسارين كما بُني له.
🟡 *ولو فُضّل النموذجُ الواحد فالبديلُ سطرٌ واحد: `p_subjects_read` إلى
`true` كأختها. قرارُ أمانٍ لا يُتّخذ من طرفي.*

- 🔴 **وبابٌ كان سيُغلق على من تراجع:** من أنشأ حسابه معلّماً ثمّ ترك
  شاشةَ التفاصيل يبقى **طالباً بلا بابٍ يعود منه** — إذ سقط رابط
  «انضم كمعلم». ⇒ `wants_teacher` في بيانات المستخدم الوصفية تعبر
  الفجوة، و`boot` تُعيده إلى الشاشة عند أوّل دخول.
- **والمسوّدة تُحفظ عند تبديل الدور** — ولولاها لفقد المسجِّل بريدَه
  واسمَه لأنّه صحّح دورَه، **فتعلّم ألّا يصحّحه**. ⚠️ وكلمةُ المرور
  **لا تُحفظ عمداً**: لا تُكتب في HTML ولا تُترك في ذاكرةٍ بلا داعٍ.
- **وشاشةُ `renderVerifyAuthor`:** ترسل رمزاً بـ`signInWithOtp`
  (`shouldCreateUser:false`) ثمّ `verifyOtp` ثمّ **تطلب** الشهادة من
  `mark_author_verified` — **ولا تمنحها**. ورسالةُ الرفض تُعرض بنصّها
  لا بـ«تعذّر»: الغامضةُ تُبقي المعلّم يعيد المحاولة على ما لا يُصلحه.
- **والمعلّمُ بلا شهادةٍ لا يُحجب عن المنصّة** — التأليفُ وحده ينتظر،
  ومن حُجب كلُّه ظنّ حسابَه معطّلاً. وفي الشاشة زرُّ «لاحقاً».
- **و«الطلبات» تقاعدت من شريط المدير**، وصار يهبط على «الأداء».
  🟡 **وطرفٌ مكشوفٌ يُقال:** `request_teacher_access` ما زالت ممنوحةً
  ويمكن نداؤها مباشرةً فتُنتج `pending_teacher` **بلا لوحةٍ تبتّ فيه**.
  لا تناديها الواجهة. ⇒ **يُسحب منحُها في ملفٍّ تالٍ.**

✅ **وقيس في المتصفّح:** التبويبان يعملان بـ`aria-pressed` · وحقولُ كلّ
دورٍ تظهر وتختفي · **والمسوّدة تعبر التبديل ذهاباً وإياباً** · والمناهج
تُعاد تعبئتُها (٥ خيارات) · ورابط «انضم كمعلم» غائب · ولا خطأَ في
الطرفيّة.

🟡 **وما لم يُقَس:** التسجيلُ الفعليّ (إنشاءُ حسابٍ حقيقيّ في `auth`)
وخطوةُ الرمز — **ولن أُنشئ حساباً في قاعدتك الحيّة بلا إذن**. يُجرَّب
ببريدٍ حقيقيّ، ومعه `auth_claims_present()` لقياس `session_id`.

**٢٦ سبتمبر — Google والموافقة على السياسة (`121` · `b91`) 🔐**

**والموافقةُ شرطُ تسجيلٍ لا شرطُ دخول:** من دخل فقد وافق يومَ سجّل،
وعرضُها في كلّ دخولٍ **يُعلّم النقرَ بلا قراءة**.

🔑 **وموافقةٌ لا تُسجَّل ليست موافقة:** `profiles.policy_accepted_at`
و`policy_version` — **ونسخةٌ لا «نعم» مجرّدة**، فالسياسةُ تتغيّر ومن
وافق على نصٍّ لم يوافق على تاليه. **وأوّلُ موافقةٍ تُصان**: إعادةُ
النداء بالنسخة نفسِها لا تُحرّك الوقت، ونسخةٌ جديدة موافقةٌ جديدة.

⚠️ **والنسخةُ في موضعين يتفارقان:** `POLICY_VERSION` في `js/policy.js`
وتاريخُ «آخر تحديث» في `privacy.html`. **حرّرتَ السياسة؟ حرّك التاريخين
معاً** — نظيرُ `--page` مع `theme-color`.

🔑 **وقرارُ الوثيقة «معلّمُ Google يتخطّى خطوة التحقّق» تحقّق بلا استثناءٍ
في الواجهة:** `mark_author_verified` (120) تقبل كلَّ طريقةٍ ليست كلمةَ
مرور — و`oauth` منها. ⇒ `boot` تحاول مرّةً صامتةً قبل عرض شاشة الرمز،
**فالقاعدة تقرّر من طريقة الجلسة، والعميلُ لا يقول «أنا Google»**
وقولُه لا يُصدَّق أصلاً. وجلسةُ كلمةِ المرور تُردّ فتظهر الشاشة.

- **وشاشةُ `renderCompleteProfile`** لداخلِ المزوّد: اختيارُ دورٍ
  **بالمكوّن نفسِه** (`roleTabs`) ثمّ يُساق صاحبُه إلى شاشته القائمة —
  ولا تُنسخ حقولُ طالبٍ ولا معلّم فيها.
  🔑 **وشرطُها المزوّد لا نقصُ الصفّ:** مسجِّلُ البريد اختار دورَه في
  البوابة، **فسؤالُه ثانيةً يُقرأ شكّاً فيما قاله**.
- **والموافقة تُعرض فيها إن لم تكن خُتمت** — فمن نقرها في البوابة ثمّ
  انطلق إلى Google لا يُسأل مرّتين، ومن دخل من زرّ «الدخول» يُسأل هنا.
- **و`policy_version` تعبر فجوةَ تأكيد البريد** في البيانات الوصفية
  كأختيها، و`boot` تختمها عند أوّل جلسة — **وإلا ضاعت موافقةٌ وقعت**.
- **وشعارُ Google مضمَّنٌ لا مرتبط**، وألوانُه ألوانُ المزوّد لا الهوية:
  **علامةٌ مسجَّلة لا تُصبغ.** وزرُّه محايدُ اللون فلا يزاحم الفعل الأول.

✅ **وقيس في المتصفّح إلى آخر ما يُقاس بلا حسابك:** المربّع يظهر في
التسجيل ويغيب في الدخول · **والرفضُ يسبق كلَّ فحصٍ آخر** («اقرأ سياسة
الخصوصية…» قبل «اختر صفّك») · وبعد النقر يمرّ · **وزرّ Google يُردّ بلا
موافقة** ثمّ ينطلق بها **فيبلغ صفحةَ Google الحقيقية** — فالمزوّد
مفعَّلٌ والطلب سليم (`redirect_uri` إلى `…supabase.co/auth/v1/callback`
و`scope=email profile`). ولم يُكمَل الدخول: **حسابُك لا حسابي.**

🔴 **وللتجربة المحلّية خطوةٌ عندك:** `redirect_to` يصير
`http://localhost:5173/`، فيلزم إدراجُه في Supabase → Authentication →
URL Configuration → Redirect URLs، وإلا ردَّك سوبابيس إلى Site URL.
**والإنتاجُ يعمل بلا ذلك** إن كان Site URL هو عنوان Pages.

🟡 **وثلاثةٌ لم تُقَس ولا تُدَّعى:** تسجيلٌ حقيقيّ ببريد · وخطوةُ الرمز
(ومعها `auth_claims_present()` لقياس `session_id`) · وربطُ حسابٍ قائمٍ
ببريده نفسِه عبر Google — وهو **إعدادٌ في لوحة سوبابيس** يستحقّ اختباراً
مخصَّصاً قبل الإطلاق.

🟡 **والتسعةَ عشرَ القائمون لم يوافقوا** — لم يُعرَض عليهم شيء، ولم
تُختَم لهم موافقةٌ لم تقع: **ختمٌ عن أحدٍ أسوأ من غيابه.** وعرضُ
السياسة عليهم قرارٌ لم يُتّخذ. ولم تُربَط الموافقةُ بـRLS ولا
بـ`can_author` — **وربطُها يقفلهم خارج حساباتهم في اللحظة نفسِها.**

**٢٦ سبتمبر — موضعُ طريق المزوّد وتذييلٌ يثبت (`b92`) 📍**

**① زرُّ Google يتصدّر ولا يُذيَّل.** كان تحت النموذج كلِّه، **ومن قصده
لم يكن ليقرأ حقلاً واحداً** — فكان يُمرِّر نموذجاً لن يملأه ليبلغ زرّاً
يُغنيه عنه. ⇒ الترتيبُ صار: اختيارُ الدور · طريقُ المزوّد · فاصلُ «أو
بالبريد الإلكتروني» · النموذج. وهو ترتيبُ أكثر المنصّات، وعلّتُه أنّ
**الطريق الأسرع يُعرض أولاً**.
- و`.gate-or` غيرُ `.gate-sep`: ذاك يقول «انتهى الإجراء»، وهذا «طريقٌ
  آخر لنفس الإجراء». **وفرقٌ في المعنى يستحقّ فرقاً في الشكل.**
- 🔴 **وتبعةٌ عولجت:** المربّع صار تحت النموذج وزرُّ Google فوقه، فرسالةُ
  الرفض **تُشير إلى ما لا يراه المستخدم**. ⇒ `askPolicy()` تُمرِّر إليه
  وتُومضه وتُعطيه التركيز. واللونُ `warn` لا `danger`: **لم يُخطئ أحد،
  بقي شرطٌ لم يُستوفَ** (طبقة الأدوار).

**② والتذييل كان يطفو — `position:fixed`.** المثبَّتُ مربوطٌ بالشاشة لا
بالمستند، فإذا كُبِّر العرضُ صار عمودُ النموذج أطولَ من الشاشة **فلا
يتحرّك التذييلُ مع ما تحته**: يبقى معلّقاً فوق الحقول.
⇒ عمودٌ مرن: `body` يملأ الشاشة، والبوّابةُ تأخذ ما بقي، والتذييلُ
يتبعها في المجرى. **فالمجموع `100vh` بالضبط حين يتّسع** — وهي علّةُ
التثبيت الأصلية (شريطُ تمريرٍ بلا محتوى) — **ويكبر الجميعُ معاً حين
يضيق.**

- 🔴 **وثمنٌ ظهر بالقياس فدُفع:** التذييلُ في المجرى يأخذ شريطاً أسفلَ
  الشبكة، **فقصرت اللوحة الكحليّة عن حافّة الشاشة** (قيس: ٦٣٦ من ٧٢٠)
  فظهر تحتها شريطٌ شاحب — وهو عينُ ما صُفِّر من أجله `padding` السفليّ.
  ⇒ خلفيّةُ `body` تُكمل النصفَ الكحليّ. ⚠️ **وليست تدرّجاً وإن كُتبت
  `linear-gradient`:** حدٌّ قاطعٌ عند ٥٠٪ ⇒ نصفان مصمتان، والشعارُ يبقى
  على `.gate-say` المصمتة فثقوبُه تُملأ بلونٍ **مطابقٍ بالضبط**
  (`--logo-hole`). و`to left` لأنّ "say" يقع يساراً في شبكة RTL.

✅ **وقيس بالأرقام لا بالنظر، في ثلاث حالات:**
- **دخول** ١٢٨٠×٧٢٠: تجاوزُ الطول **٠** (لا شريطَ تمريرٍ كاذب) ·
  `static` · أسفلُه ٧٢٠ بالضبط.
- **تسجيل** ١٢٨٠×٧٢٠ (المحتوى أطول): اللوحة `1136/1136` تساوي النموذج ·
  تحت النموذج **٠** بلا تداخل · **وبعد التمرير تحرّك ٤٥٢** — أي بمقدار
  التجاوز تماماً. **والمثبَّتُ كان يتحرّك صفراً: وهذا هو البرهان.**
- **٣٧٥×٨١٢:** الخلفيّةُ المنصّفة **لا تُطبَّق** (`none`) · عرضٌ كامل ·
  ولا تجاوزَ أفقيّ. **فلا انحدارَ في الضيّق.**
- و`askPolicy` قِيست: رسالة · صنفُ الإيماض · **التركيز على المربّع** ·
  ولا مغادرةَ للصفحة.

**٢٦ سبتمبر — نموذجُ المعلّم يعود خطوةً واحدة (`122` · `b93`) 🧩**

سُئل: **لماذا تُكتب المادّة في خطوةٍ ثانية؟** والجوابُ أنّ الانقسام كان
**نتيجةً لا اختياراً**: `p_subjects_read` تشترط `auth.uid() is not null`
بينما `p_scales_read` و`p_levels_read` مفتوحتان — **فنموذجُ الطالب خطوةٌ
واحدة ونموذجُ المعلّم انقسم**. و`register_teacher` تحتاج `subject_id`،
ولا معرّفَ بلا قائمة.

🔑 **ولم تُرخَ السياسة إلى `true` كأختيها، وإن بدا `subjects` شاذّاً
بينهما:** الجدولُ ليس أسماءً فقط — فيه `owner_id` و`placement` و
`progression` و`tool`، **وفتحُه كلِّه لأجل حقلٍ في نموذجٍ يكشف أكثرَ
ممّا يحتاجه**. ⇒ **بابٌ ضيّق:** `signup_subjects()` تُعيد **الاسمَ
والمعرّفَ لا غير**، ممنوحةً لـ`anon`. **والحقُّ يُعطى بقدره.**
⚠️ ولا `family` ولا `icon` ولا `code`: لا يحتاجها المنتقي، **وكلُّ
عمودٍ يُضاف يصير معلوماً لكلّ زائرٍ إلى الأبد — والتوسيعُ لاحقاً سطر،
والتضييقُ بعد الانتشار ليس سطراً.**

- **و`renderTeacherDetails` تبقى ولم تُهدر:** صارت مسلكَ **من سجّل ولم
  تُفتح له جلسة** (تأكيدُ البريد مفعَّل)، أو تَرَك قبل أن يُتمّ. وهي
  الحالةُ الوحيدة التي بقي الانقسامُ لها — **وضرورةٌ لا اختيار**.
- 🔴 **وردٌّ صُحّح في الطريق:** لو فشلت `register_teacher` بعد إنشاء
  الحساب، فالرجوعُ إلى البوابة **يُوهم المسجِّل أنّ حسابه لم يُنشأ
  فيُعيد التسجيل بالبريد نفسه** — والحسابُ قائمٌ والجلسةُ مفتوحة.
  ⇒ يُساق إلى شاشة التفاصيل برسالتها.

✅ **وقيس:** `anon` ينادي الدالّة · تُعيد العشرين مادّة · **ولا تُعيد
إلا `id` و`name`** · ولا أثرَ لـ`owner_id`/`placement`/`tool` في ناتجها ·
**وسياسةُ `subjects` كما هي لم تُمَسّ** · والمنتقي يمتلئ في البوابة
**قبل أيّ جلسة** (٢١ خياراً) وكلُّ حقول المعلّم حاضرةٌ معه.

**بُني وينتظر خطوةً تالية:**

- شريط تصفية الفروع في شاشة المواد — `list_lessons` ترسل `strand` منذ `80`.
  **البيانات واصلة والشريط غير مرسوم**، وينتظر محتوًى موسوماً.
- `editor_strands.js` مكتوبٌ و`editor.js` **لا يستورده** في اللقطة —
  🔍 يُتحقّق: أمُوصولٌ في المستودع أم ينتظر؟
- شارة «نسخة ٢» على الوريث — المسار (ج): التاريخ يُقرأ عند السؤال.
- `tool_readiness` لا تفحص ما يقع عند `done` — **أوّل جلسة تسكينٍ كاملة
  تُجرَّب قبل النشر للطلاب.**

**والعائق محتوى لا تقنية — ولم يتغيّر منذ ١٤ سبتمبر:**

- **عشرُ بطاقاتٍ حقيقية** في مادةٍ واحدة. ومصطلحات البلاغة أنسبُها:
  متشابهةٌ قابلةٌ للالتباس، أي حيث يعمل الخلط بأقوى ما يكون.
- **وسمُ دروس مادةٍ واحدة بالفروع** — فشريط التصفية لا يُبنى على فراغ.
- **ملء `objectives` لدرسٍ واحد** — الحلقة مبنيّةٌ منذ شهور وتنتظر صفوفاً.
  **أعلى عائدٍ تعليميّ في المشروع، وكلفتُه صفوفٌ لا شيفرة.**

**قراران تربويّان ينتظران — لا يُحسمان بالشيفرة:**

- ما مصير درجة طالبٍ أُبطل سؤالٌ من اختباره؟ (الإبطال ≠ التقاعد.)
- هل يُخبَر الطالب أنه أجاب عن نسخةٍ صُحّحت بعده؟

**عملٌ لم يبدأ:**

- 🔴 إحكام `EXECUTE` (`sql/58`) — الأثقل. خطوةٌ تمسّ كلَّ دالّة، وتُجرَّب
  بحساب طالبٍ ومعلّمٍ ومدير. **وعددُ الدوالّ يُقرأ بالاستعلام لا من هنا.**

## ① الروابط

- المنصة: https://bayan-edu.github.io · Supabase: `tjzrfevymfpktfuormhs`
- `bayan-edu.github.io` (عام) · `quiz-content` (خاص)
- `bayan-backup` (خاص · نسخ أسبوعية · استعادة مجرَّبة ١٧ أغسطس)
- ⚠️ مشروع `Test-Prep` (نموذج أوّلي · موقوف) — يُصدَّر أو يُحذف

---

## ② الأرقام

### ٢-١ بنية المنهج

```
٦ سلالم  ·  ١٨ صفاً/مستوى  ·  ٤ مسارات  ·  ٢٠ مادة  ·  ٤٠ مقرَّراً
١٣ نمط مصدر (item_kinds)   ·   ٣٦ كود تشخيص (dx_codes)
```

🔴 **وأعداد البنية (جداول · دوالّ · سياسات) حُذفت من هنا عمداً.**
كانت `٣٤ · ٩٠ · ٤٩` مقروءةً من لقطة ٥ سبتمبر، وقِيست في ١٤ سبتمبر
`٣٦ · ١٠٤ · ٤٧` قبل ملفّات البطاقات. ⇒ **رقمٌ يتغيّر كلَّ ملفّ لا يسكن
في وثيقةٍ تُحدَّث كلَّ أسبوع.** الاستعلام يجلبها.

**وللتقدير وحده — المنتظَر بعد `98`–`100`:** ‎+٥ جداول (`decks` · `cards` ·
`fsrs_params` · `card_reviews` · `card_review_log`) · ‎+١٣ دالّة · ‎+٥ سياسات.
⚠️ **يُطابَق بالاستعلام ولا يُصدَّق من هنا.**

### ٢-٢ المحتوى الحيّ — يُقاس ولا يُنقل

```sql
select set_config('request.jwt.claims', json_build_object('sub',
       (select id from profiles where role='admin' limit 1))::text, true)
  from (select 1) _;

select (select count(*) from questions where retired_at is null)  as أسئلة_حيّة,
       (select count(*) from quizzes)                             as اختبارات,
       (select count(*) from quizzes where published)             as منشورة,
       (select count(distinct variant_key) from questions)        as خانات,
       (select count(*) from lessons where published)             as دروس_منشورة,
       (select count(*) from lessons where strand_id is not null) as دروس_موسومة,
       (select count(*) from objectives)                          as أهداف,
       (select count(*) from cards)                               as بطاقات,
       (select count(*) from profiles where role='student')       as طلاب;
```

**آخر قياس (أغسطس ٢٠٢٦ — يحتاج تجديداً):**

```
اختبارات: ٤ — كلها منشورة   ·   دروس منشورة: ٣   ·   ١٣ طالباً
اختبار ٣ (إنجليزية · أول ثانوي · درس ١): ٥٤ سؤالاً · ١٨ خانة × ٣ نسخ
```

**وقياسُ ١٤ سبتمبر — البطاقات:** `cards` = ١ · `card_reviews` = ١ ·
`card_review_log` = ٣. أوّلُ بطاقةٍ للتجربة، لا للمحتوى.

🔴 **٤٠ مقرَّراً في القاعدة وثلاثة دروس منشورة.** العائق **محتوى لا تقنية**.
والأدوات صارت أوسع: بنكٌ وتشخيصٌ تراكميّ وفروعٌ ومحاكياتٌ وبطاقات — **وكلُّها
تنتظر الشيء نفسه.**

---

## ③ الخريطة — ما الذي يعمل الآن

| النظام | القاعدة | الواجهة | الحال |
|---|---|---|---|
| المنهج والهوية | `scales/levels/paths/subjects/courses` | `auth.js` | ✅ يعمل |
| التأليف | `save_*` · `author_tree` | `editor*.js` | ✅ يعمل |
| الاختبار والتصحيح | `get_quiz` · `submit_attempt` | `quiz.js` | ✅ يعمل |
| التشخيص | `dx_codes` (٣٦) · `dx_map` | شاشة المراجعة | ✅ يعمل |
| بنك الخانات | `variant_key` · `set_variant` | `editor_quiz.js` | ✅ يعمل |
| نمط msq | `41`–`44` | `quiz.js` | ✅ يعمل |
| **التحليلات** | `82`–`96` · `student_performance` · `cohort` | `analytics.js` | ✅ **يعمل** |
| **الفروع** | `79` · `80` · `97` · `strands` | `analytics.js` · `api.js` | 🔶 **قاعدةٌ وأداء · لا تصفية في المواد** |
| **المحاكيات** | `item_kinds` | `simulations.js` | ✅ **يعمل** |
| **البطاقات** | `98`–`105` · FSRS-6 | `flashcards.js` · `card_game_match.js` | ✅ **يعمل** |
| **الجرس والأعداد** | `106` · `my_counts` · `mark_feedback_read` | `student.js` · `api.js` | 🔶 **القاعدة صادقة · الجرس غير مرسوم** || الإرشاد | `choose_mentor` · `mentorships` | `student.js` | ✅ يعمل |
| الرسائل | `messages` + ٣ سياسات | `student/teacher.js` | ✅ يعمل |
| التصحيح المقالي | `attempts` | `teacher.js` | ✅ يعمل |
| الوسائط | جرّة `audio` + ٤ سياسات | `media.js` · `upload.js` | ✅ يعمل |
| تتبّع المصادر | `track_item` · `item_progress` | `student.js` | ✅ يعمل |
| الإدارة | `request_teacher_access` · `admin_*` | `auth.js` | ✅ يعمل |
| عرض الرياضيات | — | `fmt()` · `mathBoot()` في `ui.js` | ✅ يعمل |
| ~~المراجعة المتباعدة~~ | `review_queue` | — | ⛔ **متقاعدة** (⑦) |
| **حلقة العلاج** | `objectives.remedial_item_id` | `quiz.js` جاهز | 🔴 **بلا محتوى** |
| بلاغات الأسئلة | `question_reports` | — | ⚫ بلا شاشة |
| الفصول | `classes` · `enrollments` | — | ⚫ بلا شاشة |

**الواجهة:** ١٥ وحدة JS حيّة (و`37_variant_ui.js` أثرٌ من حقبة SQL لا وحدة).
`api.js` وحدها تلمس Supabase · `media.js` وحدها تبني الروابط ·
`upload.js` وحدها ترفع · `simulations.js` وحدها تعرض المحاكيات.
وأربع قواعد كلُّها قابلة للفحص بـ`grep`.
طالب : subjects · cards · perf · feedback · chat
⚠️ والشاشات الداخلية (الدرس · الاختبار · المراجعة · التأليف) ليست في الشريط.

---

## ④ التالي

1. 🔴 **اكتب عشر بطاقات** في مادةٍ واحدة — ثمّ `flashcards.js`. الطرف
   الخلفيّ كامل من الإضافة إلى العَود، **والشاشة تُصمَّم على بطاقةٍ تُراجَع.**
2. 🔴 **وسِم دروس مادةٍ واحدة بالفروع** — ثمّ شريط التصفية في شاشة المواد.
3. 🔴 **املأ `objectives` لدرسٍ واحد.** الحلقة مبنيّة وتنتظر صفوفاً.
4. 🔴 **صيانة:** إعادة استخراج `00_schema` · حسمُ `77` · ختمُ `analytics`.
5. 🟡 **`sql/58`** — إحكام `EXECUTE`. ١١٧ دالّةً بلا طبقةٍ ثانية.
6. 🔴 **كتابة المحتوى** — الدروس الباقية في مقرَّر الصف الأول.
7. ❓ **افحص أثر المؤقّت على التشخيص** (استعلام ⑨) — قبل التوسّع.
8. صقل المحرّر بما تكشفه الكتابة.
9. استقطاب معلمين ← فتح `can_author` ← شاشة «مصادر إضافية» للطالب.
10. الصفحة التعريفية — **بعد** توفّر محتوى.

---

## ⑤ المنفَّذ — بالتفصيل

### ٥-١ المنهج والهوية ✅

- ٦ سلالم: أكاديمية (بكالوريا مصرية · ثانوية سعودية · جامعي · أخرى) وإتقانية
  (CEFR · IELTS). و`levels` تخصّ سلّماً واحداً ⇒ `sec1` المصري و`sa1` السعودي
  صفّان مختلفان وإن تشابه الاسم.
- التسجيل يحمل `scale/level` في **بيانات auth الوصفية** لا في `profiles`:
  بين التسجيل وتأكيد البريد لا جلسة، والبيانات الوصفية تعبر الفجوة.
- `set_my_grade` دالّة لا `UPDATE` — تتحقّق أن الصفّ ينتمي للسلّم.
- ⚠️ `level_from_score` (`sql/57`) **لا تُخمّن الصفّ من درجة** — قرارٌ صريح:
  الدرجة تقيس مهارةً، والصفُّ حقيقةٌ إدارية. خلطُهما يُنزل طالباً صفّاً.
- 🔴 **و`lessons` لا تحمل `course_id` إطلاقاً** — بل `subject_id` و`level_id`.
  و`list_lessons(p_subject)` هي التي تبني شاشة الطالب. *(مُكتشَفٌ ١٤ سبتمبر —
  وأثرُه في ⑩.)*

### ٥-٢ التأليف — المحرّر ✅

- بوّابتان: `can_curate` (هيكل المنهج · الرسميّ) · `can_author` (مصدرٌ باسمي).
- عقد المحرّر كاملاً: `save_lesson/unit/quiz/item/question/passage` ·
  `quiz_readiness` · `publish_quiz` · `publish_lesson` · `author_tree` ·
  `author_lessons` · `lesson_items` · `quiz_for_edit` · `duplicate_question` ·
  `delete_*` · `attach_passage` · `set_section` · `reorder_items`.
- النصّ المشترك والأقسام **حاويات تعلو مدى أسئلة** لا خصائص لكل سؤال.
- استيراد JSON (لصق · رفع · سحب وإفلات) + تصدير + معاينة تجربة الطالب.
- اتجاه النصّ وحرف الخيار يتبعان لغة المحتوى (`A B C` / `أ ب ج`).
- المحرّر مصمَّم **للحاسوب أولاً** — التأليف عمل مكتب. استثناءٌ واعٍ لا سهو.
- 🔑 المشتّت وتشخيصه **صفٌّ واحد** — لا درج ولا نافذة. ولو أُخفي الكود لصار
  واجباً إدارياً يُملأ آخر الوقت.
- 📌 **رقعةٌ مسّت `parseImport` أو `save_question` أو `dx_codes` ⇒ يُحدَّث
  `أكواد_التشخيص.md` أو `قالب_الاستيراد.md`** (أو كلاهما). بلا هذا السطر
  يبليان بصمتٍ ويصيران مرجعاً يُصدَّق وهو كاذب.

### ٥-٣ الاختبار والتشخيص ✅

- 🔒 لا يصل المتصفح مفتاح إجابة واحد. التصحيح كلّه في `submit_attempt`.
- `answers` تسجّل لكل إجابة: `dx_code` · `seconds` · `changes` · `confidence` · `meta`.
- **الحلقة:** أخفق بـ`KW` ← قرأ العلاج ← أعاد بأسئلة جديدة تقيس المهارة نفسها
  ← يُقاس هل زال النمط. لا «أنهى الدرس» بل **«زال الخلل»**.
- `submit_attempt` تُرجع `remedial` — **مبنيّ وينتظر `objectives`.**
- `59`–`68` كما كانت · ⛔ `69`–`71` ملغاة · `72`–`75` الإصدارات والحرّاس.
- `78` لغة تشخيص الرياضيات — ثلاثة أكواد، و`DTL` نُقلت إلى `general`.
  **٣٦ كوداً اليوم**، وعائلة الإنتاج خمسة (`DEV · PRO · TA1 · TPL · TSK`).
- `81` تنقيح المعجم لا توسيعه.
- `95` نصُّ الطالب يصله ولغةُ المعلّم تبقى محجوبة — **لغتان لا لغة.**

### ٥-٤ بنك الخانات ✅ (SQL 34 · 34b · 35 · 36 · 38 · 72)

```
34   variant_key · set_variant · clear_variant · variant_report
34b  duplicate_question تنقل section
35   get_quiz تنتقي نسخة من كل خانة بالبذرة
36   quiz_for_edit تُرجع variant_key
38   فحص التكافؤ: النصّ يجب أن يختلف بين النسخ + passage_map
72   الخانة تُحسب بالأحياء
```

**البذرة** = معرّف المستخدم + عدد محاولاته ⇒ الإعادة تُغيّر المسار، والمسار
نفسه ثابتٌ داخل المحاولة الواحدة.

**الوسم حكمٌ بشريّ والفحص رقابة عليه:** بصمة الفخاخ شرطٌ لازم لا كافٍ.

**حدود `set_variant`:** اختبارٌ واحد · نمطٌ واحد · سؤالان فأكثر · معرّفات
غير مكرَّرة · مفتاحان مختلفان ⇒ رفض · مفتاحٌ واحد ⇒ يُتبنّى · لا مفتاح ⇒
`'v' + أصغر معرّف`. ⇒ **الانضمام مدعوم والدمج مرفوض** — قيدٌ مقصود.

🔑 المفتاح **اسمٌ لا رابط**. 🔓 والقفل يخصّ المحتوى لا الخانة: سؤالٌ أُجيب
عنه **يتقاعد ويولد وريثه** (٥-١١).

**تصميم البنك — حجم الخانة قرارٌ في القياس:**
```
عدد الخانات = عدد المهارات المقيسة   ·   حجم الخانة = مقاومة الحفظ
١٤ بنداً في ٢ خانة ⇒ سؤالان   ← المعتمد
```
🔴 والتجميع تحكمه البصمة لا الترتيب. **ولا يظهر ذلك في أي شاشة.**

⚠️ **والإنذار يقول «صحّح الخيار لا الكود»**: أسهل طريق إلى ✅ هو إعادة وسم
مشتّت كذباً، وذلك يُفسد التشخيص من داخله.

### ٥-٥ نمط الاختيار المتعدّد (msq) ✅

```
SQL 39 البنية · 40 الحفظ · 41 التصحيح · 42 البوّابة · 43 · 44
```
- الدرجة «كلٌّ أو لا شيء» · المقارنة بالاحتواء المتبادل (`<@`) لا `=`
- ٢ ≤ الصحيحة ≤ الخيارات − ١ · والحدّ الأدنى ٣ خيارات
- **لا يُعلَن العدد للطالب** — إعلانه يعيد الحلّ الترجيحي
- تشخيص الإغفال **بالنمط لا بالوسم** — يُحسب في `quiz.js` من `judgments`

**متى يُستعمل:** حين يكون **حدّ المفهوم** هو المُشكِل. وسؤالٌ واحد في القسم كافٍ.

### ٥-٦ الإرشاد والرسائل ✅

- `choose_mentor` **بالمادة** · `p_teacher = null` = المتابعة الذاتية.
- `max_mentor_switches` · `max_teacher_families` · `set_subject_capacity`.
- `was_my_mentor` تُبقي حقّ الاطلاع على تاريخ من درّسته.
- ⚠️ `messages` و`attempts` لكلٍّ **مفتاحان إلى `profiles`** ⇒ كل تضمين يحتاج
  تلميحاً صريحاً. تغييرُ اسم مفتاحٍ في SQL يكسر الرسائل **بلا خطأ ظاهر**.

### ٥-٧ الوسائط والتخزين ✅

- جرّة `audio` · ١٠ م.ب · أربع سياسات تنادي `can_upload_media()`.
- 📐 **القاعدة تخزّن مفتاحاً لا عنواناً**، والعنوان يُشتقّ في `media.js`.
- ⚠️ الفحص في المتصفح **راحةٌ لا حراسة**؛ الحراسة في سياسة `storage`.

### ٥-٨ التتبّع والتقدّم ✅

- `track_item` تسجّل `opened` و`completed` مع الثواني.
- `lessons.requires_id` يفتح درساً بدرس.
- ⚫ `question_reports` بلا شاشة · `classes/enrollments` بلا شاشة.

### ٥-٩ الإدارة والصلاحيات ✅

- **المبدأ:** الصلاحية بالعمود · والسياسة بالصفّ · والمعنى بالدالّة.
- المواضع **الأربعة** الوحيدة التي تكتب فيها الواجهة مباشرة — وكانت خمسة
  حتى `106` نقلت علامة قراءة الملاحظة إلى دالّة. والباقي: إدراج رسالة ·
  علامتا قراءة الرسائل · حفظ درجة المقاليّ. **وأخراها تُنقل في ملفٍّ لاحق.**- `rls_auto_enable` يقفل RLS على كل جدولٍ جديد في `public` تلقائياً.
- `59` النشر شرطٌ يعلو الاتّصال بدرس — الفحص **قبل التفرّع**.

### ٥-١٠ نظام التصميم (b20 → b83)
**المسألة الحاكمة:** الشاشة جزءٌ من أداة القياس لا زينةٌ فوقها. نصٌّ يُقرأ
بصعوبة يُسقط كلمةً، وإسقاط الكلمة يُنتج كود `KW` كاذباً. **العرضُ يُفسد
التشخيص كما يُفسده المؤقّت.**

- ✅ **السلّم الطباعيّ** — ست درجات بأسماء وظيفية، وأرضية ١٢px.
  🔴 وصُحّح **انقلابٌ هرميّ**: صار الشرح ١٦٫٨ و`.rev-q` ١٥٫٢ — **الشرح أكبر
  مما يشرحه**، فالسؤالُ سياقٌ والتشخيصُ متن.
- ✅ **طبقة الأدوار** — ١٧٧ موضع لون صارت ٤١ رمزاً. **صفر لونٍ حرفيّ خارج
  كتلتَي السِمة.**
- ✅ **سِمتان** — والقراءة في `<head>` قبل أول رسم.
- ✅ **الترويسة الواحدة** — نزل ما فوق المحتوى من ٣٠٪ إلى ١٩٪.
- ✅ **فصل العرض** — قراءة ٦٨٠ · افتراضي ٧٦٠ · اختيار ١٠٦٠ · تأليف ١١٨٠.
  🔴 **والشبكة للمواد والمعلمين وحدهما**: ترتيب الدروس **محتوى**، وعرضُها
  شبكةً كذبةٌ تربوية يقولها التخطيط لا النصّ.
- ✅ **البوابة** عمودان بلا ألسنة · **زرّ الخروج** ٦٫٦:١.
- 🆕 **عرض الرياضيات (b56)** — `fmt()` في `ui.js` تُحوّل `\( \)` وتُفعّل
  `**غامق**` و`_مائل_`، و`mathBoot()` تُحمّل MathJax من `vendor/` عند أول
  حاجة. وتُستعمل في `quiz.js` · `analytics.js` · `editor_quiz.js` ·
  `render_q.js` · `teacher.js`.
  ⚠️ **وشرطة سفلية منفردة خارج الرياضيات تصير مائلاً فجأة** — الفحص:
  `select count(*) from questions where explanation ~ '_[^_\n]+_';`
- 🆕 **ختمُ الوحدة** — `MOD` في `analytics.js` يُقارَن بـ`BUILD` فيُنذر عند
  الاختلاف. ✅ واستويا عند `b64` في ١٨ سبتمبر.
  🔑 **والعيب كان في تعريف الختم لا في الرقم:** صُمّم ليكشف «ملفٌّ لم يصل»
  فكشف «ملفٌّ لم يتغيّر». وحُسم بالأرخص — يُرفع `MOD` مع كلّ `BUILD`
  (٠·ج ④). **وإن عاد الإنذارُ كاذباً مرّتين فيُولَّد من محتوى الملفّ.**  الاختلاف. 🔴 **ويُنذر كاذباً اليوم** (انظر ⑦).
- 🔓 **b22** — الاختيار تبديلُ فئة لا إعادةَ رسم: **وقتُ التمرير كان يُحسب
  في `a.sec`، فيُقاس التردّدُ حركةً لا فهماً.**
- 🆕 **الشعار متجهٌ حقيقيّ (b78)** — بعد قرار «الشعار والأزرق يبقيان
  بألوانهما، والكحليّ يُستعمَل في مواضع أخرى مرشَّحة» (حوارُ هذه الجلسة).
  ملفّان لا صورتان: `compact` عربيٌّ فقط للشريط (٢٥px — كان النصّ يعوّض
  رداءة الصورة النقطية هناك، وسبب الاستبدال أن المتجه يبقى حادّاً عند
  هذا المقاس)، و`full` ثنائيّ اللغة (بيان + BAYAN) للبوابة حيث الشعار
  بطل الشاشة. سِمةٌ واحدة لكلٍّ بمتغيّرين: `--logo-ink` يتبع سِمة
  الصفحة (كحليّ فاتحة / أبيض داكنة)، و`--logo-hole` شفافةٌ دوماً —
  تكشف عمّا خلفها بلا افتراضٍ مسبق.
  ⚠️ **وviewBox الأصليّ مربّعٌ فارغ حول الرسم لا حدودُه**: أُعيد حسابه
  بـ`getBBox()` من المتصفّح نفسه لكل ملفّ (لا تخمين) — إغفالُه كان يُظهر
  الشعارَ ذرّةً تائهة وسط مربّع فارغ عند التركيب الأول.
  🔴 **وثقوب الحروف كلّفت خطأً مرئياً (b78 → b79):** جُعلت `--logo-hole`
  شفافةً بحجّة «الثقب يكشف ما خلفه». وهي ليست نفياً بقاعدة even-odd في
  هذا الملفّ، بل **أشكالٌ تُرسم فوق الحرف بلون الورق** — فالشفافيةُ لا
  ترسم شيئاً، وظهرت B و A كتلتين مصمتتين. ⇒ **تُملأ بلون ما خلفها
  بالضبط** (`var(--page)` افتراضاً، والكحليّ داخل اللوحة).
  ⚠️ **وهذا يمنع التدرّج تحت الشعار**: «بالضبط» تستحيل على خلفيةٍ متدرّجة
  ⇒ اللوحة لونٌ مصمت. القيد تقنيٌّ لا ذوقيّ، فلا يُنقض بذوق.
  🔑 **والبوابة صارت نصفين ممتدّين:** الكحليّ يبلغ حافّة الشاشة طولاً
  وعرضاً — نصفٌ للهوية ونصفٌ للإجراء. والبطاقةُ المحتواة قبله بدت
  مقطوعةً: مستطيلٌ معلَّقٌ بلا سبب. الشعار داخلها أبيضٌ دوماً — لا يتبع
  سِمة الصفحة، لأن خلفيّته المحليّة كحليّةٌ ثابتة في الحالتين.
  🆕 **ومشهد الافتتاح سكن البوابة** — خطّ معاينة splash نفسه (الكلمة
  تُكتب من اليمين · كلُّ نقطةٍ مع حرفها · الأقواس تنفتح · القبعة تهبط ·
  BAYAN يصعد) منقولاً على الشعار الجديد، والتأخيرات محسوبةٌ من منحنى
  `sp-write` لا منقولة (ب ٠٫٢٥ · ي ٠٫٣٧ و٠٫٤٠ — طابقت أرقام المعاينة).
  ⚠️ **في مكانه لا طبقةً تحجب**: البوابة إجراءٌ متكرّرٌ لعائد، وطبقةٌ ملء
  الشاشة تؤخّره ثانيتين كلَّ جلسة. والنموذج صالحٌ من اللحظة الأولى.
  ⚠️ **ومرّةً واحدة في الجلسة**: `renderGate` تُعاد عند كل تبديلٍ بين
  دخولٍ وتسجيل، والمشهد المكرَّر عائقٌ لا ترحيب.
  ⚠️ **وبلا `is-playing` يظهر الشعار تامّاً**: فشلُ الـJS يُظهر شعاراً لا
  شاشةً فارغة — الحركة زينةٌ فوق حالةٍ صحيحة لا شرطٌ لها.
  والفيروزيّ/الذهبيّ هويةٌ ثابتة أيضاً — لا تتبع السِمة، مُعرَّفةٌ مرّةً في
  `base.css` فترثها الداكنة تلقائياً؛ ولا تحمل معنى «اخترتَ/أصبتَ» فلا
  تُقاس بمنطق تمايز طبقة الأدوار.
  🔴 **وثلاثةٌ صُحّحت بالنظر لا بالظنّ (b79 → b80):**
  ① **A الثانية في BAYAN كانت معطوبةً في ملفّ المصدر نفسه** — ثقبُها
     مرسومٌ كتلةً لا مثلّثاً، ومعه شظيّتان من التتبُّع (`#3C5979`). ⇒
     وُضعت نسخةُ A الأولى مزاحةً `translate(916.87 -.68)`، والحرفان
     متطابقان مقاساً (فرق ٠٫٠٨٪). وسقط `--brand-shadow`: لم يكن لوناً
     في الهوية بل بقيّةَ تتبُّع.
     🎓 **والدرس: الأصلُ المستورَد يُقاس لا يُصدَّق.** عُرض كلُّ حرفٍ
     مكبَّراً وحده على الكحليّ فظهر العطب؛ وفي حجم الشاشة كان يُقرأ
     «شيئاً ما ليس مضبوطاً» بلا تسمية.
  ② **شريطٌ شاحب تحت اللوحة ومعه تمريرٌ بلا محتوى وراءه** — والسبب
     `body{padding:0 0 90px}`: نافعةٌ في كل شاشةٍ تُمرَّر، ضارّةٌ في
     شاشةٍ ملؤها الشاشة. ⇒ تُصفَّر في `body.gate` وينتقل الحقُّ إلى
     `.gate-form` وحدها. **وكلُّ شاشةٍ جديدة بـ`100vh` تُسأل عمّا
     تضيفه `body` تحتها.** وقيس الأثر: ٩١٠ ⇐ ٨٢٠ عند ارتفاع ٨٢٠.
  ③ **المحتوى كبر بالنسبة للفراغ** — العمود نصفُ الشاشة، وبطاقةٌ بـ٤١٠
     فيه قصاصةٌ في قاعة ⇒ ٤٨٠ وحشوٌ أوسع وشعارٌ أكبر.
  🔴 **باقٍ:** حذف `img/logo-light.png` و`img/logo-dark.png` يدويّاً —
  راجع ٠·د.
- 🆕 **كُتم dx إلى إشباع الهوية (b82)** — بدأ السؤال ذوقاً: «البرقوقيّ
  يشذّ». وأُعيد المسح ثلاثيَّ الأبعاد فأثبت أن **الصبغة مقفلة**: صفر
  مرشّحٍ نجا تحت ٣٢٠°، وكلُّ لونٍ في الهوية محجوزٌ لحكمٍ على إجابة
  (الكحليّ على بُعد ١٠٫٩° من accent · الفيروزيّ ٢٢٫٦° من ok · الذهبيّ
  ٣٢٫٣° من warn) — فلا بديل عن ٣٢٠°.
  🔑 **لكنّ الغربة كانت في الإشباع لا الصبغة**: الهوية مكتومة عند
  ٠٫٠٨٦–٠٫١٠٣، و dx ٠٫٢٠٠ — أعلى إشباعٍ على الشاشة كلّها. ⇒ ٠٫٠٩٨،
  ومعه متنُ التشخيص كحليُّ الهوية والعنوانُ وحده برقوقيّ. الأرقام
  كاملةً في رأس `base.css` (طبقة الأدوار).
  **والمكسب في كل اتجاه لا مقايضة: تمايزٌ ٩٫٦ ← ١٩٫١ وتباينٌ ٧٫٠ ← ٩٫٦.**
  🎓 **ودرسٌ أوسع من dx: الإشباع هويةٌ كما الصبغة** — لونٌ في الصبغة
  الصحيحة وبضعف إشباع إخوته يبدو دخيلاً ولو كان قياسه سليماً.
- 🆕 **وحُرِّر الأخضر من غير الحكم (b83)** — `.unlocked` في بطل النتيجة كان
  `--ok-soft`، وما فيه ليس حكماً على إجابة: «أتممتَ الدرس» إنجازٌ على
  مستوى الدرس، و«فُتح لك الآن» **حالةٌ جديدة لا صواب**. ⇒ محايدٌ مرفوع
  (`--fill-soft` + `--line`)، والمعنى باقٍ في 🎉 و🔓 والكلمات.
  🔑 **والقاعدة تُعمَّم: كلُّ أخضرَ خارج الحكم يُضعف الأخضرَ حين يُحكَم به.**
  فالبطل كان يحمل ثلاث إشارات لونية وحكمُه واحد (الدرجة).
  🟡 **وفجوةٌ تُرى ولم تُمسّ:** صنفٌ واحد (`.unlocked`) لمعنيين. ولو صار
  «فُتح لك الآن» رابطاً إلى الدرس التالي لاستحقّ لونَ التفاعل عن جدارة،
  ولقصّر الطريق بين الإنجاز والخطوة التالية — تغييرُ سلوكٍ لا لون.

### ٥-١١ إصدارات الأسئلة ✅ (SQL 68 · 72 · 73 · 74 · 75)

> **ما رآه الطالب يُحفظ. أما السؤال فيتطوّر.**

🔴 **والقاعدة القديمة كانت تحمي الكذب كما تحمي الصدق:** مشتّتٌ وُسم `KW`
وهو `VOC` كان يُحرَس بالقوّة نفسها، فيُرسَل الطالب إلى علاجٍ لا يخصّه.

```
68  retired_at · supersedes_id · questions_live_position
74  retire_question: التقاعد ثم ميلاد الوريث في موضعه وخانته ونصّه
72·75  ٢٦ حارس `and retired_at is null` في ٦ دوالّ
73  passage_history: النصّ يُعدَّل في مكانه، ويُحفظ ما كان
```

**الترتيب حاسم:** التقاعد **قبل** ميلاد الوريث — الفهرس فريدٌ على الأحياء.

**موضعان بلا حارس عمداً:** `submit_attempt` (مراجعتها يجب أن ترى المتقاعد)
و`quiz_for_edit · used`.

🔑 **والنصّ المشترك عومل بغير ما عومل به السؤال — بقرار:** يُعدَّل في مكانه
ويُحفَظ ما كان. **ثقةٌ موثَّقة، لا صرامةٌ تُعيق ولا ثقةٌ تُخفي.**

### ٥-١٢ الفروع (strands) 🔶 (SQL 79 · 80 · 97)

**ثوابتُه — تُقرأ ولا تُستنتج:**

```
① يُوسَم أصغرُ ما لا ينقسم، وما لم يُوسم يرث:
      فرعُ السؤال = item.strand_id ?? lesson.strand_id
   فالعربية تُوسم على الدرس، والإنجليزية على المكوّن — بنيةٌ واحدة
   وسلوكان. البنية لا تُصلح فهرسةً بإفساد تدريس.
② الشجرة طبقتان لا أكثر. و«البحتة» حاويةٌ محسوبة لا موضعٌ يُكتب.
③ الفرع يتبع **المادة** لا المقرَّر — وإلّا استحال أن يُقال
   «ضعفُ هذا الطالب في الجبر ممتدٌّ منذ سنتين».
④ 🔒 الفلتر يحجب البطاقة ولا يدخلها. `get_quiz` لا تعرف الفرع ولا ينبغي:
   تصفيةُ ثلثِ ورقةٍ تُنتج نسبةً من ماذا؟ و`pass_mark` لم يُعايَر على ثلث.
⑤ شريط التصفية يُبنى ممّا ظهر في دروس الطالب لا من جدول الفروع.
```

**الحال:** القاعدة كاملة · شاشة «الأداء» تُصفّي بالمادة ثم الفرع ✅ ·
شاشة «المواد» **بلا شريط** — و`list_lessons` ترسل `strand` منذ `80`.
⇒ **البيانات واصلة والشريط غير مرسوم، وينتظر محتوًى موسوماً.**

### ٥-١٣ التحليلات ✅ (SQL 82 – 96)

خمسة عشر ملفّاً بنَت شاشةَ أداءٍ للطالب وأخرى للجماعة. وأثقلُ ما فيها
قراراتٌ لا تُقرأ من الشيفرة:

```
82  المحور يُثبَّت: صفّ الطالب **لحظة المحاولة** لا صفُّه اليوم
83  جوابٌ واحد لسؤال «أيُّ درسٍ هذا؟» — ولا يُشتقّ في موضعين
84  العمود الفقريّ: سلسلةُ وصلٍ تُكتب مرّةً وتُقرأ في كل تقرير
88  الاختبار يُسمّى لا يُرقَّم — «اختبار ٣» لا يقول شيئاً لطالب
90  الإتقانُ جودةٌ والإنجازُ تغطية — رقمان لا رقم
93  نقرةٌ بلا قراءة ليست خطأً في الفهم ⇒ تُستبعد من التشخيص
94  المقامُ حقيقةٌ في المنهج لا في عين الناظر
95  نصُّ الطالب يصله، ولغةُ المعلّم تبقى محجوبة
96  نعرض الجبهة لا الباقي كلَّه — قائمةٌ لا تُثبّط
```

🔑 **و`93` أعمقُها أثراً في التشخيص:** إجابةٌ في ثوانٍ أقلَّ من زمن قراءة
السؤال **ليست خطأً في الفهم**، فعدُّها كذلك يُنتج كوداً كاذباً. وهذا يجيب
جزئياً سؤال المؤقّت المفتوح في ⑨.

### ٥-١٤ البطاقات ✅ قاعدةً (SQL 98 · 99 · 100)

```
98   decks · cards · card_key · عقد التأليف · وتقاعد review_queue
99   fsrs_params (٢١ وزناً في صفٍّ يُحدَّث) · fsrs_next النقيّة
100  card_reviews · card_review_log · review_card · due_cards
     due_counts · add_my_card · questions.card_id · حارس الحذف
```

**القرارات التي لا تُقرأ من الشيفرة — وكلُّها في ⑥-⑦ أدناه.**

**والفحص:** ٧ بنودٍ للبنية · **١٠ للمحرّك** · ٧ للذاكرة · ثمّ تجربةٌ حيّة
ببطاقةٍ وثلاث مراجعات. وأثمنُ فحوص المحرّك `R = 0.900` عند `t = S` —
تعريفُ الاستقرار في كلّ نسخ FSRS، فيصلح شاهداً بعد المعايرة أيضاً.

**التطبيق العمليّ لصيغ FSRS-6 — منقولةٌ من ويكي `awesome-fsrs` في
١٤ سبتمبر، وتؤكّدها حزمة `fsrs` على PyPI:**
```
S₀(G) = w_{G−1}                         D₀(G) = w₄ − e^{w₅(G−1)} + 1
R(t,S) = (1 + factor·t/S)^{−w₂₀}        factor = 0.9^{−1/w₂₀} − 1
I(r,S) = (S/factor)·(r^{−1/w₂₀} − 1)
اليوم نفسه:  S′ = S·e^{w₁₇(G−3+w₁₈)}·S^{−w₁₉}   و S′ ≥ S إن G ≥ 3
```
🔑 **وترتيب الخطوتين حسمه رمزُ المرجع لا شرحُه:** الصيغة مكتوبة
`S′_r(D,S,R,G)` بـ`D` غير مشتقّة ⇒ الاستقرار يُحسب بالصعوبة **قبل** تحديثها.

### ٥-١٥ المحاكيات ✅ (`simulations.js`)

الوحدة الوحيدة المسؤولة عن عرض عنصرٍ بنمط «محاكاة». وثلاث حاجاتٍ تعليمية
تُملي تصميمها:

```
① تُضمَّن في مكانها لا في تبويب — السياق جزءٌ من الفهم
② عزلٌ كامل: sandbox="allow-scripts" بلا allow-same-origin ⇒ أصلٌ معزول،
   لا وصول لجلسة الطالب ولا localStorage حتى لو كان الملف على نطاق بيان
③ الإنجاز لا يُسجَّل عند الفتح — «فتحَ» ليست «تعلّم».
   بل عند رسالةٍ صريحة:  window.parent.postMessage({bayanSim:'done'}, '*')
```

⇒ **هذا هو العقد الوحيد المطلوب من أيّ محاكٍ يُبنى لاحقاً.** وبلا ذلك
السطر يبقى العنصر «▶ مفتوح» ولا يصير «✅ منجَز» أبداً.

---

## ⑥ الثوابت — ما لا يُكسر

1. **طبقةٌ واحدة تلمس Supabase.** `grep -rn "\bdb\." js/ --exclude=api.js` ⇒ لا مخرجات.
2. **موضعٌ واحد يبني روابط الوسائط.** الفحص في رأس `media.js`.
3. **نقطة اختناقٍ واحدة للرفع.** `grep -rn "\.upload(" js/ --exclude=api.js`.
4. **وحدةٌ واحدة تعرض المحاكيات** — `simulations.js`، وعقدُها `postMessage`.
5. **صفر لونٍ حرفيّ خارج كتلتَي السِمة.**
6. **لا يصل المتصفح مفتاح إجابة واحد.**
7. **لا كتابة إلا بدالّة** — إلا المواضع الأربعة المعلنة في ⑤-٩.
8. 8. **`dx_code` للمشتّت وحده** — ولا سؤال بمشتّت بلا كود.
9. **ما يُشتقّ لا يسكن في موضعين** — نسختان لشيءٍ واحد تختلفان يوماً.
10. **الدوالّ المصدَّرة `function` لا `const`** — الاستيراد الدائري يعتمد على الرفع.
11. **`00_schema.sql` مولَّد** — لا يُحرَّر بيد، ولا يُعاد ترتيب أقسامه.
12. 🆕 **سجلُّ مراجعة البطاقات خاصٌّ بلا استثناء.** لا معلّم ولا مدير ولا
    مرشد. والسببُ يفوق الأدب: لو رآه المعلّم لضغط الطالبُ «بسهولة» ليبدو
    حسناً، **فيفسد التقدير الذاتي وهو إشارة النظام الوحيدة.** والمجموع
    مباح («١٢ من ١٥ يتعثّرون في الاستعارة») بعتبةِ ثلاثةِ طلابٍ فأكثر.
13. 🆕 **أوزان FSRS بياناتٌ لا شيفرة** — صفٌّ في `fsrs_params` يُحدَّث بمُحسِّنٍ
    يقرأ `card_review_log`. ولهذا يُختم بذرُها بـ`do nothing` لا `do update`
    **خلافاً لسائر الملفّات**: إعادةُ التشغيل يجب ألّا تمحو معايرة.
    ⇒ **والقاعدة تنقلب حين ينقلب سببُها.**

---

## ⑥-ب قراراتٌ لا تُقرأ من الشيفرة — البطاقات (١٤ سبتمبر)

- **الجلسة مادّةٌ واحدة ولا تُخلط المواد.** نفعُ التشابك يأتي من **التمييز**،
  فيشترط بنوداً قابلةً للالتباس (تشبيه · استعارة · كناية). أمّا كلمةٌ
  إنجليزية بعد قاعدةٍ نحوية فلا تمييزَ يُكتسب — كلفةُ تحويلٍ وانكسارُ سياق.
  ⇒ يُخلط **داخل** المادة، ويُفصل بينها.
  *(تصحيحٌ لموقفٍ سابق كان يوصي بالخلط الكامل.)*

- **`decks` تحمل `subject_id` + `level_id` لا `course_id`** — لأن `lessons`
  لا تحمل `course_id` و`list_lessons` تأخذ `p_subject`، ولأن `courses.path_id`
  يجعل الدرسَ الواحد ينتمي لعدّة مقرَّرات. وعيبُ `⑩` هو **المادة وحدها**،
  و`level_id` بجوارها يرفعه.

- **النظامان يلتقيان في القراءة لا في الكتابة.** لا سطرَ في `submit_attempt`؛
  و`due_cards` وحدها تضمّ «بطاقةَ سؤالٍ أخطأ فيه» إلى المستحقّ.
  ⇒ **قاعدةٌ عامّة: حين تريد أن يعرف نظامٌ بحدثٍ في آخر، اسأل عند القراءة
  قبل أن تكتب عند الحدث.** القراءة تُعاد ويُصحَّح سؤالها؛ والكتابة تقع
  مرّةً ويبقى أثرُها.

- **ثلاثة أزرار لا أربعة** (لم أتذكّرها · بصعوبة · بسهولة). فـ«سهل» تدعو
  إلى تضخيمٍ لا يُكتشف. وأثرُه معلوم: `w16` لا تُطلق ⇒ الجدولة أكثر تحفّظاً
  بقليل. والعمود يحتمل ٤ بلا هجرة.
  🔑 و«بصعوبة» **أثمن إشارةٍ في النظام**: مصطلحٌ استُحضر بعناءٍ هو ما يقع
  على حافّة النسيان.

- **البطاقة الجديدة والمُخفَق فيها تعودان في اليوم نفسه** — التعرّض الأول
  ترميزٌ لا استرجاع، وتركُ المُخفَق فيها إلى الغد أربعٌ وعشرون ساعةً من لا
  شيء. **لكن لا فوراً**: خطوةُ عشر دقائق، فالعَود بعد ثانيتين صدى ذاكرةٍ
  عاملة يبدو نجاحاً ولا يُرسّخ.

- **سقفُ الجديد للجلسة لا لليوم** — فحدُّ اليوم يقتضي حساب تاريخٍ
  (و`current_date` بتوقيت UTC) ويمنع راغباً عاد مساءً. و«منعُ الراغب»
  مرفوضٌ في هذا المشروع.

- **ثلاثة انحرافاتٍ عن إعداد FSRS الافتراضيّ — إعدادٌ لا تغييرٌ في النموذج:**
  خطواتُ التعلّم ١٠ دقائق (لا دقيقة) · `max_interval` ٦٠ يوماً (لا ٣٦٥٠٠،
  وأفقُ الاستبقاء سنةٌ دراسية وقاعدة Cepeda تعطي ٢٧–٥٤) · ولا تشويش
  (`fuzz`) — فتُختبر الدالّة بسطر.

- 🔴 **ورقمٌ اختُرع وسقط:** بُني جزءٌ من التصميم على «الطالب يفتح المنصّة
  مرّتين أسبوعياً»، **ولم يُقَس قطّ**. والقياس متاح ولم يُشغَّل بعد:
```sql
with d as (
  select user_id, submitted_at::date as يوم from attempts
  union select user_id, opened_at::date     from item_progress
  union select user_id, completed_at::date  from item_progress where completed_at is not null
)
select p.full_name, count(distinct d.يوم) as أيام_نشطة,
       round(7.0*count(distinct d.يوم)/nullif((max(d.يوم)-min(d.يوم))+1,0),1) as أيام_في_الأسبوع
  from d join profiles p on p.id = d.user_id
 where p.role='student' group by 1 order by 3 desc nulls last;
```
  ⚠️ **وهو حدٌّ أدنى لا حقيقة**: يعدّ الأيام التي تركت أثراً مكتوباً.
  🔑 **ويختلف الطلاب فردياً — وهذه حجّةٌ لـFSRS لا عليه:** لا يسأل عن
  التردّد بل عن `elapsed` الفعليّ، فيُعطي الغائبَ أسبوعاً جدولةً صحيحةً
  لغيابه. وسلّمُ الصناديق الثابت كان سيعجز.

---

## ⑦ أعطال معروفة

- 🔴 **`sql/58` لم يُطبَّق** — *مقيسٌ ٥ سبتمبر*: `has_function_privilege`
  تُرجع `true` لـ`public` على دوالّ `public` كلِّها ⇒ كلُّ دالّة قابلةٌ
  للنداء من أيّ زائر عبر PostgREST — بما فيها `admin_decide`. والحراسة
  اليوم **داخل الدوالّ وحدها**: طبقةٌ واحدة حيث ينبغي طبقتان.
  ⚠️ **و١١٧ دالّةً اليوم لا ٩٠** — والكلفة تزداد بالتأجيل.
  📌 ولهذا يكتب كلُّ ملفٍّ جديد `grant execute` صراحةً: ما يحمل منحاً ينجو
  يوم يُطبَّق `58`، وما لا يحمله يسقط. *(و`98`–`100` تحمل منحَها كلَّه،
  ومنحَ الجداول `select` وحده.)*

- 🔴 **`77` ثقبٌ في `sql_log` بلا صفّ إطلاقاً** — أُعلن في `يطبق_كل_محادثة §④`
  يوم ٧ سبتمبر ومضى أسبوع. والاستعلام سيقول غداً «`77` حرّ» لمن يسأله.
  يُحسم بسطر:
```sql
insert into public.sql_log (n, title, note) values
  ('77', '⛔ محجوز ولا ملفّ له',
   'سياسات التخزين مسجَّلةٌ تحت رقمها الصادق 48. يُستخرَج نصُّها ملفّاً 48، أو يبقى هذا الصفّ شاهداً أنّ 77 ليس حرّاً.')
on conflict (n) do nothing;
```

- 🟠 **`STATE.md` في المستودع متأخّرة** — *مقيسٌ ١٨ سبتمبر*: نسخةُ Git عند
  ٥ سبتمبر والحيّة عند ١٤. **وعطلُ وثيقةٍ أخطرُ من عطل شيفرة**: الشيفرة
  تفشل ظاهراً، والوثيقة تُصدَّق. الحارس في ٠·ج ⑥.
  و`BUILD = "b59"` ⇒ الشاشة تعرض «⚠ التحليلات b55» وهو **إنذارٌ كاذب**:
  الملفّ وصل، لكنه لم يتغيّر منذ `b55`.
  🔑 **والعيب في تعريف الختم لا في الرقم:** صُمّم ليكشف «ملفٌّ لم يصل»
  فكشف «ملفٌّ لم يتغيّر». ⇒ **ويُحسم بأحد اثنين ولا يُترك** — إمّا يُرفع
  `MOD` مع كل `BUILD` (ويصير سطراً في ٠·ج ④، وقد أُضيف)، وإمّا يُقارَن
  بختمٍ يُولَّد من محتوى الملفّ لا بيد. **وإنذارٌ كاذب يُتجاهل هو أسوأ
  من لا إنذار.**

- 🟠 **اختبارٌ منشورٌ فقد جاهزيته لا يُلغى نشرُه** — *رُئي ٢٤ سبتمبر،
  وهو أقدمُ من الشاشة الجديدة*: زرُّ `rdPub` يُعطَّل بـ`readiness.ok`
  وحدَه، فلا يفرّق بين «انشر» و«ألغِ النشر». فإن سقطت الجاهزية بعد
  النشر — حُذف سؤال، أو نُزع كود تشخيص — **بقي الاختبار المعطوب أمام
  الطلاب ولا سبيل إلى سحبه من الشاشة.** ولم يُغيَّر اليوم لأنه سلوكُ
  نشرٍ لا تصميم، وتغييرُه قرارٌ لا رقعة. والشاشة تقوله الآن صراحةً في
  `title` الزرّ بدل أن تصمت.
  ⇒ **والعلاج المقترح سطرٌ واحد:** يُعطَّل حين `!pub && !r.ok` لا غير —
  فسحبُ ما لا يصلح للطلاب لا يُشترط له أن يصلح.

- ⚠️ **السياسات نقصت والجداول زادت** — كانت `٤٩` في ٥ سبتمبر و`٤٧` في
  ١٤ سبتمبر بينما زادت الجداول من ٣٤ إلى ٣٦. يُحتمل جدولان بـRLS مفعّلة
  وبلا سياسة (اتجاهٌ آمن لكنه صامت). الفحص:
```sql
select t.tablename as جدولٌ_بلا_سياسة
  from pg_tables t where t.schemaname = 'public'
   and not exists (select 1 from pg_policies p
                    where p.schemaname='public' and p.tablename=t.tablename);
```

- ⛔ **`review_queue` متقاعدة** — *مقيسٌ ١٤ سبتمبر*: ٢٢٠ صفّاً · ٤ طلاب ·
  **١٩٤ مستحقّة** · أقدمُ استحقاقٍ ١٥ أغسطس · ولم يُعرض منها صفٌّ واحد قطّ.
  🔴 **ولماذا لا تُحيا — والحجّة تمسّ ميزة المنصّة:** تجدول **السؤال بعينه**
  لا المهارة. فبعد أربع عَوداتٍ يتذكّر الطالب موضعَ الخيار لا المفهوم، فلا
  يقع في الفخّ ⇒ **يبدو أنّ الخلل زال وهو قائم**، فلا يُرسَل إلى علاجٍ
  يحتاجه. **وكذبُ التشخيص سلباً أضرُّ من كذبه إيجاباً.**
  وبنكُ الخانات يفعل ما أُريد منها وأصحّ: الإعادة تسحب نسخةً أخرى من الخانة
  نفسها، فيُقاس زوالُ الخلل لا تذكّرُ البند.
  ⇒ التقاعد معلَنٌ في `comment on table`. **وكتابتُها تُرفع — ومعها سطرُها
  في `retire_question` — مع أوّل ملفٍّ يفتح `submit_attempt` لسببٍ آخر.**
  ⚠️ ولا تُفتح `submit_attempt` لأجل حذف سطر: أخطرُ دالّةٍ في المنصّة ولها
  أربع نسخ (`05` · `41` · `44` · `65`).

- 🕳️ **`list_quizzes` بلا حارسَي تقاعد** — ⚫ ميتة: صفرُ نداءٍ في الواجهة
  وفي القاعدة. **لم تُرقَّع عمداً**: رقعةُ دالّةٍ ميتة تزيد التاريخ ولا
  تزيد الصدق. من يُحييها يضيف `and x.retired_at is null`.

- 🕳️ **`question_count` يعدّ المتقاعدين** في `save_question` و
  `duplicate_question`. لا يُقرأ في أيّ موضع ⇒ لا ضرر اليوم.



- 🟡 **`teaches()` غير مقيّدة بالمادة** — معلّم العربية يرى محاولات الطالب
  في الإنجليزية. يظهر أثره عند المعلم الثاني.

- 🟢 `favicon.svg` مفقود · خمسة أنماط أسئلة يسمح بها المخطط ولا تعرضها
  الواجهة (يكشفها `quiz_readiness` وتمنع النشر).

- `publish_lesson` تحرس عند العتبة ولا تبقى · `has_quiz` و`lesson_done` لا
  تفحصان نشر الاختبار · درسٌ منشورٌ بلا محتوًى عامل يُبلَّغ لمؤلّفه — مؤجَّل.

---

## ⑧ دَينٌ موثَّق — يُعرَف ولا يُصلَح الآن

> الفرق بين «تُرك عمداً» و«سُهي عنه» لا يُقرأ من الشيفرة، بل من سطرٍ يقوله.

- ⚠️ **`review_card` تُرجع `retrievability: 1` لبطاقةٍ جديدة** — و`coalesce`
  هي السبب، والسجلّ يحمل `null` صحيحاً.
  . ⇒ **عرضٌ لا بيان**: المعايرة سليمة،
  لكنّ الواجهة لو عرضته لقالت «احتمالية تذكّرك ١٠٠٪» لأوّل لقاء. يُحذف
  `coalesce` مع أوّل ملفٍّ يمسّ الدالّة — **ولا يُفتح لأجله ملفّ.**
- ⬜ **تقرير المعلّم عن البطاقات** لم يُبنَ عمداً — ينتظر شاشةً تعرضه.
  **ولا تُبنى دالّةٌ بلا قارئ** (سابقة `list_quizzes`).
- ⚠️ **`is_teacher()` داخل `can_access` تسأل `auth.uid()` لا `p_user`.**
  لا أثر اليوم (كل المستدعيات بمعاملٍ واحد) ⇒ يُصلَح حين يُستدعى بمعاملين.
  📌 **ولم يُكرَّر الفخّ في `can_see_deck`**: لا تقبل مَن تُسأل عنه أصلاً.
- ⚠️ **`answers.dx_code` عمودٌ واحد لسؤالٍ قد يحمل فخّين** (msq) — والمتّجه
  كاملاً في `meta->'dx'`. يُصلَح حين تُقرأ `meta` في التحليلات.
- ⚠️ `list_quizzes.mcq_count/essay_count` تعدّان المؤلَّف لا المعروض.
- ⚠️ شرطُ جمع الإجابة (`plays` · التوقيت) لا يُحفظ في `answers.meta`
  ⇒ يُضاف قبل أن تُقرأ الأكواد.
- ⚠️ `submit_attempt` ترتّب شاشة المراجعة بـ`q.position` — **تجميليّ**.
- ⚫ `attempts.served_questions` بلا كاتب · **`import_bank` بابٌ ميّت**
  (الاستيراد كلّه في المتصفح) — تُحذف أو تُوثَّق كواجهة SQL.
- ⚫ **دوالّ بلا نداء من الواجهة**: `my_rank` · `best_pct` · `level_from_score` ·
  `my_students` · `my_courses` · `list_quizzes` · `variant_report` ·
  `has_opened` · `lesson_done` · `admin_pending_count`. **تُراجَع مرّة واحدة
  ويُكتب لكلٍّ سببُ بقائه.**
- ⚠️ **الشعار في ملفّين** — يُصلَح حين يُرسَم متجهاً حقيقياً.
- ⚠️ **ثمانية مواضع تستعمل `opacity` على نصّ** — الشفافية تُنقص التباين،
  وأثرها على الورق أوضح. تُصلَح بلونٍ صريح.
- ⚠️ **`.psg` محدودٌ بـ`44vh`** — والحلّ الناضج عمودان، وينتظر طالباً يقول
  إن التمرير أزعجه.
- ⚠️ **جرّة `audio` عامة** ⇒ يُعاد النظر يوم يُنتَج أوّل درس استماع:
  التعرّض المسبق يقيس الحفظ لا الفهم.
- ⏸️ `serve_count` مؤجَّل — وإن أُضيف **فبالقسم لا بالاختبار**.
- ⬜ **صيانة دورية — أكواد معلَّقة في `dx_map`:** سؤالٌ بكودٍ غير مسجَّل
  يُحفظ ويُنشر، ثم يقع طالبٌ في ذلك الخيار فيرفض المفتاح الأجنبي كتابة
  الإجابة ⇒ **تسقط المحاولة كلها**. الفحص:
```sql
select x.quiz_id, x.id, e.value as كود_مجهول
  from question_keys k join questions x on x.id = k.question_id,
       jsonb_each_text(k.dx_map) e
 where not exists (select 1 from dx_codes d where d.code = e.value);
```
- ⬜ لا منفذ لتعديل `difficulty` في بطاقة التحرير — `saveQ` ترسله ولا حقل له.
- ⬜ `questions.position` صفةُ استعمالٍ تسكن في صفةِ ملكية.
- ⬜ التحليلات تُبنى على `variant_key` الحالي، فنقل سؤالٍ بين الخانات يُعيد
  تصنيف إجاباته السابقة أثراً رجعياً.

- 🆕 **النصف الثاني من «لا كتابة إلا بدالّة»:** `106` نقلت علامة قراءة
  الملاحظة، وبقيت `save_grade`. وبعدها يُسحب `UPDATE` عن `attempts` كلّياً.
  **وتُرك عمداً**: يمسّ مسار تصحيح المعلّم فيستحقّ ملفَّه وفحصَه.
  🔒 ولا ثغرة بينهما — الطالب محجوبٌ بـRLS أصلاً.
- 🆕 **عدّادُ المدير يشمل عملَ غيره:** `teaches()` تُرجع `true` للمدير عن كلّ
  طالب، فـ`grading` في `my_counts` تعدّ ما يخصّ معلّمين آخرين. لا أثر اليوم
  (المدير أوسع المعلّمين)، **ويظهر يوم ينضمّ ثانٍ**. ⇒ يُقصر حينها على
  الإرشاد الصريح. **ولا تُبنى بنيةٌ لحلّ ازدحامٍ لم يقع.**
- 🆕 **المقاليُّ الفارغ يُعَدّ «بانتظار التصحيح»** — والطالب لم يكتب حرفاً.
  رأيٌ مبدئيّ: يُعَدّ، لأن درجته تبقى معلّقة حتى يُغلقها معلّمه ولو بصفرٍ
  وسطر. **ويُحسم على بياناتٍ حقيقية بعد تصفير الركام، لا على ركام.**
---

## ⑨ قرارات مفتوحة

- ❓ **ما الذي يستحقّ جرساً؟** القاعدة المعتمدة في `106`: **الحدثُ يُجرَّس
  والحالُ لا.** فملاحظةُ معلّمٍ حدثٌ يقع مرّةً ويُقرأ فينتهي؛ وبطاقةٌ
  مستحقّة حالٌ تتجدّد كلَّ يوم. **وجرسٌ على الحال يصير ضجيجاً يُعلّم
  الصَّمَم.** والسؤال المفتوح: أين يقع «درسٌ جديد نُشر في مادتك»؟
- ❓ **هل تُصفَّر محاولات التجربة أم تُوسَم؟** التصفير يُنقّي العدّادات
  ويمحو تاريخاً، والوسم يُبقيه ويُعقّد كلّ استعلام. **ولا يُحسم قبل أوّل
  دفعة طلابٍ حقيقيين** — فحينها يُعرف ما يُقاس.
- ❓ **هل يفسد المؤقّت التشخيص؟** ١٨ سؤالاً في ٢٥ دقيقة يُبقي للاختيار أقلّ
  من دقيقة. وكودٌ مثل `SU` يفترض أن الاختيار السطحي أثرُ فهمٍ سطحي؛ لكن تحت
  الضغط **يقرأ الجميع سطحياً**، فيقيس الكود السرعة لا المفهوم.
```sql
select a.dx_code, count(*) as مرات, round(avg(a.seconds)) as متوسط_الثواني
  from answers a where a.dx_code is not null
 group by 1 order by 2 desc limit 12;
```
  ✅ حُسم جزئياً مرّتين: بـ`plays` (خاصّةٌ للاختبار لا مبدأً للمنصّة)،
  وبـ`93` (نقرةٌ بلا قراءة تُستبعد من التشخيص). **ويبقى السؤال الأصليّ قائماً
  للأسئلة التي تُقرأ فعلاً تحت ضغط.**

- ❓ **«أستقبل طلاباً» — مفتاحٌ قائمٌ لم يُفتح (٢٥ سبتمبر · `b88`).**
  `teacher_subjects.accepting` موجودٌ ومحترَمٌ فعلاً في `list_mentors`:
  `full = cnt >= capacity or not accepting`. **لكنّ الطالب يقرأ في الحالتين
  «اكتمل النصاب»** — وهو كذبٌ على معلّمٍ أغلق بابه وعنده عشرون مقعداً.
  ولم يُعرَض المفتاحُ في شاشة «موادّي» مع أنّ الحقل والدالّة يسعانه، **لأنّ
  فتحه يجعل الكذبة واقعةً**: كانت قبله تحتاج تعديلاً مباشراً في القاعدة.
  ⇒ **يُفتح بعد أن يُفرَّق النصّان** — «اكتمل النصاب» و«لا يستقبل الآن» —
  وهي كلمةٌ في `list_mentors` وأخرى في `student.js`.

- ❓ **ما طولُ السطر الصحيح للعربية؟** قاعدة «٤٥–٧٥ حرفاً» **مُعايَرة على
  اللاتينية**. والعربية أكثف. **الحكم لطالبٍ يقرأ لا لجدولٍ منقول.**

- ❓ **تجميع التحليلات: بالخانة أم بالسؤال؟** «٧ طلاب أخفقوا في `KW`» لا يصحّ
  إن توزّعوا على ثلاث نسخ.

- ❓ **متى تُعايَر أوزان FSRS؟** الزناد: **ألف مراجعة في `card_review_log`**.
  وعندها تُقارَن `FSRS-6` بـ`FSRS-7` على بياناتك — والسجلّ يحمل التوقيت
  بالثواني فالمقارنة ممكنة بلا فقدان صفّ.

- **السؤال الصوتي** — التخزين جاهز، والباقي سياسة خصوصية لأصوات قُصَّر.
- **فتح `can_author` للمعلمين** — سطر واحد في الدالة، ينتظر معلمين.
- **الشعب داخل المسار** — البنية تحتملها بـ`courses.path_id`، ولم تُطلب بعد.
- **دمج خانتين** — ترفضه `set_variant` صراحةً. فتحه يقتضي قراراً.
- ⏳ **Form B · C** — قد يُحلّ بـ`variant_key` القائم. لا تُبنى بنيتُه قبل أن يُكتب.
- ⏳ **المحادثة** — تسجيلٌ + مقياسٌ رباعيّ + خصوصية أصوات القُصَّر.

### قرارٌ مؤجَّل — الدرجة الجزئية في msq

مرفوضة الآن، ويُكتب السبب لئلا يُعاد فتحها بلا حجّة:

① «كلٌّ أو لا شيء» تُبقي عتبة ٦٥٪ ذات معنى ولا تُنتج كسوراً لا تُقسم.
② والقاعدة الشائعة «نصف درجة لكل صحيح بلا عقوبة على الخاطئ» معطوبة في ذاتها:
   من يختار الخيارات كلها يأخذ الدرجة كاملة.
③ وأثرها التعليمي: حين لا يربح الطالب بالاحتياط ولا بالتوسّع، يصير أفضل ما
   يفعله أن يُعلن اعتقاده كما هو. **فالإجابة تقريرٌ صادق عنه.**

**إعادة الفتح تقتضي حلّ ② أولاً.**

### لم يُفحص بعد 🔍

- 🔍 `editor_strands.js` مكتوبٌ و`editor.js` لا يستورده في اللقطة — أمُوصولٌ
  في المستودع أم ينتظر؟
- 🔍 ضمّ ١١٧ و١١٨ إلى `F-TC`. ⚠️ وبعد الفكّ يظهر ١١٨ حرّاً في قائمة مرشّحي
  ١١٧ — اختياره يعيد بناء الزوج القديم بدل الانضمام إلى الستّة.
- 🔍 زرّا الاقتران في الواجهة (الاستيراد الآلي وحده هو المفحوص).
- 🔍 عين الطالب: سؤالا msq لا سبعة — والمجموع ٢٢ من ٣٤ بنداً.

---

## ⑩ اقتراحٌ بنيويّ للنقاش — بنك على مستوى المقرَّر ⬜

**الحاجة:** «اختبارٌ شاملٌ على منهج الصف» لا يُبنى اليوم إلا بنسخ البنود،
فتتباعد النسخ ويصير التشخيص كذباً.

**العقبة:** عمودٌ مفرد `questions.quiz_id` يخلط مفهومين:
```
من يملك السؤال؟  ⇒  المستوى الصحيح
من يستعمله؟      ⇒  جدول وصل quiz_questions (quiz_id, question_id, position)
```

🔴 **وتصحيحٌ جوهريّ — ١٤ سبتمبر:** كان النصّ يقول «المستوى الصحيح: المقرَّر
لا المادة». **وفُتحت الجداول فظهر ما ينقض شطرَه:**

```
lessons   →  subject_id · level_id      ولا course_id إطلاقاً
courses   →  subject_id · level_id · path_id · elective_group
```

⇒ **الدرسُ الواحد ينتمي إلى عدّة مقرَّرات** تختلف بالمسار. فتعليقُ سؤالٍ
بمقرَّرٍ واحد ودرسُه يمتدّ عبر مقرَّرات **تناقضٌ لا يُحلّ**.

🔑 **والصواب: `subject_id` + `level_id`** — وهو ما تفعله `lessons` أصلاً.
فالعيبُ الذي حذّر منه هذا القسم هو **المادة وحدها** (تجمع الإعدادي بالثانوي)،
و`level_id` بجوارها يرفعه بلا اختراع كيانٍ موازٍ.
*(وقد طُبِّق هذا فعلاً في `decks` — انظر ⑥-ب.)*

**يعمل بلا تعديل:** `variant_key` · البصمة و`variant_report` · `dx_map`
والتشخيص كله · و`objective_id` يصير محور «اختبار شامل على أهداف الصف».

**أثمانٌ تُعرف قبل القرار:**
✅ ① **سقط** (SQL 68–75): الإصدارات بُنيت.
✅ ② **نُفِّذ أكثرُه**: القائمة فُلترت للتقاعد في ٦ دوالّ. ويبقى
   `question_count` في دالّتين — **فخٌّ نائم لا ضرر له.**
③ `can_edit_quiz` تسأل عن الاختبار، والأسئلة تصير ملك (مادة · صفّ).

🔴 **التوقيت: لا الآن — لكنّ الثمن صار أخفّ.** ثمنان من ثلاثة سقطا.
وهذه بنيةٌ تُبنى لحلّ ازدحام، **ولا ازدحام بعد**. ابنِ خمسة دروس كاملة؛
فإن وجدتَ نفسك تنسخ البند إلى اختبارٍ ثالث فقد نضج الوقت. ولا شيء مما
يُبنى اليوم يُهدَر: العمود يتغيّر والمحتوى لا يُمسّ.

---

## ⑪ نمط الفحص — دروسٌ تكرّرت

- **فحصُ RLS يلزمه سطران:** `set_config` يُبدّل الهويّة، و
  `set local role authenticated` يُخضعك للسياسات (المحرّر `postgres`).
- **`set_config` في `from` لا في جملةٍ سابقة** — المحرّر يعرض ناتج جملةٍ واحدة.
- **واختر طالباً يبلغ مستواه الهدف**، وإلا حجبه حارسٌ آخر فبدت الفتحة أصغر.
- 🆕 **وكلُّ دالّة تسأل `auth.uid()` تُرفض في المحرّر بلا هويّة** — وهذا
  سلوكٌ صحيح لا عطل. *(وقع في ١٤ سبتمبر مع `add_my_card`.)*
- 🆕 **كتلةُ فحصٍ بين `/* */` لا تُشغَّل بلصق الملفّ** — تُنسخ وحدها.
- 🆕 **اجعل أرقام الفحص محسوبةً خارج القاعدة** وضعها في عمود «المنتظر»
  بجوار «المقيس». فالفحص الذي لا يقول ما كان يجب أن يكون **يُثبت التشغيل
  ولا يُثبت الصحّة.**
