# CLAUDE.md — بيان

> يُقرأ في كلّ جلسة قبل أيّ عمل. **الأحكام والتاريخ في `STATE.md`، وهذا ملفُّ تشغيل.**

---

## ما هو المشروع

منصّة تعلّم عربية. المعمارية: **GitHub Pages (`index.html`) → Supabase مباشرة.**
لا خادم وسيط ولا Apps Script. الأمان كلُّه في RLS ودوالّ `security definer`.

**وميزتها التي لا يُقترح ما يُفرغها: التشخيص (`dx_codes`).** الطالب لا يُقال له
«أخطأت» بل **أيّ نوعٍ من الخطأ وقع فيه وما علاجه**. وكلُّ مشتّتٍ خاطئ يحمل كوداً.

---

## أسلوب العمل

- **كلّ الردود بالعربية**، ولغةٌ موقّرة عن المعلّم.
- **خطوة واحدة في كلّ مرة.** لا تُجمع خطوتان في ردّ.
- اشرح الفعل ببساطةِ معلّمٍ حاذق، واكتب الشيفرة باحتراف مهندس.
- تطويرٌ عالي القيمة يقتضي تغيير البنية ⇒ **اقتراحٌ للنقاش، لا تنفيذ.**
- **نبّه حين يكون العائق محتوى لا تقنية.**

---

## ١ · قبل أن تكتب سطراً

```bash
# أين نحن؟ — يحلّ محلّ أيّ رقمٍ مكتوبٍ في وثيقة
psql "$DATABASE_URL" -c "select n, title, coalesce(applied_at::date::text,'—') as طبق, note
                           from sql_log order by sql_log_sort(n), n;"

# بمَ تعمل الواجهة؟
grep -n 'BUILD' js/ui.js
```

⚠️ **و`sql/00_schema.sql` لقطةٌ مؤرَّخة لا حقيقة.** يُقرأ للاطّلاع، ولا يُبنى عليه.

---

## ٢ · القواعد التي كلّفت ثمناً — لا تُخرق

### ① لا `create or replace` قبل قراءة الحيّ

```sql
select pg_get_functiondef('public.اسم_الدالّة(الوسائط)'::regprocedure);
```

اقرأ ← قارن ← ثمّ استبدل.

> **٥ سبتمبر ٢٠٢٦:** قُرئ `00_schema.sql` وهو مستخرَجٌ عند الملفّ `50`،
> فاستُبدلت `get_quiz` و`quiz_readiness` و`quiz_for_edit` بنسخٍ أقدم ⇒
> **سقط نمط `gap` والإعدادات** (`plays` · `reveal` · `shuffle` · `tool` ·
> `station`). ولم يظهر عند التشغيل بل عند المعلّم وهو يحفظ سؤالاً.

### ② `STATE.md` حُكمٌ لا مصدرَ أرقام

كلُّ رقمٍ منقولٍ ينحرف. الأرقام في القاعدة، والاستعلامات تجلبها.

### ③ لا تُحِل إلى ملفٍّ قبل أن يوجد

«طبِّق `sql/58`» بقيت في `STATE` شهوراً — **وملفٌّ لم يُكتب قطّ.**
نيّةٌ صيغت أمراً فصُدِّقت.

### ④ الفحص يُصاغ لكلّ حالةٍ بما يخصّها

اختبارٌ واحد على ثلاثة أشياء يُنتج «فشلاً» لا يعني شيئاً — ويُدرِّب الناظر
على تجاهل الإنذار. وذاك أسوأ من غياب الفحص.

### ⑤ الفجوة تُرى والصمت لا يُرى

رقمٌ ملغًى يُسجَّل بسببه · قرارٌ يُشرَح في موضعه · عطلٌ مؤجَّلٌ يُكتب.
**والخطأ الصاخب يُصلَح، والصامت يُصدَّق.**

---

## ٣ · قواعد المحتوى

- **السؤال المُجاب عنه لا يُعدَّل ولا يُحذف — بل يتقاعد** ويولد وريثه في موضعه
  وخانته (`retire_question`). المبدأ: **ما رآه الطالب يُحفظ، والسؤال يتطوّر.**
- **النصّ المشترك عكسه:** يُعدَّل في مكانه، ويُحفَظ ما كان في `passage_history`.
  ثقةٌ موثَّقة، لا صرامةٌ تُعيق ولا ثقةٌ تُخفي.
- **كلُّ مشتّتٍ خاطئ يحتاج كود تشخيص.** واقتراحٌ يُفرغ ذلك مرفوض.
- **الحُجَّة تُكتب حيث يقع السؤال** — لا في ملفٍّ يُفتح مرّةً كلَّ سنة.
  (مثال: أسباب مسارات التسكين في `placement_routes.note`، تُقرأ في الشاشة.)

---

## ٤ · ملفّات `sql/`

```
NN_name.sql          مرقَّمة · تاريخٌ يُقرأ لا رقعةٌ تُعاد
NNb · NNج            لواحق للأجزاء
```

**كلُّ ملفٍّ:**

1. **قابلٌ لإعادة التشغيل** — `create or replace` · `if not exists` ·
   `drop constraint if exists` ثم `add` · `on conflict`.
2. **رأسٌ يقول «لماذا»** لا «ماذا» — الشيفرة تقول ماذا.
3. **يُختَم بسطره:**

```sql
insert into public.sql_log (n, title, applied_at)
values ('NN', 'العنوان', now())
on conflict (n) do update set applied_at = now();
```

4. **يفصل المعدَّل عن المنشأ** بعنوانٍ صريح — دالّةٌ أصلُها في ملفٍّ سابق
   تُوضع تحت «معدَّلة لا منشأة»، وإلا بحث عنها القارئ في موضعها فوجدها ناقصة.

⚠️ **ولا تنسَ `grant execute`** لكلّ دالّة جديدة. القسم `⑧` في المخطّط
يسحب الافتراضيّ يوماً، فما لا يحمل منحاً صريحاً **يسقط بلا شكوى**.

---

## ٥ · حدودٌ لا تُتجاوز

**⛔ لا `delete` ولا `drop` على القاعدة الحيّة بلا إذنٍ صريح في الجلسة.**
والحذف يُعرَض دائماً **تجربةً جافّة أولاً**:

```sql
begin;
  … الحذف …
  select … as الباقي;   -- يُعرض للمستخدم
rollback;                -- تُبدَّل إلى commit بعد اطمئنانه
```

**⛔ لا تُشغَّل ملفّات `sql/` القديمة** — هي تاريخٌ محفوظ. إعادةُ تشغيلٍ
تُرجّع القاعدة إلى الوراء إن كان بعدها ما يعدّلها.

**⛔ والملفّات `69` · `70` · `71` ملغاةٌ نهائياً** — أوامرُ هدمٍ جاهزة للنقر.

**🔑 و`service_role` يتخطّى RLS كلَّه.** استعمل `anon` لأكثر العمل، ولا
تُشغّل بدور القوّة إلا لهجرةٍ تقتضيه.

---

## ٦ · فحص الاتّساق — يُشغَّل عند الشكّ

```bash
# ملفّاتٌ في sql/ لا سجلّ لها ⇒ لم تُشغَّل
ls sql/*.sql | grep -oE '[0-9]+[a-zA-Zب]*' | sort -u > /tmp/files
psql "$DATABASE_URL" -tAc "select n from sql_log" | sort -u > /tmp/logged
comm -23 /tmp/files /tmp/logged   # في الملفّات ولا سجلّ
comm -13 /tmp/files /tmp/logged   # في السجلّ ولا ملفّ  ⇐ نصٌّ مفقود
```

📌 هذا الفحص يكشف ما ظهر بعد شهور: `48` (سياسات التخزين) **مطبَّقةٌ في
القاعدة ولا نصَّ لها في `sql/`**.

---

## ٧ · فحص RLS — درسان تكرّرا

```sql
set local role authenticated;              -- المحرّر postgres ⇒ يتخطّى السياسات
select set_config('request.jwt.claims',
       json_build_object('sub', '‹uuid›')::text, true);
```

- `set_config` **في `from`** لا في جملةٍ سابقة — المحرّر يعرض ناتج جملةٍ واحدة.
- واختر طالباً **يبلغ مستواه الهدف**، وإلا حجبه حارسٌ آخر فبدت الفتحة أصغر.

---

## ٨ · عند الختام

```
① سطر sql_log في الملفّ الجديد
② قرارٌ لا يُقرأ من الشيفرة ⇒ يُكتب في موضعه (تعليق الملفّ أو STATE ⑤)
③ STATE.md · القسم ٠·د «حيث انتهينا» يُحدَّث: ما أُنجز يُشطب وما ظهر يُضاف
④ تغيّرت الواجهة ⇒ BUILD يُرفَع في js/ui.js
⑤ تغيّرت البنية ⇒ 00_schema.sql يُعاد استخراجه، ورأسه يُصحَّح من المتن
```

---

## ٩ · أين تجد ماذا

| | |
|---|---|
| الحُكم والتاريخ والأعطال | `STATE.md` |
| حالة القاعدة الآن | `select … from sql_log` |
| لقطة البنية (مؤرَّخة) | `sql/00_schema.sql` |
| تاريخ الهجرات | `sql/NN_*.sql` |
| نسخة الواجهة | `js/ui.js` سطر ١٠ |

**وابدأ من `STATE.md` القسم `٠ · د` — فيه ما بقي، مصنَّفاً ومؤرَّخاً.**
