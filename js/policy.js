/* ══════════════════════════════════════════════════════════
   بيان — policy.js  ·  الموافقة على سياسة الخصوصية

   🔑 **نسخةُ السياسة هنا وحدها.** تُقرأ في البوابة وفي شاشة إكمال
      الملفّ، وتُرسَل إلى `accept_policy` (121) فتُحفَظ مع وقتها —
      فيُعرف **على ماذا** وافق لا أنّه وافق.

   ⚠️ **وهي تاريخُ «آخر تحديث» في `privacy.html` بحرفه.** موضعان
      لقيمةٍ واحدة يتفارقان — وهو نظيرُ `--page` مع `theme-color`
      في هذا المشروع. **حرّرتَ السياسة؟ حرّك التاريخين معاً**،
      فتُطلب موافقةٌ جديدة ممّن وافق على نصٍّ سابق.

   ⚠️ ولا يحرس هذا الملفُّ باباً: المربّع شرطٌ في الواجهة، والختمُ
      في القاعدة. ولم تُربط الموافقةُ بـRLS — وربطُها يقفل الحسابات
      القائمة في اللحظة نفسِها (انظر رأس 121).
   ══════════════════════════════════════════════════════════ */

export const POLICY_VERSION = '2026-09-25';
export const POLICY_URL     = './privacy.html';

/* مربّعُ الموافقة — نصُّه فعلٌ لا إقرارٌ مبهم، والرابط يُفتح في تبويبةٍ
   أخرى فلا يُفقد ما كُتب في النموذج. */
export const policyCheck = (id = 'pol_ok') => `
  <label class="pol-ck" for="${id}">
    <input type="checkbox" id="${id}">
    <span>قرأتُ <a href="${POLICY_URL}" target="_blank" rel="noopener">سياسة الخصوصية</a>
      وأوافق عليها</span>
  </label>`;

/* الجواب: هل نُقر؟ — تُنادى قبل الإرسال، والرسالةُ واحدةٌ في الموضعين */
export const policyOk = (root, id = 'pol_ok') =>
  !!root.querySelector('#' + id)?.checked;

export const POLICY_MSG = 'تُقرأ سياسة الخصوصية ويُوافَق عليها للمتابعة';
